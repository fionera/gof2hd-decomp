#include "engine/core/KeyFrame.h"

// KeyFrame's constructor is defined inline in the header so the complete-object C1 folds into the
// base-object C2 the original exports -- see docs/VALIDATION.md.
