- [ ] src/game/weapons/ObjectGun.h: ObjectGun (the polymorphic base) should declare the full predicate trio as virtuals returning 0 -- `virtual int isBombGun();` and `virtual int isMineGun();` alongside the existing `virtual int isRocketGun();`. The Android binary's vtables (e.g. 0x223608, 0x21e030) place isRocketGun(0x1668c4 ->0), isBombGun(0x1668c8 ->0), isMineGun(0x1668cc ->1) in three consecutive slots, so MineGun::isMineGun is truly an override of a base virtual. We only model isRocketGun on the base today, so isMineGun is currently declared `virtual` directly on MineGun (correct symbol/mangling, returns 1) rather than `override`. Adding the base virtuals would let this become a proper override and keep the vtable slot ordering identical to the original.

## Coordinated: setPosition3 -> setPosition(float,float,float) family rename
Original binary names the actor 3-float positioner `setPosition(float,float,float)` (KIPlayer @0xb6394
+ overload `setPosition(Vector const&)` @0xb6360); our tree calls it `setPosition3` and `setPosition_vec`.
NOT a blind rename: some classes (PlayerEgo.h:344) ALREADY declare `setPosition(float,float,float)`
alongside `setPosition3` (redundant) -> those need the `setPosition3` decl/def DELETED and call sites
repointed, while classes with only `setPosition3` need a true rename. Do as one careful coordinated
pass (base KIPlayer + derived PlayerFighter/Jumpgate/WormHole/FixedObject/Ego + call sites in
CutScene/SpaceLounge/Level/SentryGun/TouchButton), build-gating every affected TU, verifying each
`_ZN..11setPositionEfff` matches. Also rename `setPosition_vec` -> `setPosition` (the Vector overload).
- [ ] `_ZN11AbyssEngine8KeyFrameC1Ev` (extra:KeyFrame:11): The original .so has NO C1 anywhere: address 0x75934 carries only _ZN11AbyssEngine8KeyFrameC2Ev (verified against _work/symbols TSV; the sole ctor caller, Transform::InsertKeyFrame @0x6f838, calls C2 directly). The complete-object ctor C1 was an unreferenced constructor-alias that the original link dropped via --gc-sections. Clang 7.0.2 emits BOTH C1 and C2 as full definitions from a single out-of-line ctor body (both at offset 0 in our object), and there is NO idiomatic, in-component, C++14 way to suppress just the standalone C1: doing so would require either a non-idiomatic __attribute__((alias))/asm hack (a shortcut) or a link-time --gc-sections / global-flag change in the build system (files outside this component). The per-object verify model (nm of our .o vs the linked binary's symbol table) cannot see linker GC, so a dead C1 alias will always surface as an 'extra'. This is the same benign ctor/dtor-alias class as the other ~790 informational extras; the 'extra' metric is explicitly Informational, not gated. The real symbol (C2) is now correctly recovered, so deferring the C1-only removal is the non-shortcut choice.
- [ ] `_ZN11BoundingAABD0Ev` (extra:BoundingAAB:13): Cross-hierarchy vtable-emission problem, NOT a self-contained BoundingAAB fix. Verified against the delinked original: target/engine/math/BoundingAAB.o emits ONLY D1/D2 (coalesced at one addr, a thin tail-call into the BoundingVolume dtor) and NO D0 deleting destructor, NO _ZTV/_ZTI/_ZTS. The same holds for BoundingVolume.o (D1/D2 only) and BoundingSphere.o (D1/D2 only); all three D0 symbols appear together in extra.txt. A scan of EVERY original .o shows zero TUs define OR reference _ZTV/_ZTI/_ZTS or D0 for BoundingVolume/BoundingAAB/BoundingSphere -- the vtables and deleting destructors are absent from the entire program. Our build emits _ZN11BoundingAABD0Ev plus _ZTV11BoundingAAB/_ZTI/_ZTS because clang anchors the vtable (and therefore D0) in the TU holding the key function (first out-of-line virtual). To suppress D0 here we must relocate where the vtables/key functions anchor across the hierarchy. That requires editing BoundingVolume.h (the base, outside this component) and coordinating BoundingVolume.cpp + BoundingSphere.cpp/.h so the vtable+typeinfo+D0 of all three classes are emitted nowhere (matching the original) -- a coordinated cross-hierarchy change to virtual/key-function placement. Per the HARD STOP rule for 'extra', this cannot be a caller_rewrite and must not be attempted from BoundingAAB.cpp/.h alone (would leave the base/derived TUs inconsistent). Base: BoundingVolume; derived: BoundingAAB, BoundingSphere. Needs a unified vtable-emission strategy across engine/math/Bounding*.
- [ ] `_ZN16ParticleSettingsD1Ev` (extra:ParticleSettings:28): Not a genuine extra; it is a stripped-binary / Ghidra recovery artifact, and there is no ABI-correct source-level way to suppress it. EVIDENCE: (1) The original .so is stripped; the 'binary_names' set verify compares against is the Ghidra-derived android_2.0.16.symbols.tsv. That TSV lists _ZN16ParticleSettingsD2Ev at 0x185474 (the dtor body) but NO D1 entry, and lists BOTH _ZN16ParticleSettingsC1Ev and C2Ev at the same address 0x183fe8. (2) The dtor body at 0x185474 (push {r4,r5,r7,lr}; add r7,sp,#8; mov r4,r0; mov.w r5,#0x1d60; loop: adds r0,r4,r5; blx String::~String; subs r5,#0xa0; adds.w r0,r5,#0xa0; bne; mov r0,r4; pop) matches our emitted D1/D2 body byte-for-byte. (3) ParticleSettings has a non-trivial out-of-line destructor (it destroys the 48 embedded String 'name' members). For any class with no virtual bases and a defined out-of-line dtor, Clang 7 (the match toolchain) ALWAYS emits D1 and D2 as two names folded to ONE address -- verified with a minimal 'struct S{int a;~S();}; S::~S(){}' which emits both _ZN1SD1Ev and _ZN1SD2Ev at offset 0. The original is identical: D1 was emitted aliased to D2 at the same address, the linker folded them, the binary was stripped, and Ghidra recovered only ONE name (D2) for that single address. Every comparable non-virtual-base class that retained symbols (ParticleSystemManager, SolarSystem, StarSystem, LODManager, SystemPathFinder, ApplicationManager) shows BOTH D1+D2 at one address -- ParticleSettings simply had only the D2 name recovered. (4) Eliminating D1 while keeping D2 is impossible without an ABI-violating hack (e.g. a hand-written .set alias or inlining tricks) that would misrepresent the original's real codegen and is exactly the kind of shortcut the rules forbid. The fix would require changing the verify oracle's symbol set (add the D1 alias name at 0x185474 to the TSV), which is outside this component's two files. No source change to ParticleSettings.cpp/.h is correct here.
- [ ] `_ZN14PlayerJumpgate11setPositionEfff` (extra:PlayerJumpgate:31): Cross-hierarchy virtual rename (HARD STOP). The original binary's actor-vtable slot +0x48 virtual is named 'setPosition(float,float,float)', NOT 'setPosition3' — there is NO 'setPosition3' symbol anywhere in android_2.0.16_libgof2hdaa.so. Ghidra confirms 0xb51c0 is named setPosition and is referenced from vtable data at 0x221f34 and 0x21d8ac (so it IS the polymorphic slot-+0x48 override, not a standalone). Its body (store positionX/Y/Z at +0x124/+0x128/+0x12c as int, then tail-call geometry->setPosition(x,y,z) via [this+8]) matches our current PlayerJumpgate::setPosition3 exactly. To make our override mangle to _ZN14PlayerJumpgate11setPositionEfff while keeping it a real 'override', the BASE virtual must be renamed: KIPlayer.h:151 'virtual void setPosition3(float,float,float)' -> 'setPosition', and EVERY derived 'override' + all call sites updated in lockstep. Affected files (all outside this component): src/game/ship/KIPlayer.h, KIPlayer.cpp, PlayerEgo.h/.cpp, PlayerFighter.h/.cpp, PlayerFixedObject.h/.cpp, PlayerGasCloud.h, PlayerWormHole.h/.cpp, plus call sites in src/game/world/Level.cpp, src/game/core/CutScene.cpp, src/game/menu/SpaceLounge.cpp, src/game/weapons/SentryGun.cpp, src/game/ui/TouchButton.h/.cpp, src/engine/render/AEGeometry.cpp. Coordinated base-virtual rename across the KIPlayer hierarchy is required; cannot be done from this component without breaking every derived TU.
- [ ] `_ZN14PlayerJumpgate12setPosition3Efff` (extra:PlayerJumpgate:31): Same single function as the absent _ZN14PlayerJumpgate11setPositionEfff (see that entry). Our 'setPosition3' is the mis-named version of the original 'setPosition' slot-+0x48 virtual override. It cannot be renamed in isolation because it is declared 'override' of KIPlayer's slot-+0x48 virtual, which our codebase also mis-named 'setPosition3'. Renaming requires the coordinated cross-hierarchy base-virtual rename described in the paired entry, touching ~20 files outside this component. Deferred as the paired half of the same fix; do not delete this symbol without renaming the base virtual or the override would no longer occupy vtable slot +0x48.
- [ ] `_ZN14PlayerWormHole11setPositionEfff` (extra:PlayerWormHole:32): This absent symbol (the actor vtable slot +0x48 position setter at 0xb5336) is the SAME function as the 'extra' _ZN14PlayerWormHole12setPosition3Efff. Ghidra confirms: vtable _ZTV14PlayerWormHole @0x21d8d0 slot +0x48 (=0x21d920) points at 0xb5336, and 0xb5336 has NO code xrefs, only DATA xrefs from vtables (0x21d920, 0x221f6c) -> it is purely the virtual slot override. The function body is byte-identical to our existing PlayerWormHole::setPosition3 (stores floats at +0x58/+0x5c/+0x60, truncated ints at +0x124/+0x128/+0x12c, then tail-calls this->geometry->positionChanged()). The ONLY problem is the name: in the original, the actor virtual at slot +0x48 is mangled 'setPosition', not 'setPosition3'. This is a hierarchy-wide misnaming: report.json flags _ZN8KIPlayer12setPosition3Efff, _ZN9PlayerEgo12setPosition3Efff, _ZN13PlayerFighter12setPosition3Efff, _ZN17PlayerFixedObject12setPosition3Efff, _ZN14PlayerJumpgate12setPosition3Efff ALL as 'extra', while _ZN9PlayerEgo11setPositionEfff is PRESENT (match 9.5). Correct fix = rename the BASE virtual KIPlayer::setPosition3(float,float,float) -> setPosition(float,float,float) (declared 'virtual' in src/game/ship/KIPlayer.h:151) and every derived 'override' (PlayerWormHole, PlayerEgo, PlayerFighter, PlayerFixedObject, PlayerJumpgate) plus all call sites (Level.cpp, CutScene.cpp, SentryGun.cpp, SpaceLounge.cpp, KIPlayer.cpp, the *.cpp/*.h above). That coordinated cross-hierarchy virtual rename touches headers/overrides outside this component, so it is a HARD STOP and cannot be done as a self-contained caller_rewrite. Renaming only PlayerWormHole's method would break the 'override' (base would still be setPosition3) and corrupt the vtable.
- [ ] `_ZN14PlayerWormHole12setPosition3Efff` (extra:PlayerWormHole:32): Same function as the paired absent _ZN14PlayerWormHole11setPositionEfff (see that entry). Our PlayerWormHole::setPosition3 override IS the original slot-+0x48 function at 0xb5336 with a correct body; it is only mis-NAMED. The original name is 'setPosition'. Cannot rename in isolation: setPosition3 is a virtual declared in base KIPlayer (KIPlayer.h:151 'virtual void setPosition3(float,float,float)') and overridden across the whole actor hierarchy; renaming just PlayerWormHole's override to 'setPosition' would no longer match the base virtual name, breaking the override and the vtable slot. Requires the same coordinated base+derived+callers rename described in the paired entry, which spans files outside this component (KIPlayer.h and sibling ship classes + Level/CutScene/SentryGun/SpaceLounge call sites). HARD STOP per cross-hierarchy-virtual rule; not expressible as caller_rewrites.
- [ ] `_ZN11AbyssEngine12ResourceMeshC1EPKctb` (extra:ResourceMesh:34): Not a real defect — symbols.tsv/verify attribution artifact that cannot be fixed from within ResourceMesh.cpp/.h without a shortcut. The original .so has ResourceMesh C2 at 0x112cf0 (=ghidra 0x122cf0) and our C2 body byte-matches it exactly (verified via orbobjdump: same push/strb/strh/blx String_GetStringLength/blx _Znaj/memcpy sequence). Clang on this armv7-thumb target ALWAYS emits BOTH the complete-object (C1) and base-object (C2) constructor variants for a concrete, non-abstract class, co-located at the SAME address (our orbnm shows C1 and C2 both at offset 0). The original binary likewise had C1 as a same-address alias of C2, but _work/symbols/android_2.0.16.symbols.tsv records only ONE name per address (delink.py: 'nm attribute only ONE name per address'), so only C2 was captured. verify.py's extra-symbol check compares raw NAMES against binary_names (the tsv name set) WITHOUT the C1/C2 alias-collapse it applies on the coverage side, so our legitimate C1 alias is flagged 'extra'. There is no C++14 source-level way to make Clang emit C2 but suppress the C1 alias for a concrete class without changing C2/D2's linkage from strong T to weak comdat (e.g. marking the ctor inline), which would itself diverge from the original's strong symbols. The correct fix is in the verify tooling / symbols.tsv (record C1/D1 aliases, or extend verify.py's alias-collapse to the extra-symbol set) — files outside this component. Per verify.py's own comment, extra here is 'mostly intentional ... informational, not gated.'
- [ ] `_ZN11AbyssEngine12ResourceMeshD1Ev` (extra:ResourceMesh:34): Same root cause as the C1 entry (paired). Original has ResourceMesh D2 at 0x77ed0 (=ghidra 0x87ed0); our D2 body byte-matches exactly. Clang emits both D1 (complete-object) and D2 (base-object) destructor variants co-located at the same address (our orbnm: D1 and D2 both at offset 0x30) — D1 is a legitimate same-address alias of the matching D2. symbols.tsv recorded only D2 (one name per address), so our D1 alias is flagged 'extra'. Cannot be removed from ResourceMesh.cpp/.h without forcing weak/comdat linkage that diverges D2 from the original's strong symbol. Fix belongs in verify tooling/symbols.tsv (out of component).
- [ ] `_ZN11AbyssEngine8KeyFrameC1Ev` (extra:KeyFrame:10): ALIAS-COLLAPSE ARTIFACT, not a source defect. Verified in the original symbol table (_work/symbols/android_2.0.16.symbols.tsv): the ONLY KeyFrame ctor symbol the original .so retains is _ZN11AbyssEngine8KeyFrameC2Ev @ 0x75934 (Ghidra 0x85934). KeyFrame is a leaf class with no virtual/virtual bases, so for its single out-of-line ctor definition Clang (NDK r18b clang 7.0.2, the matching toolchain) UNCONDITIONALLY emits BOTH C1 and C2 as aliases of the same body. Confirmed via orbobjdump -t on src/engine/core/KeyFrame.cpp's object: both _ZN11AbyssEngine8KeyFrameC1Ev and _ZN11AbyssEngine8KeyFrameC2Ev are global F .text at offset 0x0, size 0x4a (identical address = alias pair). The original's `new KeyFrame()` sites in Transform.cpp (AbyssEngine::Transform::InsertKeyFrame, e.g. 0x85f10) call the ctor via the PLT thunk 0x6f838 which resolves to the C2 symbol, i.e. complete-object construction binds to C2; the aliased C1 name was simply dropped from the final .so symbol table (one name kept per address). There is NO C++14, file-local (KeyFrame.cpp/.h-only), no-C-cast source construct that suppresses Clang's C1 emission: tested an `asm(".set C1, C2")` alias (ignored, frontend still emits C1), and tested co-locating the ctor body with a `new KeyFrame()` in the same TU (still emits both C1 and C2). Removing the extra C1 requires either (1) the verify framework to collapse extra ctor/dtor aliases by ADDRESS the same way it already collapses the 'missing' side (the right fix; verify.py's `extra` set is purely name-based and does not address-collapse, unlike its coverage/missing logic which explicitly notes nm keeps only one name per address), or (2) a final-link ICF/alias-resolution step on the .so. Both are outside this component (cannot edit verify.py / link scripts; cannot change Transform.cpp caller codegen). Not fixable from KeyFrame.cpp/.h without a non-portable shortcut. This pattern is shared by at least 3 other extra C1 entries in extra.txt (Material, IApplicationModule, GameData) whose originals likewise retain only C2.
- [ ] `_ZN11BoundingAABD0Ev` (extra:BoundingAAB:11): This deleting destructor is 'extra' because our header declares `virtual ~BoundingVolume()`. The original binary's BoundingVolume destructor is NON-VIRTUAL: the base vtable (0x21dd8c, installed by the ctor at 0x12f6dc as vtable+8) has exactly 5 function slots and none is a destructor; ~BoundingVolume (0x12f718) is referenced only as a plain function / GOT-relocation entry (0x221910, 0x222258), never occupying a vtable slot. The BoundingAAB vtable (GOT[0x220228]=0x21d834, installed by ctor at 0xa8104 as +8) mirrors this with 5 slots [0xa8270 getCollisionNormal zero-writer, 0xa827a thunk, 0xa819c collide, 0xa81d2 outerCollide, 0xa8280 projectCollisionOnSurface] and NO D1/D0 destructor pair. Because the original dtor is non-virtual, the compiler never emits a deleting destructor D0, so no _ZN11BoundingAABD0Ev exists. Additionally getCollisionNormal IS a virtual (vtable slot 0, the zero-Vector writer), but our header makes it non-virtual. The correct fix is a COORDINATED CROSS-HIERARCHY change: in BoundingVolume.h (OUTSIDE this component) change `virtual ~BoundingVolume()` to a non-virtual `~BoundingVolume()` and make `getCollisionNormal` virtual; then update the derived classes BoundingSphere and BoundingAAB to match (non-virtual dtors, getCollisionNormal as override/virtual). This flips a base-class destructor's virtualness and alters the shared vtable layout that BoundingSphere and BoundingAAB both depend on, which is a HARD-STOP type-system change spanning files I do not own (BoundingVolume.h, BoundingSphere.h/.cpp). It cannot be done as a self-contained call-site rewrite.
- [ ] `_ZN16ParticleSettingsD1Ev` (extra:ParticleSettings:14): Systemic ABI/codegen artifact, NOT a per-function logic bug. The original binary contains only the base-object destructor _ZN16ParticleSettingsD2Ev (single body at android V=0x195474: loops r5 from 0x1d60 down by 0xa0, calling AbyssEngine::String dtor @0x6ee30 over all 48 embedded set names, returns this). Our build matches that D2 at 100%, but Clang 7.0.2 (NDK r18b, the fixed match toolchain) ALSO emits a standalone complete-object destructor D1 at the same body offset, which the original toolchain unified into D2 and never emitted as a distinct symbol. This is confirmed cross-cutting: the verify report lists 13 classes with the identical 'extra D1, present D2' pattern (AbyssEngine::MeshMerger/ResourceMesh/EaseInOutMatrix/ResourceTexture/ResourceTransform/Radar/Trail/Material, IParticleSystem, ParticleSettings, ApplicationManager, IApplicationModule, GameData). There is NO clean in-component fix: ParticleSettings has a normal non-deleted destructor, so any C++14 source that yields a matching D2 also yields D1; dropping only D1 would require an __attribute__((alias))/inline-asm '.set' hack (not real hand-written C++, a shortcut) or a shared build/codegen-level mechanism applied uniformly to all 13 classes. match_flags.sh has no D1-suppression knob and the flags are fixed. Per the HARD STOP rule this needs a coordinated cross-file/build change, not a per-function edit smuggled into ParticleSettings.cpp/.h.
- [ ] `_ZN14PlayerJumpgate11setPositionEfff` (extra:PlayerJumpgate:15): CROSS-HIERARCHY VIRTUAL RENAME (HARD STOP). The original binary names the actor-vtable slot +0x48 virtual 'setPosition(float,float,float)' for the entire KIPlayer-derived hierarchy (orig symbols: KIPlayer 0xa6394, PlayerEgo, PlayerJumpgate 0xa51c0, PlayerWormHole, PlayerFighter, PlayerFixedObject). Our codebase fakes this virtual under the project-wide name 'setPosition3'. The base virtual is declared `virtual void setPosition3(float,float,float)` in src/game/ship/KIPlayer.h:151 (NOT in my component). To make PlayerJumpgate mangle to _ZN14PlayerJumpgate11setPositionEfff I must rename that base virtual KIPlayer::setPosition3 -> setPosition and rename every derived `override` + definition + call site in lockstep. Renaming only PlayerJumpgate's override to setPosition (with override) would not compile because no base virtual setPosition(fff) exists. Coordinated change required across files OUTSIDE my component: base virtual in src/game/ship/KIPlayer.h (and its def in KIPlayer.cpp:422); derived override decls/defs in PlayerWormHole.h/.cpp, PlayerFixedObject.h/.cpp, PlayerFighter.h/.cpp, PlayerEgo.h/.cpp; and ~20 call sites in src/game/core/CutScene.cpp, src/game/world/Level.cpp, src/game/weapons/SentryGun.cpp, src/game/menu/SpaceLounge.cpp, src/game/ship/PlayerFixedObject.cpp, src/game/ship/PlayerFighter.cpp, src/game/ship/PlayerEgo.cpp, src/game/ship/PlayerWormHole.cpp. Note: TouchButton::setPosition3(int,int,int) is a DIFFERENT unrelated method and must NOT be renamed.
- [ ] `_ZN14PlayerJumpgate12setPosition3Efff` (extra:PlayerJumpgate:15): SAME VIRTUAL as the absent _ZN14PlayerJumpgate11setPositionEfff entry. 'setPosition3' is the project's fake name for the actor-vtable +0x48 virtual that the binary names 'setPosition(float,float,float)'. It cannot be removed/renamed in PlayerJumpgate alone because it overrides the base virtual KIPlayer::setPosition3 (src/game/ship/KIPlayer.h:151) which is outside my component. Requires the same coordinated base-class virtual rename described for _ZN14PlayerJumpgate11setPositionEfff.
- [ ] `_ZN14PlayerWormHole11setPositionEfff` (extra:PlayerWormHole:16): This is the original (correct) name for the polymorphic position setter that our port calls 'setPosition3'. It is a VIRTUAL method at actor vtable slot +0x48, DECLARED in the base class KIPlayer (src/game/ship/KIPlayer.h:151 'virtual void setPosition3(float,float,float)') and overridden across the hierarchy. To emit setPosition under the original mangled name I would have to rename the base virtual KIPlayer::setPosition3 -> setPosition AND every derived override AND all call sites -- a cross-hierarchy type-system change in files outside my component. HARD STOP per instructions: base-class virtual renames must not be done piecemeal or via caller_rewrites. Paired with the 'extra' _ZN14PlayerWormHole12setPosition3Efff below -- they are the same vtable slot. NOTE: the original body also differs from our setPosition3 (stores float x/y/z at +0x58/5c/60 and int(x/y/z) at +0x124/128/12c, then TAIL-CALLS a single-arg veneer passing this->geometry at +0x8 -- NOT the getPosition()/setPosition() pair our current body uses); the body should be corrected at the same time as the rename.
- [ ] `_ZN14PlayerWormHole12setPosition3Efff` (extra:PlayerWormHole:16): Our build's name for the same vtable-slot-+0x48 virtual that the original binary calls 'setPosition'. The original has NO setPosition3 symbol -- our whole hierarchy mis-names this virtual. Fixing requires the coordinated rename of the base virtual KIPlayer::setPosition3 -> setPosition (src/game/ship/KIPlayer.h:151) plus the overrides in PlayerWormHole, PlayerJumpgate, PlayerEgo, PlayerFixedObject, PlayerFighter, PlayerGasCloud (all *.h/*.cpp) and every call site (game/world/Level.cpp, game/core/CutScene.cpp, game/weapons/SentryGun.cpp, game/ui/TouchButton.{h,cpp}, game/menu/SpaceLounge.cpp, engine/render/AEGeometry.cpp, plus KIPlayer.cpp/PlayerEgo.cpp/PlayerFighter.cpp/PlayerFixedObject.cpp/PlayerJumpgate.cpp). HARD STOP: base-class virtual rename across files I don't own -- cannot be a caller_rewrite. Must be done as one coordinated hierarchy-wide pass. Paired with the 'absent' _ZN14PlayerWormHole11setPositionEfff above.
- [ ] `_ZN11AbyssEngine12ResourceMeshC1EPKctb` (extra:ResourceMesh:17): Benign Itanium-ABI alias artifact, not fixable by editing ResourceMesh.cpp/.h. The original binary's symbol table (android_2.0.16.symbols.tsv) records ONLY the C2 (base-object) constructor _ZN11AbyssEngine12ResourceMeshC2EPKctb at 0x112cf0; it has no C1 entry. Our build correctly emits C2 (which matches the original asm byte-for-byte: strb r3,[r0,#6]; strh r2,[r0,#4]; String::GetStringLength; operator new[]; __aeabi_memcpy) AND, as the Itanium C++ ABI mandates for any concrete non-virtual-base class, the C1 (complete-object) alias at the same address. Clang cannot be made to emit C2 without also emitting C1 for a normal class; suppressing C1 would require making the class abstract (pure virtual), but the asm shows NO vtable store, so that would be wrong and break the match. This is one of 80 classes in the original (incl. all Array<T> instantiations, ResourceTexture, ResourceMaterial, Camera, Matrix) that recorded C2-only. Per verify.py line 296-299 the 'extra' list is explicitly 'Informational, not gated' and notes ctor/dtor aliases are expected scaffolding. No coordinated cross-file change can remove this without an ABI-violating shortcut.
- [ ] `_ZN11AbyssEngine12ResourceMeshD1Ev` (extra:ResourceMesh:17): Same benign Itanium-ABI alias situation as the C1 constructor. The original binary records only the D2 (base-object) destructor _ZN11AbyssEngine12ResourceMeshD2Ev at 0x77ed0 (matching our asm byte-for-byte: operator delete[](this->name); this->name=0). Our build emits D2 (matches) plus the unavoidable D1 (complete-object) alias at the same address. There is no vtable/virtual destructor in the original asm, so emitting only D2 without D1 is impossible for a normal concrete class without an ABI-breaking shortcut. Non-gated/informational per verify.py.
- [ ] `_ZN11AbyssEngine15ResourceTextureC1EPKcf` (extra:ResourceTexture:22): FALSE-POSITIVE extra, NOT fixable in source. Our object emits BOTH C1 and C2 for this ctor at the IDENTICAL offset (verified: orbnm shows _ZN...C1EPKcf and _ZN...C2EPKcf both at offset 0x0) — they are co-located ABI aliases that clang always emits for a complete-object ctor, and the ORIGINAL .so has both too. The original body exists (Ghidra 0x122c88, decompiled body matches our impl exactly: String::GetStringLength + operator new[] + __aeabi_memcpy + store value). The original is flagged 'extra' only because _work/symbols/android_2.0.16.symbols.tsv recorded ONLY the C2 alias (00112c88 _ZN11AbyssEngine15ResourceTextureC2EPKcf) — nm attributes a single name per aliased address, exactly the situation verify.py's own comment documents ('aliased sibling looks missing... only ONE name per address'). No source/header edit can drop C1 while keeping C2 without breaking the ABI; both are legitimate. Fix belongs in the verify tooling's symbol-attribution (collapse C1/C2/D1/D2 aliases), outside this component's two files.
- [ ] `_ZN11AbyssEngine15ResourceTextureC1ERKNS_6StringEf` (extra:ResourceTexture:22): FALSE-POSITIVE extra, same as the char* ctor. orbnm shows C1 and C2 variants both at offset 0x32 (aliased). Original body at Ghidra 0x12b6d2 (symbols.tsv 0011b6d2 records only the C2 alias _ZN11AbyssEngine15ResourceTextureC2ERKNS_6StringEf); decompiled body matches our impl (String::GetAEChar + length+1 + operator new[] + memcpy + value + operator delete[]). Both C1 and C2 are real ABI symbols the original also defines; only C2 was captured in the symbol table. Not fixable without a tooling-side alias collapse outside this component.
- [ ] `_ZN11AbyssEngine15ResourceTextureD1Ev` (extra:ResourceTexture:22): FALSE-POSITIVE extra, same aliasing. orbnm shows D1 and D2 both at offset 0x6e (co-located aliases). Original dtor body at Ghidra 0x87ebc (symbols.tsv 00077ebc records only the D2 alias _ZN11AbyssEngine15ResourceTextureD2Ev); decompiled body matches our impl (operator delete[] name + name=null). ResourceTexture has no vtable (non-virtual dtor — both D1 and D2 are the same non-virtual complete-object dtor), so there is no D0 and no polymorphism question. Both D1/D2 are legitimate ABI symbols present in the original; only D2 is in the symbol table. Fix is a verify-tooling alias collapse, not a source change in this component.
- [ ] `_ZN11AbyssEngine6CameraC1Efffff` (extra:Camera:23): C1 (complete-object) is an extra compiler-emitted constructor variant; the original binary retains ONLY C2 (_ZN11AbyssEngine6CameraC2Efffff, present and compared in the report at ~90%). clang on this armv7/-Oz target emits BOTH C1 and C2 for any out-of-line ctor definition, and there is no clean C++14 construct that suppresses C1 while keeping a normal out-of-line ctor body (the original only dropped C1 because no site odr-used the complete-object variant and the linker GC'd it). This is the identical, still-open systemic state as _ZN9SpriteGunC1EP3Guni, _ZN11AbyssEngine12ResourceMeshC1EPKctb and _ZN11AbyssEngine15ResourceTextureC1EPKcf (all C2 matched, C1 still flagged extra). Fixing it requires a build/link-level change (ctor-variant aliasing or section GC of unreferenced C1), not a per-component source edit. The C2 body itself is now correct: default-constructs projection (Matrix()) and forwards to the 6-arg free function CameraSetPerspective(fov,aspect,near,far,param5,this).
- [ ] `_ZN11AbyssEngine8MaterialC1Ev` (extra:Material:24): Link-time-dropped alias, NOT a source divergence. Our TU correctly emits C1 and C2 as ALIASES at the same address (orbnm: C1Ev and C2Ev both @0x00). The original .so symbol table (delinker DEFAULT_SYMS + nm -D) has ONLY the weak C2 (_ZN11AbyssEngine8MaterialC2Ev @0x7c188) and no C1 anywhere, because the unreferenced weak C1 alias was garbage-collected during the FINAL .so link step (callers like PaintCanvas::MaterialCreate @0x89e4c call C2 directly via operator new(0x74)+Material::Material). A minimal trivial class compiled under the exact match flags ALSO emits both C1 and C2 aliases, proving this is unavoidable default Clang-7/ARM codegen at the .o level. Suppressing C1 at source level is impossible without also suppressing the C2 the original DOES have (e.g. visibility attrs or deleting the ctor body would break C2 and MaterialCreate). Fix belongs at the link/build stage (drop unreferenced weak ctor/dtor aliases), not in Material.cpp/Material.h.
- [ ] `_ZN11AbyssEngine8MaterialC1EPS0_` (extra:Material:24): Same link-time-alias situation as MaterialC1Ev. Our TU emits C1EPS0_ and C2EPS0_ as aliases at the same address (orbnm: both @0x98). Original .so has only weak _ZN11AbyssEngine8MaterialC2EPS0_ @0x7a3d8; the C1 clone-ctor alias was GC'd at final link. Not suppressible per-TU at source level without breaking the C2 the original keeps.
- [ ] `_ZN11AbyssEngine8MaterialD1Ev` (extra:Material:24): Same link-time-alias situation. Our TU emits D1Ev and D2Ev as aliases at the same address (orbnm: both @0x162). Original .so has only weak _ZN11AbyssEngine8MaterialD2Ev @0x7c8b8 and no D1/D0; the unreferenced D1 alias was GC'd at final link. Material is non-polymorphic (0x74 bytes, no vtable, dtor just deletes the 5 Array members), so no virtual D0 is needed. Not suppressible per-TU at source level without breaking the D2 the original keeps.
- [ ] `_ZN17ScrollTouchWindow6scrollEi` (extra:ScrollTouchWindow:31): INLINED-ONLY: the original binary has no ScrollTouchWindow::scroll symbol; it was inlined. Its only caller is SpaceLounge::onKeyPress (out-of-component, src/game/menu/SpaceLounge.cpp:668/670, mode==2 branch). Unlike the other three forwarders, scroll's body is multi-statement and touches ScrollTouchBox internals (box->contentHeight, box->height, box->scrollOffset, box->update(0)) and would require adding #include "game/ui/ScrollTouchBox.h" to SpaceLounge.cpp plus a multi-line body insertion at two call sites — beyond a self-contained caller_rewrite find/replace. Moreover the original SpaceLounge::onKeyPress (0x17e2f0) is structurally different from our decomp (it manipulates dialogPage/scrollOffset fields directly, not via a scroll() call), so the correct inlining must be done as part of matching SpaceLounge::onKeyPress in that component. Defer until SpaceLounge is matched; then inline scroll's body there and delete the standalone.
- [ ] `_ZN12ChoiceWindow13setButtonTextERKN11AbyssEngine6StringES3_` (extra:ChoiceWindow:32): Original binary has NO ChoiceWindow::setButtonText (confirmed via Ghidra list_methods; our header comment marks it an invented 'engine-name forwarder'). Correct fix is INLINED-ONLY: the only caller is SpaceLounge::startChat (src/game/menu/SpaceLounge.cpp:1079), where in the original the title/body + button labels are set by a SINGLE ChoiceWindow::set(title, message, hasButtons, left, right, single, -1, -1) call that subsumes BOTH this->choiceWindow->setText(title,body) (line 1078) and ->setButtonText(left,right) (line 1079). I cannot emit this as a caller_rewrite because (a) it merges two call-site statements into one 8-arg call and (b) the 6th 'single' argument cannot be recovered confidently from asm/DeepOpen. The body also reads private members leftButton/rightButton + calls TouchButton::setText, so it is not a self-contained argument-only inline. Needs a coordinated SpaceLounge.cpp rewrite verifying the exact 8-arg set() arguments against the original SpaceLounge::startChat disassembly.
- [ ] `_ZN14BoundingVolume25projectCollisionOnSurfaceERKN11AbyssEngine6AEMath6VectorE` (extra:BoundingVolume:38): In the original binary BoundingVolume::projectCollisionOnSurface is a PURE virtual: vtable _ZTV14BoundingVolume (file 0x20dd8c) slot +0x10 = __cxa_pure_virtual (0x1a2d68), and there is NO standalone symbol. Our header declares it as a non-pure virtual, which emits the extra body. Making it `= 0` is a coordinated cross-hierarchy change: BoundingSphere and BoundingAAB derive from BoundingVolume and override projectCollisionOnSurface, and the base's vtable layout (5 slots: getCollisionNormal/update/collide/outerCollide/projectCollisionOnSurface, with a NON-virtual dtor) must be recovered as a unit across BoundingVolume.h + BoundingSphere/BoundingAAB. Out of this component's scope; needs the whole hierarchy edited together.
- [ ] `_ZN14BoundingVolumeD0Ev` (extra:BoundingVolume:38): Original BoundingVolume has only D1/D2 (same addr 0x11f718) and NO D0; the destructor is NON-virtual (the ctor/dtor set vptr = vtable+8, i.e. it points at slot[0]=getCollisionNormal, so the dtor is not in the vtable at all). Our header declares `virtual ~BoundingVolume()`, which emits the deleting destructor D0 and inserts the dtor into the vtable. Flipping the destructor to non-virtual is a HARD-STOP cross-hierarchy change: it alters the vtable layout shared by derived BoundingSphere and BoundingAAB. Must be done together with making getCollisionNormal virtual (slot 0) and projectCollisionOnSurface pure-virtual, across all three classes' headers.
- [ ] `_ZN14BoundingVolumeC1Effffff` (extra:BoundingVolume:38): Original has only C2 (base-object ctor, 0x11f6dc) and NO C1 (complete-object ctor). BoundingVolume is only ever constructed as a base subobject (BoundingAAB/BoundingSphere `: BoundingVolume(...)`), never as a complete object. The spurious C1 emission is tied to the same vtable/non-virtual-dtor structure as the D0/projectCollisionOnSurface deferrals (the dtor-in-vtable + non-pure virtual cause the complete-object ctor to be materialized). Suppressing C1 requires the coordinated hierarchy reshape (non-virtual dtor, getCollisionNormal as virtual slot 0, projectCollisionOnSurface pure-virtual) across BoundingVolume/BoundingSphere/BoundingAAB headers; cannot be done independently within this component.
- [ ] `ndk23_handleTouchScreenEvent` (absent:jni_bridge:40): The original (file 0x94014, just a single instruction `b.w 0x19b848`) is NOT a real function body: it is an unconditional Thumb tail-branch into the MIDDLE (epilogue region, 0x19b848) of an unrelated huffman/JPEG bit-packing routine FUN_0019b5cc (0x19b5cc-0x19b8cf). This is a linker ICF-fold/tail-merge artifact, not source-expressible C++ -- there is no callable signature and no semantically meaningful body; emitting a thunk that branches into another TU's interior cannot be written in C++ without cross-TU label hacks. Needs a manual linker-alias/asm-stub mechanism outside normal source, so deferred.
- [ ] `ndk23_handleAcceleration` (absent:jni_bridge:40): Hand-scheduled VFP double-precision IIR low-pass accelerometer/gravity filter (file 0x94018) with orientation correction. It reads/writes two anonymous .bss double-array globals (0x227b60 and 0x227b9a, each a 3-double accel/grav vector written by ndk23_Init/ndk23_InitWithZip), an orientation flag global (0x227b28), and branches on a PaintCanvas flag byte at (*g_pEngine)->appManager->paintCanvas[0]. The body packs SetAccelValue/SetGravValue args through vstmia/vldmia + r2:r3 CONCAT pairs that Ghidra cannot recover (extraout_s0..s5 artifacts). Byte/linked-exact matching requires reproducing the exact vmla/vneg/vmov scheduling, the precise K0=0.95/K1=0.05 constant pool placement, and the exact layout/typing of the two unmodelled global double-vector structs; recovering this confidently from asm alone is not achievable in one pass without an iterative asm-diff convergence loop and a modelled accel/grav-state struct shared with ndk23_Init. Deferred to avoid committing a low-fidelity guess.
- [ ] `_ZN15IParticleSystem16getParticleCountEv` (extra:IParticleSystem:43): Original body is `ldr r0,[r0,#0x48]; bx lr` i.e. it returns the field at offset 0x48 (72). That field is `particleCount`/`maxParticles`. In OUR header the same field lands at offset 0x44 (68), so my added accessor (return this->maxParticles) compiles to `ldr r0,[r0,#0x44]` and does NOT byte-match. The root cause is a whole-class layout discrepancy: the original embeds `Array<ParticleSettings::ParticleSet>` INLINE at offset 0x38 (12 bytes: size@0x38, capacity@0x3c, data@0x40) and the embedded AERandom at 0x10, pushing particleSetIndex->0x44, fadeColorByLifetime->0x45, particleCount->0x48, flipRight->0x4c, particleIndex->0x50. Our IParticleSystem.h models `particleSets` as a heap pointer (with new/delete in the ctor and `delete particleSets` in the dtor) and arranges the 0x34..0x5c field block differently, so maxParticles is 4 bytes early. Correcting this is a contained-to-my-files refactor (size stays 112, derived ParticleSystemMesh/Sprite fields start at 0x70 and are unaffected) BUT it re-models the Array as an embedded member, which rewrites the ctor (C2), the inline dtor (D2), setParticleSet, emit, emitManual, interpolateColor and calcEmitterVelocity field accesses — i.e. it alters the compiled bytes of 6 out-of-scope functions in the same .cpp whose match status I cannot validate in this pass. Deferring the byte-exact accessor rather than emitting a wrong-offset (0x44) shortcut. Fix = redo the IParticleSystem field layout to the original offsets (embed Array<ParticleSet> at 0x38, AERandom at 0x10), then re-add getParticleCount returning maxParticles.
- [ ] `_ZdlPv` (absent:SpriteGun:54): This is the GLOBAL replaceable scalar operator delete(void*) (canonical libsupc++ body: tail-calls free(ptr) -- orig_asm is a single b.w to the `free` import veneer at 0x1aca38). It has nothing to do with SpriteGun: my component can only emit SpriteGun's own member symbols (verified: SpriteGun.cpp compiles EXIT 0 and produces _ZN9SpriteGun* only; the header's static SpriteGun::operator delete mangles to the class-scoped _ZN9SpriteGundlEPv, NOT the global _ZdlPv). The correct home is src/platform/recovered_1a3c94.cpp, which is OUTSIDE my allowed component files. That file's own header comment claims the address range 0x1a3c94..0x1a3d24 (which contains 0x1a3d10/0x1a3d18), already defines the sibling operators _Znwj/_Znaj/_ZdaPv/_ZdaPvj, and already CALLS `::operator delete(ptr)` (line 46) without defining it. Fix needed (cannot do it here): add to recovered_1a3c94.cpp:  `void operator delete(void *ptr) noexcept { std::free(ptr); }` (matches the tail-call-to-free; free(NULL) is a no-op so no null check, matching the unconditional b.w).
- [ ] `_ZdlPvj` (absent:SpriteGun:54): GLOBAL sized scalar operator delete(void*, unsigned int) (size_t is 32-bit on armeabi-v7a -> param mangles to `j`). orig_asm is a single b.w to the veneer at 0x1ab098 which resolves to _ZdlPv, i.e. the sized form ignores its size argument and forwards to operator delete(void*), exactly like the array sized form in recovered_1a3c94.cpp forwards to operator delete[]. Same misplacement issue as _ZdlPv: it is a global libsupc++ replaceable operator, not a SpriteGun member, and belongs in src/platform/recovered_1a3c94.cpp (outside my component). Fix needed there:  `void operator delete(void *ptr, std::size_t) noexcept { ::operator delete(ptr); }`. Defining a global ::operator delete inside SpriteGun.cpp would be a misplacement of a standard-library symbol into an unrelated game-class TU and is not the right structural home.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (extra:GameData:56): Too large/entangled to recover byte-exactly without coordinated cross-component changes I am not allowed to make. The original body (a) writes ~30 GameData fields at offsets 0x4,0xc,0xe,0x10,0x14,0x44,0x3c-0x4c,0x5c-0x68,0x50-0x5c,0x6d-0x78,0xa0-0xc4 etc. — but our GameData layout is a 200-byte stub modeling only 8 embedded Strings; the full field layout is unknown and several of those offsets overlap the String members. (b) It calls ~20 namespaced methods that are NOT declared on their classes in headers I cannot edit: AbyssEngine::ApplicationManager::{SetApplicationData,SetLoadingCallback,RegisterApplicationModule,CheatSetCallback,CheatAddCode,SetCurrentApplicationModule} (ApplicationManager is declared at GLOBAL scope in engine/core/ApplicationManager.h, so adding these as real namespaced methods is a header edit outside my component), plus AbyssEngine::Engine::GetDeviceInfo, AbyssEngine::PaintCanvas::{GetWidth,GetHeight,TextureCreateGlobal}, GameText::{setLanguage(short,int),getLanguage}, Globals::{loadFont,init}, MGame/ModMainMenu/ModStation/MTitle ctors, Layout::initTip, Status::resetGame, AERandom::reset, BuildResourceList. (c) It loads/stores ~30 module-local global statics via PC-relative GOT slots (DAT_000e07f0..0e0c68) whose exact addresses/types live in other TUs; without those exact externs the constant pool and load order cannot be reproduced. (d) String::TextureCreateGlobal is passed a String-by-value built from uninitialized stack slots (Ghidra shows SVar16 with garbage data/length) — recovering the exact ABI of that by-value String arg requires the real PaintCanvas::TextureCreateGlobal(String,uint) decl. Implementing this would require editing ApplicationManager.h/Engine.h/PaintCanvas headers and expanding the shared GameData layout that other TUs depend on — a HARD-STOP coordinated cross-hierarchy change.
- [ ] `_ZN14BoundingSphereD0Ev` (extra:BoundingSphere:57): Hierarchy-wide vtable-layout property rooted in the base class BoundingVolume (a file outside this component). The original binary's symbol table (android_2.0.16.symbols.tsv) shows the ENTIRE BoundingVolume hierarchy was compiled with NO deleting-destructor (D0) slot: BoundingVolume has only D1/D2 @0x11f718, BoundingAAB only D1/D2 @0x98198, BoundingSphere only D1/D2 @0x151d64 — no D0Ev anywhere in the hierarchy (though D0Ev exists for 81 other classes in the binary, so the toolchain does emit it generally). Our build emits a D0 for BoundingVolume itself and every derived class because the base declares `virtual ~BoundingVolume()`, which makes GCC put a deleting-dtor in every vtable. Suppressing D0 across the hierarchy requires a COORDINATED change to the base BoundingVolume.h/.cpp vtable structure PLUS each derived class (BoundingVolume, BoundingAAB, BoundingSphere) — a base-class virtual-destructor/vtable-layout change that the HARD STOP rule forbids attempting from a single derived component and forbids emitting as a caller_rewrite. Must be fixed in one cross-hierarchy pass that also touches BoundingVolume and BoundingAAB.
- [ ] `_ZN9ObjectGun11isRocketGunEv` (extra:ObjectGun:59): Cross-hierarchy type-system change (HARD STOP). In the original binary ObjectGun's vtable is [~dtor(D1,D0), setEnemies, setEnemy, update, render, translate, replaceGun, isRocketGun, isBombGun]; the isRocketGun slot (vtable+0x20, called virtually from render() as (**(this->vptr+0x20))(this)) points at the SHARED folded body at 0x1668c4 (return 0), which is referenced by 5 distinct class vtables (0x21e028/0x21e16c/0x21e278=ObjectGun/0x21e3c0/0x21e4d4) via identical-code-folding -- there is NO distinct _ZN9ObjectGun11isRocketGunEv symbol in the original; ours is the extra. Removing our standalone requires restructuring the predicate hierarchy so ObjectGun inherits isRocketGun/isBombGun from the AbstractGun base instead of redeclaring its own virtual: edit AbstractGun.h (already declares isRocketGun/isBombGun/isMineGun/isAutoTurret), make ObjectGun derive from AbstractGun, drop ObjectGun's own 'virtual int isRocketGun()' decl+def, and update RocketGun.h (override), MineGun.h, BombGun, SentryGun overrides and the render()/MineGun call sites accordingly so the folded base body is used. All of that touches headers outside this component (AbstractGun.h, RocketGun.h, MineGun.h, BombGun.*, SentryGun.*) and flips virtual/override across the hierarchy, which the rules forbid doing piecemeal.
- [ ] `_ZN13PlayerFighter11setPositionEfff` (extra:PlayerFighter:61): This is the override of a base virtual at vtable slot +0x48. The base virtual is declared as KIPlayer::setPosition3(float,float,float) in src/game/ship/KIPlayer.h (out-of-component). To make PlayerFighter's override mangle to ...11setPositionEfff while still overriding, the BASE virtual must be renamed setPosition3 -> setPosition in KIPlayer.h, and every derived class that overrides slot +0x48 must rename its override to match. That is a coordinated cross-hierarchy virtual rename (base KIPlayer + all derived overriders + their call sites) which is a HARD STOP for this single-component pass. Renaming only PlayerFighter's method to setPosition would silently break the override (it would no longer match the base virtual name and would create a new vtable slot). Body itself is already correct; only the name/override-chain blocks it.
- [ ] `_ZN13PlayerFighter11setPositionERKN11AbyssEngine6AEMath6VectorE` (extra:PlayerFighter:61): Non-virtual overload PlayerFighter::setPosition(Vector const&) that unpacks the vector and tail-calls vtable slot +0x48 (the float setPosition virtual). Its body is fine (currently setPosition_ref calling setPosition3). It cannot be renamed to setPosition independently because doing so requires the float overload to ALSO be named setPosition (overload set), which in turn requires the base-virtual rename in KIPlayer.h described for _ZN13PlayerFighter11setPositionEfff. Deferring together as the same coordinated cross-hierarchy change: rename KIPlayer::setPosition3 -> setPosition (and the non-virtual KIPlayer::setPosition_vec -> setPosition) plus all derived overriders, then rename both PlayerFighter methods to the setPosition overload set.
- [ ] `_ZN20FileInterfaceAndroid9OpenWriteERKN11AbyssEngine6StringEjj` (absent:FileInterfaceAndroid:64): extra: this symbol is ours, emitted by the override 'void *OpenWrite(const String&, uint32_t, uint32_t) override' in FileInterfaceAndroid.h:47. The original binary has NO such symbol; its only OpenWrite is the by-value _ZN20FileInterfaceAndroid9OpenWriteEN11AbyssEngine6StringEibj (OpenWrite(String,int,bool,unsigned int)), which is the actual vtable override. objdump -T of android_2.0.16_libgof2hdaa.so confirms FileInterfaceAndroid uses BY-VALUE String for every backend slot (OpenRead(String,int,bool,int,int,uint), OpenWrite(String,int,bool,uint), OpenAppend(String,int,bool,uint), FileExist(String), FileDelete(String), SetSaveDirectory(String)), whereas the base class FileInterface in engine/file/AEFile.h declares these virtuals with const String& (e.g. line 90: virtual void *OpenWrite(const String&, uint32_t, uint32_t)). The correct fix is to retype the base FileInterface::OpenWrite virtual (and likewise OpenRead/OpenAppend/FileExist/FileDelete/SetSaveDirectory) to the original by-value signatures in AEFile.h, then update the matching virtual call site AEFile.cpp:186 (fileInterface->OpenWrite(localPath, path.size(), 0)) and the AELowLevelHeldFile::OpenWrite mirror at AEFile.h:43. This is a coordinated cross-hierarchy base-virtual signature change spanning AEFile.h/AEFile.cpp (outside this component) and touching every derived override + virtual dispatch site, which is a HARD STOP for this single-component pass. NOTE: AEFile.h is being concurrently mutated by another wave (it flipped these virtuals to '= 0' pure mid-pass and back), so the retype must be done by a wave that owns AEFile.h.
- [ ] `_ZN13FileInterface8OpenReadERKN11AbyssEngine6StringEjjjjj` (extra:AEFile:83): Coordinated cross-hierarchy change required. This extra symbol is the weak out-of-line body of the inline virtual FileInterface::OpenRead, emitted by the FileInterfaceAndroid TU's vtable (verified: AEFile.cpp itself emits NO _ZN13FileInterface* symbols; AEFile::FileExist @0x810a8 calls the slot virtually via *(vptr+0x24)). The original binary has no such base symbol, so in the original FileInterface::OpenRead is PURE VIRTUAL (= 0). Making it pure in AEFile.h requires base FileInterface (declares the virtual) + derived FileInterfaceAndroid (src/engine/file/FileInterfaceAndroid.h/.cpp, NOT my component) to provide a matching override. Currently FileInterfaceAndroid::OpenRead has a non-matching signature (OpenRead(String name, int p2, bool p3, int p4, int p5, unsigned int p6) — by-value String, int/bool) so it does NOT override the base; with the base pure, FileInterfaceAndroid would stay abstract and its TU (which constructs it, e.g. initFileInterface) would fail to compile. Fix must, in one pass, make these base virtuals pure AND retype FileInterfaceAndroid's slots to real overrides of the base signatures.
- [ ] `_ZN13FileInterface10OpenAppendERKN11AbyssEngine6StringEjj` (extra:AEFile:83): Same coordinated change as OpenRead. Original has no base symbol => FileInterface::OpenAppend must be pure virtual (=0). But FileInterfaceAndroid::OpenAppend is declared STATIC (static void* OpenAppend(String, int, bool, unsigned int)) and does not override the base virtual; making the base pure leaves FileInterfaceAndroid abstract and breaks its TU. Requires editing FileInterfaceAndroid.h (out of component) to turn it into a real override.
- [ ] `_ZN13FileInterface13OpenDirectoryEPvj` (extra:AEFile:83): Same coordinated change. Original has no base symbol => FileInterface::OpenDirectory must be pure virtual. FileInterfaceAndroid does NOT declare OpenDirectory at all, so making the base pure leaves the derived abstract and breaks the FileInterfaceAndroid TU. Requires adding an override in FileInterfaceAndroid.h/.cpp (out of component).
- [ ] `_ZN13FileInterface13ReadDirectoryERN11AbyssEngine6StringE` (extra:AEFile:83): Same coordinated change. Original has no base symbol => FileInterface::ReadDirectory must be pure virtual. FileInterfaceAndroid does NOT declare ReadDirectory (only static FileGetNextEnum(String&) exists), so the base going pure leaves the derived abstract and breaks its TU. Requires an override in FileInterfaceAndroid.h/.cpp (out of component).
- [ ] `_ZN13FileInterface16SetSaveDirectoryERKN11AbyssEngine6StringE` (extra:AEFile:83): Same coordinated change. Original has no base symbol => FileInterface::SetSaveDirectory must be pure virtual. FileInterfaceAndroid::SetSaveDirectory is STATIC (static void SetSaveDirectory(String dir)) and does not override the base; base going pure breaks the derived TU. Requires retyping it to a real override in FileInterfaceAndroid.h (out of component).
- [ ] `_ZN13FileInterface18ResetSaveDirectoryEv` (extra:AEFile:83): Same coordinated change. Original has no base symbol => FileInterface::ResetSaveDirectory must be pure virtual. FileInterfaceAndroid::ResetSaveDirectory is STATIC and does not override; base going pure breaks the derived TU. Requires a real override in FileInterfaceAndroid.h (out of component).
- [ ] `_ZN13FileInterface9FileExistERKN11AbyssEngine6StringE` (extra:AEFile:83): Same coordinated change. Original has no base symbol => FileInterface::FileExist must be pure virtual (the runtime slot is FileInterfaceAndroid::FileExist @0x7e630; AEFile::FileExist @0x810a8 dispatches via *(vptr+0x24)). FileInterfaceAndroid::FileExist has a non-matching signature (FileExist(String name) by-value, not virtual/override) so it does not override the base; base going pure leaves the derived abstract and breaks its TU. Requires retyping FileInterfaceAndroid::FileExist to a real override in FileInterfaceAndroid.h (out of component).
- [ ] `_ZN13FileInterface10FileDeleteERKN11AbyssEngine6StringE` (extra:AEFile:83): Same coordinated change. Original has no base symbol => FileInterface::FileDelete must be pure virtual. FileInterfaceAndroid::FileDelete is STATIC (static void FileDelete(String name)) and does not override the base; base going pure breaks the derived TU. Requires retyping it to a real override in FileInterfaceAndroid.h (out of component).
- [ ] // AbyssEngine::ApplicationManager (src/engine/core/ApplicationManager.h) is modelled at GLOBAL scope but the binary mangles it inside namespace AbyssEngine (_ZN11AbyssEngine18ApplicationManager...). jni_bridge.cpp works around this locally via asm()-labelled extern declarations, but the canonical fix is to relocate class ApplicationManager into namespace AbyssEngine (with a global `using` alias) so its methods mangle correctly. Until then these methods are referenced by asm-label: SetExitCallback(void(*)()) [_ZN11AbyssEngine18ApplicationManager15SetExitCallbackEPFvvE], OnTouchBegin/Move/End(int,int,void*), OnTouchEnd() [void], OnUpdate(long long) [_ZN11AbyssEngine18ApplicationManager8OnUpdateEx], GetApplicationData() returning a pointer [_ZN11AbyssEngine18ApplicationManager18GetApplicationDataEv]. Add these as real AbyssEngine::ApplicationManager members.
- [ ] // Global symbols referenced by the JNI/NDK bridge that have no owning header yet (declared extern in jni_bridge.cpp): char* g_apkPath (.bss 0x2279a4, also read by ToJNI_initialize), char* g_zipPath (.bss 0x2279a8), JNIEnv* g_pEnv (.bss 0x22d314), jobject g_pActivity (.bss 0x226970), AbyssEngine::Engine** g_pEngine (.bss 0x227b24, the slot GetEngine() double-derefs), int g_android_back_button_pressed (.bss 0x22d2fc). jni_bridge.cpp currently DEFINES g_pEnv/g_pActivity/g_apkPath/g_zipPath and references g_pEngine/g_android_back_button_pressed/g_pVM(0x22d318)/g_pClass(0x22d310) as externs; if/when an owning TU (e.g. the ndk23_Init bridge) is recovered, ownership of these globals must be reconciled to avoid duplicate definitions.
- [ ] MISSING DECL: AbyssEngine::ApplicationManager::GetApplicationData() -> void* (mangled _ZN11AbyssEngine18ApplicationManager18GetApplicationDataEv); ApplicationManager is currently declared at GLOBAL scope in src/engine/core/ApplicationManager.h, so this namespaced method has no real declaration. GameData.cpp declares a local namespaced stub class to obtain the exact mangling for OnDestroyApplication; the canonical declaration belongs on the real (namespaced) ApplicationManager class.
- [ ] REFUSED caller_rewrite: src/game/ship/PlayerEgo.cpp: REFUSED — touches the type system ('struct') — needs coordinated change
- [ ] REFUSED caller_rewrite: src/game/ship/PlayerEgo.cpp: REFUSED — touches the type system ('struct') — needs coordinated change

## Clean-C++ cleanup backlog (NOT byte-match hacks, but non-idiomatic — convert opportunistically)
- ~42 reference-workarounds: `extern T x asm("_ZN...")` declarations that reach another class's
  member/method/global by hand-mangled name to avoid including a header that ODR-conflicts (the
  duplicate-`String` / two-PaintCanvas problem). Files: src/platform/jni_bridge.cpp,
  src/engine/render/LODManager.cpp (Globals::cItemListID_NN), src/game/core/Radio.cpp
  (GameText::getText), src/platform/recovered_a462c.cpp (Globals::status/options). Fix by resolving
  the header conflicts so the owning header can be included and `Class::member` used directly.
- `_ctor(self)` / `_base_ctor` emulation shims (ParticleSystemMesh/Sprite, FileRead, AERandom,
  BoundingSphere, Explosion, etc.) — pre-existing decompile shims pending methodize into real
  ctors/methods (the dispatcher tags these 'shim'; the corrected wave prompt converts them cleanly).
tools/lint_hacks.py gates against the egregious ctor/dtor asm-override hack recurring.
- [ ] `_ZN11AbyssEngine17ResourceTransformD1Ev` (extra:ResourceTransform:11): The original binary exports ONLY the base-object destructor _ZN11AbyssEngine17ResourceTransformD2Ev (the target .o has D2 only; our base .o emits the spurious D1). The presence of a standalone D1 in our build is the symptom of modelling ~ResourceTransform as a NON-virtual destructor. In the original, ResourceTransform is a polymorphic Resource subtype: there is a vtable at 0x220c78 whose first slot is the D2 body at 0x87ee4, and a virtual-dispatch dtor thunk at 0x6f9a0 that loads the vtable pointer and calls through it (adr r12; add #0xb1000; ldr pc,[r12,#0x2d0]). PaintCanvas::~PaintCanvas (0x87b2c) destroys these resources polymorphically by Resource* in a switch over a runtime type tag (case 2 -> ResourceTexture, case 4 -> ResourceMesh, case 5 -> ResourceTransform), so ResourceTexture/ResourceMesh/ResourceTransform are sibling subclasses of a common polymorphic base 'Resource' with a virtual destructor. Because the destructor is virtual, the compiler emits D0/D1/D2 but the original only retains D2 (referenced from the vtable); the standalone complete-object D1 was never odr-used and so was not emitted. The correct fix is to make ~ResourceTransform virtual under a real polymorphic base class Resource (vptr at +0x00, replacing the current header[0x40] layout model) and mark it 'override'. This is a coordinated cross-hierarchy type-system change: it requires (1) introducing/recovering the base class AbyssEngine::Resource with a virtual destructor and its vtable (header outside this component), (2) re-laying-out ResourceTransform with a vptr and deriving it from Resource, (3) doing the same for sibling subclasses ResourceTexture and ResourceMesh so the vtables/RTTI line up, and (4) updating PaintCanvas::~PaintCanvas (0x87b2c) which destroys them via Resource* virtual dispatch. Per the HARD STOP rule (adding a base-class virtual / flipping a destructor's virtualness / editing headers outside this component / changing types that derived classes override), this must be deferred for coordinated recovery of the Resource base + ResourceTexture/ResourceMesh/ResourceTransform subclasses + PaintCanvas call site, not patched piecemeal.
- [ ] `_ZN11AbyssEngine6CameraC1Efffff` (extra:Camera:12): Benign compiler code-folding artifact, not a source defect. The Camera(float,float,float,float,float) ctor is already a real, correct, idiomatic C++ constructor (src/engine/render/Camera.cpp:13) that default-constructs the projection Matrix then calls CameraSetPerspective -- this exactly matches the original at Ghidra 0x0008b724. The original Android binary's toolchain emitted ONLY the C2 base-object variant (_ZN11AbyssEngine6CameraC2Efffff @ 0007b724) and folded/dropped the C1 complete-object variant (android symbols table has 309 C2 but only 222 C1 symbols -- ~87 classes including Camera have C2 without a standalone C1; iOS 1.0.5 conversely has only C1). Our Clang correctly emits the matching C2 AND also emits a non-aliased C1 copy, so C1 shows as 'extra'. The C2 (the symbol the original actually has) is produced correctly. There is NO idiomatic-C++ source change that suppresses the C1 variant while keeping a real constructor: the only options are (a) asm link-name/alias/.set hacks -- explicitly FORBIDDEN, or (b) a build-flag change (e.g. linker --icf=all / identical-code-folding) which lives outside this component and is a build-system concern, not a Camera.cpp/.h edit. Per the task's own guidance ('It is FINE that a real ctor/dtor also emits the C1/C2 variants; do not try to suppress them'), no source action is warranted.
- [ ] `_ZN11AbyssEngine8KeyFrameC1Ev` (extra:KeyFrame:13): NON-ACTIONABLE C1/C2 codegen artifact, not a real defect. The original binary (symbols.tsv line 516, ghidra 0x85934) defines ONLY the C2 base-object ctor _ZN11AbyssEngine8KeyFrameC2Ev and has NO standalone C1 anywhere in the symbol table; the complete-object C1 was inlined at every call site by the original NDK clang. Our src/engine/core/KeyFrame.cpp already implements a real, idiomatic C++ constructor `KeyFrame::KeyFrame()` whose body matches the decompile exactly (memclr 0x48; scale & localScale = (1,1,1); timestampLo/Hi, field_0x58/0x5c = 0; alpha=1.0; +0x4c left alone). clang necessarily emits BOTH C1 and C2 from any real constructor, so the C1 'extra' cannot be removed without (a) an asm("_ZN..C2..") link-name override, (b) rewriting the ctor as a free function taking explicit `this`, or (c) deleting the real constructor — all three are explicitly FORBIDDEN, and the task rules state 'It is FINE that a real ctor/dtor also emits the C1/D1 alias variants; do not try to suppress them.' verify.py (lines 296-301) itself documents this C1/C2 pair as benign re-expression scaffolding that is 'Informational, not gated.' This is pervasive: 165 nullary C2 ctors exist in the original but only 92 C1s, so ~73 classes share this exact artifact. No clean fix exists; leaving the correct implementation as-is.
- [ ] `_ZN11BoundingAABD0Ev` (extra:BoundingAAB:14): Extra deleting-destructor (D0) that the original binary does not emit. The original exports exactly ONE BoundingAAB destructor symbol (~BoundingAAB at 0x000a8198, a 4-byte EXTERNAL branch thunk) and no separate _ZN11BoundingAABD0Ev; likewise BoundingVolume/BoundingSphere each expose only a single destructor symbol, indicating standalone deleting-dtor emission was suppressed across the whole hierarchy. Our build emits D0/D1/D2 because the base class BoundingVolume (engine/math/BoundingVolume.h, outside my component) declares `virtual ~BoundingVolume()`, which mandates a deleting-destructor slot in every derived vtable and makes GCC/Clang emit D0. The D0 cannot be removed from idiomatic C++ on a per-class basis without an asm/alias hack. Eliminating it requires a coordinated cross-hierarchy change to how the base's virtual destructor / operator delete is modeled in BoundingVolume.h, affecting all derived classes (BoundingVolume base + BoundingSphere + BoundingAAB) and their vtables. Per the HARD STOP rule, flipping/altering a base-class virtual destructor that derived classes depend on is forbidden here and cannot be expressed as a caller_rewrite. Defer to a hierarchy-wide pass that owns BoundingVolume.h.
- [ ] `_ZN16ParticleSettingsD1Ev` (extra:ParticleSettings:16): Benign ABI-alias artifact that cannot be removed without a FORBIDDEN shortcut. GROUND TRUTH: the original .so (orbnm -D) exports only `_ZN16ParticleSettingsC1Ev`/`C2Ev` (aliased, addr 0x183fe8) and `_ZN16ParticleSettingsD2Ev` (weak, addr 0x185474) for this class -- NO D1Ev and NO D0Ev. The destructor body at 0x195474 (image-rel 0x185474) loops the 48 embedded AbyssEngine::String members destructing each (this->field + 0x1d60 down by 0xa0), i.e. exactly our current idiomatic `ParticleSettings::~ParticleSettings(){}` (the 48 String members destruct automatically). The original's static instance is built in _INIT_2 (0x794fe) and registered with __cxa_atexit pointing at the D2 body, so the original toolchain only ever ODR-used the base-object destructor and emitted only D2; this is a common original-toolchain pattern (the .so has 206 D2Ev symbols vs only 135 D1Ev -- 71 classes have D2-without-D1). OUR toolchain (NDK r18b clang 7.0.2), given the same correct out-of-line `~ParticleSettings()` definition, emits the base-object destructor D2 as the body AND unconditionally emits the complete-object destructor D1 as an alias to it (verified: our .o yields D1Ev and D2Ev both at the same offset 0x38). There is NO idiomatic C++ source change that suppresses the D1 alias: making the destructor implicit instead drops the standalone D2 entirely (turning D2 into a MISSING symbol -- strictly worse, since the original genuinely has a standalone weak D2), and the only mechanisms that would keep D2 while dropping D1 are exactly the forbidden ones (asm("_ZN16ParticleSettingsD2Ev") label, __attribute__((alias)), or .set/.global asm). The prompt's own forbidden-list states the C1/D1 alias variants are FINE and must not be suppressed. The destructor is therefore already correct, idiomatic, and compiles EXIT 0 under its proper mangled name; the extra D1 is an unavoidable toolchain alias and is left as-is.
- [ ] `_ZN14PlayerJumpgate11setPositionEfff` (extra:PlayerJumpgate:18): This is the override of a base-class virtual that our codebase names 'setPosition3' but the original binary names 'setPosition' (vtable slot +0x48; confirmed by Ghidra: both PlayerJumpgate::setPosition and PlayerWormHole::setPosition decompile to this slot, storing (int)x/y/z into positionX/Y/Z at +0x124/+0x128/+0x12c then forwarding to geometry). To make PlayerJumpgate's override mangle to _ZN14PlayerJumpgate11setPositionEfff (and drop the extra setPosition3), the BASE virtual must be renamed in KIPlayer.h (line 151: 'virtual void setPosition3' -> 'virtual void setPosition') AND every derived override updated: KIPlayer.cpp, PlayerWormHole.h/.cpp, PlayerFixedObject.h/.cpp, PlayerFighter.h/.cpp, PlayerEgo.h/.cpp, PlayerGasCloud.h, SentryGun.cpp, plus the PlayerJumpgate override itself. Renaming only PlayerJumpgate's method to setPosition would break its 'override' (no matching base virtual) and detach it from slot +0x48. Cannot edit those out-of-component files; this is the cross-hierarchy HARD STOP. Coordinated change needed: base KIPlayer + derived {PlayerWormHole, PlayerFixedObject, PlayerFighter, PlayerEgo, PlayerGasCloud, SentryGun, PlayerJumpgate} + dispatch call sites in Level.cpp, CutScene.cpp, SpaceLounge.cpp, AEGeometry.cpp, TouchButton.cpp.
- [ ] `_ZN14PlayerJumpgate12setPosition3Efff` (extra:PlayerJumpgate:18): Same root cause as the paired absent _ZN14PlayerJumpgate11setPositionEfff: our PlayerJumpgate::setPosition3 IS the original PlayerJumpgate::setPosition (same vtable slot +0x48, same body). The base virtual KIPlayer::setPosition3 is misnamed across the whole hierarchy; the original calls it 'setPosition'. Fixing this single entry requires the coordinated base-virtual rename described in the paired entry (KIPlayer.h + all derived overrides + all polymorphic call sites), all in files outside this component. HARD STOP — cannot rename a base virtual + every derived override piecemeal.
- [ ] `_ZN14PlayerWormHole11setPositionEfff` (extra:PlayerWormHole:19): Same misnamed virtual as the 'extra' _ZN14PlayerWormHole12setPosition3Efff below. The original binary names the actor position-setter virtual 'setPosition' (mangled 11setPositionEfff), not 'setPosition3' (12setPosition3Efff). This virtual is DECLARED in base class KIPlayer.h as `virtual void setPosition3(float,float,float)` (actor vtable slot +0x48) and overridden by the whole hierarchy (KIPlayer, PlayerFighter, PlayerJumpgate, PlayerWormHole, PlayerFixedObject, PlayerEgo) -- verify.py shows the matching 11setPosition missing / 12setPosition3 extra pair for EVERY one of these classes. The body in PlayerWormHole.cpp is already correct (stores floats to posX/Y/Z @0x58, (int) casts to positionX/Y/Z @0x124, then forwards to geometry->setPosition). Renaming only PlayerWormHole's override would break the override/vtable relationship. The fix is a coordinated cross-hierarchy rename of the virtual setPosition3->setPosition in KIPlayer.h + the 5 subclass headers + all their .cpp definitions/call sites -- editing base/sibling headers I do not own, which is the explicit HARD STOP. Must be done as one atomic hierarchy-wide rename, not piecemeal.
- [ ] `_ZN14PlayerWormHole12setPosition3Efff` (extra:PlayerWormHole:19): This is our build's name for the SAME virtual as the absent _ZN14PlayerWormHole11setPositionEfff above -- it is misnamed, not genuinely extra. The original virtual (KIPlayer vtable slot +0x48) is named setPosition (11setPositionEfff); our port named the base virtual setPosition3 in KIPlayer.h and every subclass overrides it under that wrong name. Cannot rename PlayerWormHole's override alone without breaking the override against the base virtual declared in KIPlayer.h (not in my component). Requires the coordinated hierarchy-wide rename setPosition3->setPosition in KIPlayer.h plus PlayerFighter.h, PlayerJumpgate.h, PlayerFixedObject.h, PlayerEgo.h, PlayerWormHole.h and all corresponding .cpp definitions -- a base/derived virtual rename across files I do not own = HARD STOP, deferred.
- [ ] `_ZN11AbyssEngine12ResourceMeshC1EPKctb` (extra:ResourceMesh:21): Not fixable without a forbidden hack. ResourceMesh is non-polymorphic (no vtable: orig ctor at 0x122cf0 only sets name/wResourceId/flag, no vptr install). The real C++ constructor in ResourceMesh.cpp is already CORRECT and its base-object variant C2 (_ZN11AbyssEngine12ResourceMeshC2EPKctb) matches the original 100% (linked_equal in report.json). For a class with no virtual bases, clang necessarily emits the complete-object C1 as an alias to C2 (orbnm shows C1 and C2 both at offset 0x0 = identical code). The original binary kept C2 but folded/stripped the C1 alias. There is NO idiomatic-C++ way to emit C2 but suppress C1 -- the only mechanisms are asm() rename, __attribute__((alias)), visibility hacks, or making the class polymorphic, all of which are explicitly FORBIDDEN. The instructions themselves state: 'It is FINE that a real ctor/dtor also emits the C1/D1 alias variants; do not try to suppress them.' This is a systemic codegen artifact (23 such C1 extras across the codebase), not a source defect.
- [ ] `_ZN11AbyssEngine12ResourceMeshD1Ev` (extra:ResourceMesh:21): Same root cause as the C1 entry. ResourceMesh has a non-virtual destructor (orig dtor at 0x87ed0 just deletes name and nulls it, no vtable). The real C++ destructor in ResourceMesh.cpp is already CORRECT; its base-object variant D2 (_ZN11AbyssEngine12ResourceMeshD2Ev) matches the original 100% (linked_equal). clang emits the complete-object D1 as an alias to D2 (orbnm: D1 and D2 both at 0x30 = identical). The original lacks the standalone D1 alias. Suppressing D1 while keeping D2 requires a forbidden asm/attribute/visibility hack or an incorrect type-system change (making the dtor virtual, which the binary contradicts). Per instructions, emitting the D1 alias from a real dtor is expected and must NOT be suppressed.
- [ ] `_ZN11AbyssEngine15ResourceTextureC1EPKcf` (extra:ResourceTexture:25): This is the complete-object constructor (C1) alias that the C++ compiler unavoidably emits alongside the base-object (C2) variant for the real, idiomatic constructor ResourceTexture::ResourceTexture(const char*, float). The original binary's dynamic symbol table standalone-exports ONLY the C2 form (_ZN11AbyssEngine15ResourceTextureC2EPKcf @ 0x112c88), which our build already emits and which matches the original's behavior exactly (decompile @ 0x122c88: GetStringLength -> operator new[] -> memcpy -> store value). ResourceTexture has no virtual/virtual-base, so C1==C2 and the compiler folds C1 as an alias to C2. Removing the C1 symbol is impossible without a FORBIDDEN shortcut (asm symbol override / __attribute__((alias)) / free-function self-taking ctor). Task rules explicitly say it is FINE for a real ctor to also emit the C1 alias and not to suppress it. No clean fix exists.
- [ ] `_ZN11AbyssEngine15ResourceTextureC1ERKNS_6StringEf` (extra:ResourceTexture:25): Same situation: complete-object (C1) alias of the real constructor ResourceTexture::ResourceTexture(const String&, float). Original standalone-exports only C2 (_ZN11AbyssEngine15ResourceTextureC2ERKNS_6StringEf @ 0x11b6d2), which our build emits and which matches the original (decompile @ 0x12b6d2: String::GetAEChar -> operator new[] -> memcpy -> store value -> operator delete[]). C1==C2 (no vbase); the C1 symbol is a benign compiler-emitted alias that cannot be removed without a forbidden asm/alias hack. Per task rules the C1 alias is acceptable and must not be suppressed.
- [ ] `_ZN11AbyssEngine15ResourceTextureD1Ev` (extra:ResourceTexture:25): Complete-object (D1) alias of the real destructor ResourceTexture::~ResourceTexture(). Original standalone-exports only D2 (_ZN11AbyssEngine15ResourceTextureD2Ev @ 0x77ebc), which our build emits and which matches the original (decompile @ 0x87ebc: operator delete[](name); name = 0). Non-virtual dtor, no vbase, so D1==D2 and D1 is a benign compiler-emitted alias. It cannot be removed without a forbidden asm/alias hack; task rules explicitly allow the D1 alias and forbid suppressing it.
- [ ] `_ZN11AbyssEngine8MaterialC1Ev` (extra:Material:26): False-positive 'extra'. The original binary (verified via the delinker symbol table) has only the base-object variant _ZN11AbyssEngine8MaterialC2Ev and NO complete-object C1 alias (C1=False, C2=True). Our Material::Material() is already a real, idiomatic C++ constructor that compiles cleanly (EXIT 0) and faithfully matches the Ghidra decomp at 0x8c188 (blendMode=0, builds the five Array members, sets ambient color, fills 8 textures with -1 then textures[0]=0, addData/addDataSize=0). Modern clang emits the C1 complete-object variant as an alias at the same address as the matching C2; the original toolchain emitted only C2. The C1 string is therefore not in the binary symbol table, so verify.py's extra check (which, unlike its 'missing' check, deliberately does NOT collapse C1/C2 aliases and is documented as informational/not-gated) flags it. Suppressing the C1 alias would require an explicitly forbidden hack (asm link-name override, __attribute__((alias)), or rewriting the ctor as a free function with explicit 'this'). No clean fix exists; the code is already correct.
- [ ] `_ZN11AbyssEngine8MaterialC1EPS0_` (extra:Material:26): False-positive 'extra', same as the default ctor. Binary has _ZN11AbyssEngine8MaterialC2EPS0_ (True) but not the C1 alias (False). Material::Material(Material*) is already a real, idiomatic clone constructor matching the Ghidra decomp at 0x8a3d8 (null-other => empty material with textures=-1/blendMode=0; non-null => copy textures, blendMode, allocate addData of addDataSize bytes, copy ambientColor). Compiles EXIT 0 emitting the correct C2 variant. The C1 alias is an unavoidable modern-clang artifact; suppressing it requires a forbidden hack.
- [ ] `_ZN11AbyssEngine8MaterialD1Ev` (extra:Material:26): False-positive 'extra'. Binary has only base-object _ZN11AbyssEngine8MaterialD2Ev (True); no D1 and no D0 (both False) => non-virtual destructor used only as a base subobject. Our Material::~Material() is a real, idiomatic destructor matching the Ghidra decomp at 0x8c8b8 (deletes the five Array members in reverse construction order). Compiles EXIT 0 emitting the correct D2 variant. The D1 complete-object alias is an unavoidable modern-clang artifact at the same address; suppressing it requires a forbidden asm/attribute hack.
- [ ] `_ZN14BoundingVolumeD0Ev` (extra:BoundingVolume:27): D0 (deleting destructor) is part of the BoundingVolume vtable group (_ZTV/_ZTI/_ZTS + D0), which the Itanium ABI emits as one strong unit in the key function's TU. Our key function is the virtual ~BoundingVolume defined in BoundingVolume.cpp, so clang emits the whole vtable group (including D0) here. In the original binary the per-TU object (target/engine/math/BoundingVolume.o) exports NO vtable group at all and NO D0 -- both the C2 ctor (0x12f6dc) and the D1/D2 dtor (0x12f718) load the vtable address GOT-relative (ldr r4,[r4] after add r4,pc), proving the vtable is defined in a DIFFERENT TU and merely referenced here. No original TU in the verify set exports _ZTV14BoundingVolume/_ZTI14BoundingVolume, so the vtable's owning TU is outside the delinked set. Suppressing the local vtable+D0 emission requires relocating the key function (the first non-pure non-inline virtual) into another TU, a cross-TU vtable-ownership arrangement that cannot be reproduced by editing only BoundingVolume.{cpp,h} without an anonymous-namespace / out-of-line-forcing hack (forbidden). Needs coordinated decision on which TU owns the BoundingVolume vtable group across BoundingVolume/BoundingAAB/BoundingSphere.
- [ ] `_ZdlPv` (absent:SpriteGun:41): This is the GLOBAL ::operator delete(void*) (mangles _ZdlPv), not a SpriteGun member (that would be _ZN9SpriteGundlEPv, which the header already defines as a trapping deleting-dtor helper). In the original binary it is one of the four replaceable global new/delete operators that the game ships itself; the canonical home for them already exists at src/platform/recovered_1a3c94.cpp, which defines operator new / operator new[] / operator delete[](void*) / operator delete[](void*, size_t) and already CALLS ::operator delete(ptr). The correct fix is to add `void operator delete(void *ptr) noexcept { std::free(ptr); }` to src/platform/recovered_1a3c94.cpp (include <cstdlib>). That file is outside my component scope (SpriteGun.cpp/.h). Placing a global operator delete in SpriteGun.cpp would be a misplacement of an ABI-global symbol unrelated to SpriteGun, so deferring rather than introducing it in the wrong TU.
- [ ] `_ZdlPvj` (absent:SpriteGun:41): GLOBAL sized ::operator delete(void*, unsigned int) (mangles _ZdlPvj; size_t is 32-bit on armeabi-v7a so the param mangles to `j`). Original is a thunk that discards the size and tail-calls free, mirroring the existing operator delete[](void*, size_t) forwarder in src/platform/recovered_1a3c94.cpp. Correct fix: add `void operator delete(void *ptr, std::size_t) noexcept { ::operator delete(ptr); }` to src/platform/recovered_1a3c94.cpp alongside the scalar form and the other global allocation operators. That file is outside my component scope (SpriteGun.cpp/.h), and this symbol has no relationship to SpriteGun, so deferring rather than misplacing it in SpriteGun.cpp.
- [ ] `_ZN13PlayerFighter11setPositionEfff` (extra:PlayerFighter:44): HARD STOP: cross-hierarchy base-virtual rename. The original binary names this the virtual override PlayerFighter::setPosition(float,float,float) at vtable slot +0x48. Our build names the same virtual setPosition3 (extra _ZN13PlayerFighter12setPosition3Efff). It is declared `override` against the base virtual KIPlayer::setPosition3 (KIPlayer.h:151). Renaming PlayerFighter's method to setPosition requires renaming the BASE virtual KIPlayer::setPosition3 -> setPosition(float,float,float) and EVERY sibling override in lockstep: KIPlayer, PlayerEgo, PlayerWormHole, PlayerJumpgate, PlayerFixedObject (and PlayerGasCloud). All those are headers/cpps outside this component, and this is a base-class virtual rename (forbidden to do piecemeal and forbidden as caller_rewrites). Must be done as one coordinated pass that also touches the virtual decl in KIPlayer.h plus the many non-virtual-name call sites in Level.cpp, CutScene.cpp, SentryGun.cpp, SpaceLounge.cpp, PlayerWormHole.cpp.
- [ ] `_ZN13PlayerFighter11setPositionERKN11AbyssEngine6AEMath6VectorE` (extra:PlayerFighter:44): HARD STOP: same coordinated unit as the float overload. Original PlayerFighter::setPosition(Vector const&) (addr 0xecb9a, non-virtual trampoline that dispatches through vtable slot +0x48 to the float setPosition). Our build names it setPosition_ref (extra _ZN13PlayerFighter15setPosition_refERKN11AbyssEngine6AEMath6VectorE). It and the float setPosition form one overload set on PlayerFighter; the float member can only be named setPosition once the base KIPlayer virtual setPosition3 is renamed to setPosition across the whole hierarchy. Renaming only this Vector& member in isolation would leave a split/confusing overload set and still leave the float absent/extra unfixed. Defer together with the base-virtual rename.
- [ ] `_ZN13PlayerFighter12setPosition3Efff` (extra:PlayerFighter:44): Paired EXTRA of the absent _ZN13PlayerFighter11setPositionEfff. Not misnamed-in-isolation: it is the `override` of base virtual KIPlayer::setPosition3, so renaming it to setPosition demands the base-class virtual rename across KIPlayer + all derived overrides (cross-hierarchy, HARD STOP). Fix in the same coordinated pass.
- [ ] `_ZN13PlayerFighter15setPosition_refERKN11AbyssEngine6AEMath6VectorE` (extra:PlayerFighter:44): Paired EXTRA of the absent _ZN13PlayerFighter11setPositionERKN11AbyssEngine6AEMath6VectorE. The Vector& trampoline; should be renamed setPosition but only as part of the same overload-set / base-virtual rename coordinated pass.
- [ ] src/game/ui/HangarWindow.h: in `struct Layout` (the local minimal decl, after `void resetWindowDimensions();`) add: `static AbyssEngine::String formatCredits(int n);` -- HangarWindow.cpp now calls `Layout::formatCredits(...)` which links to `_ZN6Layout13formatCreditsEi` defined in Layout.cpp; the member is static (no `this`) and returns a String by value, matching the real method.
- [ ] `_ZN11AbyssEngine17ResourceTransformD1Ev` (extra:ResourceTransform:5): FALSE-POSITIVE 'extra' from the name-based symbol diff, not a real defect. The original symbol map (_work/symbols/android_2.0.16.symbols.tsv) catalogues exactly ONE destructor for this class: _ZN11AbyssEngine17ResourceTransformD2Ev at vaddr 0x77ee4 (= Ghidra 0x87ee4). That body (verified in the .so) does exactly: if dataA delete[] dataA; dataA=0; if dataB delete[] dataB; dataB=0; return — which is precisely what our real C++ ResourceTransform::~ResourceTransform() in src/engine/render/ResourceTransform.cpp implements. The class is non-virtual (no vtable store in the body; invoked via a tail-call thunk at 0x6f9a0 from ~PaintCanvas), so Clang necessarily emits D2 as the real function PLUS D1 as an identical alias at the same address. Our build correctly produces _ZN11AbyssEngine17ResourceTransformD2Ev (the original's exact symbol, EXIT 0) and the natural _ZN11AbyssEngine17ResourceTransformD1Ev alias. The original binary IS this function at the same address; symbols.tsv just only recorded the D2 name. Per the task's own rule ('It is FINE that a real ctor/dtor also emits the C1/C2/D1 alias variants; do not try to suppress them'), and verify.py's own comment that ctor/dtor aliases in 'extra' are 'Informational, not gated', the D1 alias must NOT be suppressed. The only way to remove it would be an asm/alias/attribute hack, which is explicitly forbidden. No correct, idiomatic change is possible; nothing to fix.
- [ ] `_ZN11AbyssEngine6CameraC1Efffff` (extra:Camera:6): This 'extra' C1 is the unavoidable complete-object-constructor ALIAS that Clang/GCC emit for the real C++ constructor AbyssEngine::Camera::Camera(float,float,float,float,float). The original binary (android_2.0.16_libgof2hdaa.so) defines the ctor body at 0x8b724 and exports it only under the C2 base-object symbol _ZN11AbyssEngine6CameraC2Efffff (the only Camera ctor mangled string present, at 0x2fd0f); there is no C1 in the original because the build folded/aliased it away (e.g. ICF or it only ever needed the base-object variant from CameraCreate at 0x8b6f0). Our src/engine/render/Camera.cpp already implements the correct idiomatic constructor (default-construct projection matrix via placement-new AEMath::Matrix(), then call CameraSetPerspective) and compiles cleanly (EXIT 0) emitting the correct C2 symbol. Suppressing only the C1 alias while keeping C2 is impossible from idiomatic C++ and would require a forbidden asm("...")/alias/attribute hack, which the task rules explicitly prohibit ('It is FINE that a real ctor/dtor also emits the C1/D1 alias variants; do not try to suppress them'). Therefore this extra C1 is a benign alias artifact, not a code defect; nothing to fix without a shortcut.
- [ ] `_ZN11AbyssEngine8KeyFrameC1Ev` (extra:KeyFrame:7): AbyssEngine::KeyFrame has no base classes and no virtual bases, so writing the real, idiomatic constructor KeyFrame::KeyFrame() (already present and verified correct against the Android decomp at 0x85934: zero timestampLo/Hi, memclr 0x48, scale/localScale=(1,1,1), field_0x58/5c=0, alpha=1.0) makes the compiler emit BOTH the C1 (complete-object) and C2 (base-object) variants as global/weak symbols. The Android target binary's symbol map (_work/symbols/android_thumb_map.tsv) contains ONLY _ZN11AbyssEngine8KeyFrameC2Ev @0x85934 and NO C1 symbol (C1 was inlined at every call site / linker-GC'd in the original toolchain output; note the iOS map DOES have both C1+C2). Our build correctly produces C2; the C1 is reported 'extra'. There is no source-level, idiomatic-C++ way to emit C2 but suppress the C1 alias for a class with no bases -- the duplicate is inherent to the Itanium ABI. Removing the standalone C1 would require an asm("...") link-name override or __attribute__((alias))/visibility hack, all of which are explicitly FORBIDDEN, and the task guidance states it is FINE for a real ctor to also emit the C1 alias and that one should NOT try to suppress it. The constructor body itself is already correct and idiomatic; no code change is warranted. Deferring the standalone-C1-elimination as it cannot be done without a shortcut.
- [ ] `_ZN11BoundingAABD0Ev` (extra:BoundingAAB:8): Linker GC artifact, not a source-level defect; correct idiomatic C++ already in place. The original binary (symbols.tsv) has BoundingAAB::~BoundingAAB emitted only as D1 and D2 (both at 0x98198, aliased) and NO D0 deleting destructor. Same pattern holds for the whole hierarchy: BoundingVolume (D1/D2 at 0x11f718, no D0) and BoundingSphere (D1/D2 at 0x151d28, no D0). BoundingVolume declares `virtual ~BoundingVolume()`, so the destructor IS virtual and the vtable has a dtor slot; our source `BoundingAAB::~BoundingAAB(){}` is a real, correct C++ destructor that the compiler naturally mangles to D1/D2 AND auto-emits D0 (verified: orbcc EXIT 0, orbnm shows D0/D1/D2). The original's missing D0 is a `--gc-sections` linker artifact: nothing ever deletes a BoundingAAB* polymorphically, so the deleting-destructor variant was unreferenced and stripped. Eliminating only BoundingAAB's D0 while keeping the destructor virtual is impossible in idiomatic C++ without either (a) making ~BoundingVolume/~BoundingAAB non-virtual -- a forbidden coordinated cross-hierarchy change (base BoundingVolume + derived BoundingAAB + BoundingSphere virtual/vtable edit) that would break the polymorphic dtor slot the binary's vtable depends on, or (b) an asm-alias / symbol-suppression byte-match hack, which is explicitly forbidden. Per the GOAL (we do NOT care about exact symbol/alias matching, NEVER write a byte-match hack) and the `extra` HARD STOP, this D0 should be left as the correct compiler-generated variant.
- [ ] `_ZN14BoundingSphereD0Ev` (extra:BoundingSphere:9): The extra D0 (Itanium deleting destructor) is auto-emitted by the compiler purely because BoundingSphere inherits the virtual ~BoundingVolume() and its vtable is emitted in this TU. The original binary has only ONE ~BoundingSphere (addr 0x161d64) and its verify report shows D1 (_ZN14BoundingSphereD1Ev) and D2 (_ZN14BoundingSphereD2Ev) BOTH linked_equal=true matching the original, while D0 has no counterpart in the original. This is NOT a BoundingSphere-local defect: the SAME pattern holds across the whole polymorphic hierarchy -- _ZN14BoundingVolumeD0Ev, _ZN11BoundingAABD0Ev, _ZN11SpaceLoungeD0Ev, _ZN17AELowLevelPakFileD0Ev and _ZN18IApplicationModuleD0Ev are ALL flagged extra. The original toolchain simply did not emit separate D0 deleting-destructor symbols for virtual-dtor classes (the deleting path lives elsewhere; orig BoundingVolume::~BoundingVolume at 0x12f718 is a base dtor that returns this and never calls operator delete). Eliminating D0 for BoundingSphere while preserving D1/D2 cannot be done within this component: it would require restructuring the base class BoundingVolume's virtual destructor (BoundingVolume.h, owned by another component) and would have to be applied in lockstep to BoundingAAB and every other class deriving from BoundingVolume, plus their vtable deleting-dtor slots. Per the HARD STOP rule this coordinated cross-hierarchy virtual-destructor change must not be attempted piecemeal, and an asm/alias suppression hack is forbidden. Coordinated change needed: base BoundingVolume (+ derived BoundingSphere, BoundingAAB, and the other listed classes) must be compiled so no standalone D0 deleting destructor is emitted, matching the original ABI.
- [ ] `_ZN16ParticleSettingsD1Ev` (extra:ParticleSettings:10): False-positive 'extra'. ParticleSettings::~ParticleSettings() is already a correct, idiomatic real C++ destructor in src/engine/render/ParticleSettings.cpp (loops over the 48 SetDefinition entries destroying each embedded AbyssEngine::String, matching the original at 0x185474). Because ParticleSettings is non-polymorphic with no virtual bases, the compiler ALWAYS emits D1 (complete-object) and D2 (base-object) at the same code (as aliases) -- our build emits both _ZN16ParticleSettingsD1Ev and _ZN16ParticleSettingsD2Ev at offset 0x38. The original binary's symbol table (_work/symbols/android_2.0.16.symbols.tsv) lists ONLY _ZN16ParticleSettingsD2Ev at 0x185474 and no D1; the original's compiler dropped the standalone D1 because the sole user is the global static object's __cxa_atexit registration, which references D2 directly (xrefs from _INIT_2 / DATA at 0x2205f0). Note the constructor is asymmetric the other way: the original keeps BOTH C1 and C2 (at 0x183fe8), and our build matches that. Eliminating only the D1 destructor symbol while keeping a normal '~ParticleSettings(){...}' is impossible in idiomatic C++14 -- it would require an asm("_ZN...D2Ev") alias override or attribute-based suppression of the D1 variant, both explicitly FORBIDDEN by the task (which also states it is FINE for a real dtor to emit the D1 alias and not to suppress it). No clean fix exists; deferring rather than introducing a byte-match/asm hack.
- [ ] `_ZN14PlayerJumpgate11setPositionEfff` (extra:PlayerJumpgate:11): absent. This is the actor vtable slot +0x48 virtual override, which the ORIGINAL binary names 'setPosition'. Our codebase models the same slot project-wide as 'setPosition3' to disambiguate it from the coexisting non-virtual setPosition(float,float,float)/setPosition(Vector) overloads. PlayerJumpgate::setPosition3 (the 'extra' entry) IS this same function under the wrong name. Renaming it to 'setPosition' so it mangles correctly while remaining a real C++ 'override' requires a coordinated cross-hierarchy virtual rename: the BASE virtual KIPlayer::setPosition3 (declared `virtual void setPosition3(float,float,float)` at src/game/ship/KIPlayer.h:151, defined KIPlayer.cpp:422) must become 'setPosition', and EVERY derived override must follow: PlayerWormHole (.h:33/.cpp:100,188), PlayerFixedObject (.h:98), PlayerFighter, PlayerEgo (.h:344/.cpp), PlayerJumpgate (.h:30/.cpp:69), plus all call sites (e.g. PlayerWormHole.cpp:188 this->setPosition3(...), Level.cpp, SentryGun.cpp, CutScene.cpp, SpaceLounge.cpp). Per the HARD STOP rule, base-class virtual + derived 'override' renames across files outside this component must not be attempted in isolation and must not be emitted as caller_rewrites (they would break every derived TU piecemeal). Defer for a single coordinated pass over the whole actor hierarchy.
- [ ] `_ZN14PlayerJumpgate12setPosition3Efff` (extra:PlayerJumpgate:11): extra. Same function as the absent _ZN14PlayerJumpgate11setPositionEfff above (this is the misnamed 'setPosition3' our build emits; the original names the slot 'setPosition'). MISNAMED, but the fix is a cross-hierarchy virtual rename of the base actor virtual KIPlayer::setPosition3 and all derived overrides + call sites in files outside this component (see paired deferral). HARD STOP: cannot rename a base virtual / derived 'override' piecemeal and cannot express it as caller_rewrites. Defer to a coordinated hierarchy-wide rename.
- [ ] `_ZN14PlayerWormHole11setPositionEfff` (extra:PlayerWormHole:12): ABSENT and the paired EXTRA _ZN14PlayerWormHole12setPosition3Efff are the SAME virtual method. Ghidra's demangled comments prove the original binary names this vtable-slot-+0x48 method 'setPosition' throughout the hierarchy: PlayerWormHole::setPosition(float,float,float) @0xb5336 and PlayerJumpgate::setPosition(float,float,float) @0xb51c0. Our port mislabeled it 'setPosition3'. The correct fix is a base-class virtual RENAME: KIPlayer declares `virtual void setPosition3(float,float,float)` (KIPlayer.h:151, OUTSIDE this component) and it is overridden by PlayerEgo, PlayerFighter, PlayerJumpgate, KIPlayer, PlayerWormHole, and shared by PlayerGasCloud. Renaming only PlayerWormHole's override to setPosition would break the override/vtable relationship. This requires a coordinated cross-hierarchy change: rename the base virtual KIPlayer::setPosition3 -> setPosition in KIPlayer.h and KIPlayer.cpp, and every override in PlayerEgo.{h,cpp}, PlayerFighter.{h,cpp}, PlayerJumpgate.{h,cpp}, PlayerGasCloud.h, PlayerWormHole.{h,cpp}, plus all virtual call sites (CutScene.cpp, Level.cpp, SentryGun.cpp, SpaceLounge.cpp, PlayerFighter.cpp:167, PlayerEgo.cpp:312). NOTE: TouchButton::setPosition3(int,int,int) is an UNRELATED different-signature method and must NOT be renamed. The HARD STOP rule forbids base-class virtual renames via caller_rewrites (would break every derived TU piecemeal), so deferring.
- [ ] `_ZN14PlayerWormHole12setPosition3Efff` (extra:PlayerWormHole:12): Same method as the paired absent _ZN14PlayerWormHole11setPositionEfff. Our setPosition3 IS the correct body (sets posX/Y/Z floats at 0x58/0x5c/0x60, positionX/Y/Z ints at 0x124/0x128/0x12c via (int)cast, then tail-calls geometry->setPosition) but under the wrong name because the whole KIPlayer virtual hierarchy was mislabeled setPosition3 instead of setPosition. Cannot rename without the coordinated cross-hierarchy base-virtual rename described in the paired entry, which touches KIPlayer.h (base) and 6+ other classes' files outside this component. Deferred per HARD STOP.
- [ ] `_ZN11AbyssEngine12ResourceMeshC1EPKctb` (extra:ResourceMesh:13): Not fixable without a forbidden hack. ResourceMesh is non-polymorphic: the original ctor at 0x122cf0 installs NO vptr (only sets name/wResourceId/flag), and PaintCanvas::~PaintCanvas (0x87b2c) destroys ResourceMesh via a DIRECT non-virtual call ResourceMesh::~ResourceMesh selected by a manual runtime type-tag switch (case 4 -> ResourceMesh), not virtual dispatch through a vtable -- decisive proof there is no virtual destructor. The real C++ constructor in ResourceMesh.cpp is already correct and its base-object variant C2 (_ZN11AbyssEngine12ResourceMeshC2EPKctb) is linked_equal to the original (report.json). For a class with no virtual bases, clang necessarily emits the complete-object C1 as an alias to C2 (orbnm: C1 and C2 both at offset 0x0 = identical code). The original kept C2 but folded/stripped the C1 alias. Emitting C2 while suppressing C1 requires only asm() rename / __attribute__((alias)) / visibility hacks / a false polymorphism change, all FORBIDDEN. The instructions state plainly: 'It is FINE that a real ctor/dtor also emits the C1/D1 alias variants; do not try to suppress them.' Systemic codegen artifact, not a source defect.
- [ ] `_ZN11AbyssEngine12ResourceMeshD1Ev` (extra:ResourceMesh:13): Same root cause. ResourceMesh has a NON-virtual destructor: orig dtor at 0x87ed0 just deletes name and nulls it (no vtable), and PaintCanvas::~PaintCanvas (0x87b2c) destroys it by a direct named call after a runtime type-tag switch (case 4), then operator_delete -- explicitly NOT virtual dispatch. The real C++ destructor in ResourceMesh.cpp is already correct; its base-object variant D2 (_ZN11AbyssEngine12ResourceMeshD2Ev) is linked_equal to the original. clang emits the complete-object D1 as an alias to D2 (orbnm: D1 and D2 both at offset 0x30 = identical). The original lacks the standalone D1 alias. Suppressing D1 while keeping D2 needs a forbidden asm/attribute/visibility hack or an incorrect (binary-contradicted) virtual-dtor change. Per instructions the D1 alias from a real dtor is expected and must NOT be suppressed.
- [ ] `_ZN11AbyssEngine15ResourceTextureC1EPKcf` (extra:ResourceTexture:16): Benign codegen artifact, not a defect. The original binary only defines the C2 base-object variant (_ZN11AbyssEngine15ResourceTextureC2EPKcf @ 0x112c88); it has NO C1 complete-object symbol. Our src/engine/render/ResourceTexture.cpp already declares a REAL C++ constructor ResourceTexture::ResourceTexture(const char*, float) with a body matching the original (String_GetStringLength -> new char[len+1] -> memcpy -> this->value=value). Compiling it (EXIT 0) correctly emits C2 under the exact original mangled name; it ALSO emits C1 as an alias at the same address (0x0), which is exactly what a correct, idiomatic C++ ctor produces on this toolchain. The original just happened to fold/emit only the C2 variant. Removing the extra C1 alias would require an asm("...C1...") link-name override or attribute alias suppression, both explicitly FORBIDDEN; the workflow rules state C1/D1 alias variants are fine and must not be suppressed. No clean fix exists.
- [ ] `_ZN11AbyssEngine15ResourceTextureC1ERKNS_6StringEf` (extra:ResourceTexture:16): Same as the char* ctor: original defines only C2 (_ZN11AbyssEngine15ResourceTextureC2ERKNS_6StringEf @ 0x11b6d2), no C1. Our real C++ ctor ResourceTexture::ResourceTexture(const String&, float) compiles cleanly (EXIT 0) and emits C2 under the exact original name plus a benign C1 alias at the same address. Body matches original logic (String_GetAEChar -> new char[len+1] -> memcpy -> value -> delete[] utf8). Suppressing the C1 alias would need a forbidden asm/alias hack; C1 aliases are explicitly blessed by the workflow. No clean fix.
- [ ] `_ZN11AbyssEngine15ResourceTextureD1Ev` (extra:ResourceTexture:16): Same pattern for the destructor: original defines only D2 (_ZN11AbyssEngine15ResourceTextureD2Ev @ 0x77ebc), no D1. ResourceTexture is non-polymorphic (no vtable in the original decompilation), so it has no D0 deleting dtor. Our real C++ destructor ResourceTexture::~ResourceTexture() compiles cleanly (EXIT 0) and emits D2 under the exact original name plus a benign D1 alias at the same address (0x6e). Body matches original (delete[] this->name; this->name=nullptr). Suppressing the D1 alias would require a forbidden asm/alias override; D1 aliases are explicitly allowed. No clean fix.
- [ ] `_ZN11AbyssEngine8MaterialC1Ev` (extra:Material:17): Unavoidable clean-C++ byproduct, not a fixable defect. The original binary (symbols.tsv) defines ONLY the base-object variants _ZN11AbyssEngine8MaterialC2Ev / C2EPS0_ / D2Ev; it has NO C1/D1 (complete-object) symbols (they were inlined/folded away at link time). Material.cpp already implements real, idiomatic C++ ctors/dtors (AbyssEngine::Material::Material(), ::Material(Material*), ::~Material()) whose behavior matches the Ghidra decompile exactly, and which correctly mangle to the original's C2/C2/D2. clang 7.0.2 (NDK r18b) necessarily emits the C1/D1 complete-object variants alongside C2/D2 for any non-trivial ctor/dtor of a class with no virtual bases — orbnm shows C1 and C2 at the same code offset (0x0 / 0x98) and D1==D2 at 0x162. The ONLY ways to make our object lack standalone C1/D1 are forbidden shortcuts: asm("_ZN...C2...") link-name overrides, __attribute__((alias)), or rewriting the ctor as a base-only free function with explicit `this`. The task prompt explicitly states: 'It is FINE that a real ctor/dtor also emits the C1/D1 alias variants; do not try to suppress them.' So the C2/D2 already match and the C1/D1 'extra' cannot and should not be removed.
- [ ] `_ZN11AbyssEngine8MaterialC1EPS0_` (extra:Material:17): Same root cause as C1Ev: original has only the base-object copy ctor C2EPS0_, never the complete-object C1EPS0_. Material::Material(Material*) is already correctly implemented and mangles to C2EPS0_ (matching the original); clang unavoidably also emits the identical C1EPS0_ complete-object variant. Suppressing it would require a forbidden asm/alias/free-function hack. The prompt permits these alias variants explicitly.
- [ ] `_ZN11AbyssEngine8MaterialD1Ev` (extra:Material:17): Same root cause: original has only base-object dtor D2Ev (no D1/D0; class is non-polymorphic, no vtable). Material::~Material() is already correctly implemented, matches the Ghidra decompile (deletes arr_5c, arr_50, meshes, arr_38, arr_2c), and mangles to D2Ev. clang unavoidably also emits the identical D1Ev complete-object variant; suppressing it needs a forbidden hack. Prompt permits the D1 alias variant explicitly.
- [ ] `_ZN14BoundingVolumeD0Ev` (extra:BoundingVolume:18): D0 is the compiler-emitted deleting-destructor variant of BoundingVolume's REAL virtual destructor. The original (likely GCC-built) omits D0 for this abstract base because no complete BoundingVolume object is ever deleted, but Clang places D0 in the vtable and emits it. Removing it at source level is impossible without (a) an asm/attribute/alias hack (explicitly forbidden), or (b) making ~BoundingVolume non-virtual -- which is a forbidden cross-hierarchy change: the binary clearly has a virtual dtor (the ctor/dtor install a vptr at offset 0) and the derived BoundingSphere (src/engine/math/BoundingSphere.h) and BoundingAAB (src/engine/math/BoundingAAB.h) override the virtual interface (collide/outerCollide/update/projectCollisionOnSurface) via this vtable. Flipping the dtor's virtualness would break both derived classes' vtables and all polymorphic call sites. D0 is the FINE-to-emit alias-variant category the task says not to suppress; it is a codegen artifact, not a source defect.
- [ ] `_ZN11AbyssEngine16ResourceMaterialC1EtNS_9BlendModeE` (extra:ResourceMaterial:28): Harmless C1 alias of a correctly-written real constructor. The original binary keeps only the C2 base-object variant (_ZN11AbyssEngine16ResourceMaterialC2EtNS_9BlendModeE at 0x122d20 in android_2.0.16_libgof2hdaa.so, a real standalone function with 4 xrefs whose body our C++ already matches: blendMode=blend, field_14/18/20/24=0, field_1c=0x3f800000, fill 8 texIndices with 0xffff, then texIndices[0]=texId). Our real C++ constructor ResourceMaterial::ResourceMaterial(uint16_t,BlendMode) compiles cleanly (EXIT 0) and emits C1 and C2 as identical-address aliases. The original simply never emitted the C1 complete-object variant (its callers used the C2 path / C1 was DCE'd by the original linker). Suppressing the C1 alias is impossible from idiomatic C++ and would require a forbidden asm link-name override / alias-suppression attribute. Per the task rules, emitting the C1/C2 alias pair from a real ctor is explicitly FINE; this 'extra' is an inherent benign artifact, not a code defect.
- [ ] `_ZN11AbyssEngine16ResourceMaterialC1EttNS_9BlendModeE` (extra:ResourceMaterial:28): Harmless C1 alias of a correctly-written real constructor. The original binary keeps only the C2 base-object variant (_ZN11AbyssEngine16ResourceMaterialC2EttNS_9BlendModeE at 0x122cbc, a real standalone function with 4 xrefs whose body our C++ already matches: blendMode=blend, field_14/18/20/24=0, field_1c=0x3f800000, fill 8 texIndices with 0xffff, texIndices[1]=texId2, texIndices[0]=texId). Our real C++ constructor ResourceMaterial::ResourceMaterial(uint16_t,uint16_t,BlendMode) compiles cleanly (EXIT 0) and emits C1 and C2 at the same address (aliases). The original never emitted the C1 complete-object variant. Suppressing C1 would require a forbidden asm/alias hack; emitting the C1/C2 pair from a real ctor is explicitly permitted by the task rules. Benign artifact, not a defect.
- [ ] `_ZN8GameDataC1Ev` (extra:GameData:30): ALIAS-COLLAPSE ARTIFACT, not a source defect. The original .so retains ONLY _ZN8GameDataC2Ev (base-object ctor) for GameData — verified in report.json: /functions lists _ZN8GameDataC2Ev (orig body @ android 0xe0c6c, which constructs the 8 embedded AbyssEngine::String members at offsets 0x18,0x24,0x30,0x7c,0x88,0x94,0xa8,0xb8 — exactly our layout). Our C2 matches. GameData is a leaf class (no virtual/virtual-base) with a normal non-deleted ctor, so Clang 7.0.2 (NDK r18b, the fixed match toolchain) UNCONDITIONALLY emits BOTH C1 and C2 as aliases of one body: orbobjdump -t on our GameData.o shows _ZN8GameDataC1Ev and _ZN8GameDataC2Ev BOTH at .text offset 0x0, size 0xa4 (identical address = alias pair). The original toolchain unified these to a single C2 name and dropped C1 from the symbol table (one name per address). No clean C++14, file-local, no-C-cast source construct suppresses Clang's C1 emission for a normal ctor; removing it needs an asm('.set')/__attribute__((alias)) hack (forbidden) OR a verify-framework change to address-collapse the 'extra' ctor-alias set the way it already collapses the 'missing' side (verify.py's extra set is name-based, not address-collapsed) — both outside GameData.cpp/.h. Same systemic pattern documented in docs/DEFERRED.md for KeyFrame/Material/IApplicationModule. Source is already correct (GameData::GameData(){} on the class).
- [ ] `_ZN8GameDataD1Ev` (extra:GameData:30): ALIAS-COLLAPSE ARTIFACT, not a source defect. The original .so retains ONLY _ZN8GameDataD2Ev (base-object dtor) for GameData — verified in report.json /functions (orig body @ android 0xe06f4, destroying the 8 String members in reverse). Our D2 matches. GameData is a leaf class with a normal non-deleted, non-virtual destructor, so Clang 7.0.2 emits BOTH D1 and D2 as aliases of one body: orbobjdump -t on our GameData.o shows _ZN8GameDataD1Ev and _ZN8GameDataD2Ev BOTH at .text offset 0xa4, size 0x44 (identical address = alias pair). The original kept only D2 (one name per address). Suppressing the extra D1 from idiomatic C++14 is impossible without an alias/.set hack (forbidden) or a verify-framework address-collapse of the extra dtor-alias set (outside this component). Identical 'extra D1, present D2' pattern affects ~13 classes per docs/DEFERRED.md (ParticleSettings, ApplicationManager, IApplicationModule, etc.). Source is already correct (GameData::~GameData(){} on the class).
- [ ] `_ZdlPv` (absent:SpriteGun:31): _ZdlPv (0x1a3d10) is the GLOBAL replaceable ::operator delete(void*), not a SpriteGun member. In the binary it is a tail-call thunk (b.w 0x1aca38 -> PLT free). Its correct, idiomatic home is src/platform/recovered_1a3c94.cpp, which already defines the sibling global allocation operators (operator new / operator new[] / operator delete[] / sized operator delete[]) and where operator delete[](void*) already calls ::operator delete(ptr) expecting this symbol to exist. That TU is OUTSIDE my component (I may only edit SpriteGun.cpp/SpriteGun.h). I removed the fabricated class-scope `static void operator delete(void*){__builtin_trap();}` hack from SpriteGun.h (it mangled to _ZN9SpriteGundlEPv, not an original symbol, and was unreferenced); SpriteGun now correctly emits `U _ZdlPv` from its D0 deleting destructor. The actual definition needs: in src/platform/recovered_1a3c94.cpp add `void operator delete(void *ptr) noexcept { std::free(ptr); }` (forwarding to free, matching the PLT thunk).
- [ ] `_ZdlPvj` (absent:SpriteGun:31): _ZdlPvj (0x1a3d18) is the GLOBAL sized ::operator delete(void*, unsigned int) (size_t is 32-bit/uint on armeabi-v7a, hence the `j` mangling). In the binary it is a tail-call thunk (b.w 0x1ab098 -> PLT free) that ignores the size and frees the pointer. Same situation as _ZdlPv: its correct home is src/platform/recovered_1a3c94.cpp alongside the other global new/delete operators, which is outside my editable component. Needed definition there: `void operator delete(void *ptr, std::size_t) noexcept { ::operator delete(ptr); }` (delegating to the scalar form, exactly as the array operators in that file delegate).
- [ ] `_ZN13PlayerFighter11setPositionEfff` (extra:PlayerFighter:35): Coordinated cross-hierarchy virtual rename required. The original binary names this overload PlayerFighter::setPosition(float,float,float) and it is a vtable override (binary slot 0x48). Our codebase deliberately names the whole hierarchy's virtual 'setPosition3' to dodge the overload: the base virtual is declared as KIPlayer::setPosition3(float,float,float) in src/game/ship/KIPlayer.h (NOT my component), and it is overridden by PlayerWormHole, PlayerGasCloud, PlayerEgo, PlayerJumpgate, PlayerFixedObject (all separate .h files). Renaming PlayerFighter::setPosition3 -> setPosition(float,float,float) alone would desync this override from the base virtual and break the vtable. Correct fix = rename the base virtual KIPlayer::setPosition3 -> setPosition in KIPlayer.h AND every derived override declaration (PlayerWormHole.h, PlayerGasCloud.h, PlayerEgo.h, PlayerJumpgate.h, PlayerFixedObject.h, PlayerFighter.h) plus internal call sites, in one atomic change. This touches headers outside my component and the type-system virtual table, so it is a HARD STOP.
- [ ] `_ZN13PlayerFighter11setPositionERKN11AbyssEngine6AEMath6VectorE` (extra:PlayerFighter:35): Same hierarchy-wide rename as the float overload. Original = PlayerFighter::setPosition(Vector const&); we name it setPosition_ref to keep it distinct from the virtual currently named setPosition3 (base: KIPlayer::setPosition_vec / setPosition3). Its body calls this->setPosition3(v.x,v.y,v.z). Renaming setPosition_ref -> setPosition(Vector const&) only makes sense once the paired float overload (and the base virtual KIPlayer::setPosition3 in KIPlayer.h, plus all derived overrides) is renamed to setPosition in the same atomic cross-file change. Editing those headers / the base virtual is a HARD STOP for this component.
- [ ] `_ZN17AELowLevelPakFile11GetFileSizeEv` (extra:AEFile:57): Cross-hierarchy type-system change in a file I don't own. The 7 extras all come from the duplicate class AELowLevelPakFile defined in my component (AEFile.h/.cpp). The original binary's pak backend is class AEPakFile (mangled _ZN9AEPakFile...), and the codebase ALREADY has a real AEPakFile in src/engine/file/AEPakFile.h/.cpp that emits exactly those correct symbols (verified in Ghidra: ctor 0x80010, ~D2 0x80034, ~D0 0x80058, Write 0x81308, Read 0x80068, Skip 0x800a2, Release 0x800d4, GetFileSize 0x800d0; binary vtable order [D2][D0][Write][Read][Skip][Release][GetFileSize]). To delete my duplicate and route AEFile::findPakFile through the real AEPakFile, AEPakFile must become the single pak backend stored in Array<AELowLevelFile*>. That requires editing AEPakFile.h (NOT in my component) to make it `class AEPakFile : public AELowLevelFile` and re-declare Write/Read/Skip/Release/GetFileSize as virtual ... override in the binary's vtable order (its current header wrongly leaves Skip/GetFileSize/Write non-virtual, so its vtable [D2][D0][Release][Read] is incompatible with AELowLevelFile and with the binary). Renaming my class to AEPakFile instead would duplicate-symbol-clash with AEPakFile.cpp. No in-component-only fix exists without breaking virtual dispatch. Needs coordinated edit: base=AELowLevelFile, derived=AEPakFile (AEPakFile.h), call site=AEFile::findPakFile (new AEPakFile(...) + drop AELowLevelPakFile).
- [ ] `_ZN17AELowLevelPakFile4ReadEjPv` (extra:AEFile:57): Same duplicate-class issue as _ZN17AELowLevelPakFile11GetFileSizeEv: fix requires making out-of-component AEPakFile.h derive from AELowLevelFile with the binary vtable order, then deleting AELowLevelPakFile and switching findPakFile to AEPakFile. Cross-hierarchy edit in a non-owned header.
- [ ] `_ZN17AELowLevelPakFile4SkipEj` (extra:AEFile:57): Same as above. AEPakFile.h (not my component) leaves Skip non-virtual; unifying with AELowLevelFile's vtable to drop the duplicate requires editing that header's virtual layout/base.
- [ ] `_ZN17AELowLevelPakFile5WriteEjPv` (extra:AEFile:57): Same as above. Original AEPakFile::Write exists (binary 0x81308, _ZN9AEPakFile5WriteEjPv). Removing my duplicate's Write requires the cross-hierarchy AEPakFile.h edit.
- [ ] `_ZN17AELowLevelPakFile7ReleaseEv` (extra:AEFile:57): Same as above. Original AEPakFile::Release exists (binary 0x800d4). Removing my duplicate requires the cross-hierarchy AEPakFile.h edit.
- [ ] `_ZN17AELowLevelPakFileD0Ev` (extra:AEFile:57): Deleting (D0) destructor of the duplicate AELowLevelPakFile. Real AEPakFile D0 exists (binary 0x80058). Removing this extra requires deleting the whole duplicate class, which needs the cross-hierarchy AEPakFile.h (not my component) edit to keep the open-files array dispatch correct.
- [ ] `_ZN17AELowLevelPakFileD2Ev` (extra:AEFile:57): Complete (D2) destructor of the duplicate AELowLevelPakFile. Real AEPakFile D2 exists (binary 0x80034). Same cross-hierarchy DEFER as D0.
- [ ] `_Z19ArrayReleaseClassesIP20ParticleSystemSpriteEvR5ArrayIT_E` (template:ParticleSystemManager:30): ArrayReleaseClasses<ParticleSystemSprite*> destroys its pointees, so it must be instantiated in a TU where the FULL ParticleSystemSprite type is visible. That type is only available via engine/render/ParticleSystemSprite.h, which CANNOT be co-included in ParticleSystemManager.cpp: both ParticleSystemSprite.h and ParticleSystemMesh.h define the same UNGUARDED 'enum AbyssEngine::BlendMode' (no GOF2_ENUM_BlendMode guard check), so including both produces a hard 'redefinition of BlendMode' error. ParticleSystemManager.cpp already needs ParticleSystemMesh.h (for ArrayReleaseClasses<ParticleSystemMesh*>), so the Sprite header is excluded. Deleting through a forward-declared ParticleSystemSprite only emits operator-delete (no virtual dtor call) -> wrong behavior, so a forward decl is not acceptable. Correct fix requires either (a) guarding the BlendMode enum in ParticleSystemSprite.h / ParticleSystemMesh.h with the shared GOF2_ENUM_BlendMode macro (edit to files outside this component), or (b) emitting this explicit instantiation from src/engine/render/ParticleSystemSprite.cpp, which already has the full Sprite type. releaseSprites() currently keeps the _psm_ArrayReleaseSprites forwarder so the rest of the file is unchanged/correct.
- [ ] `_Z14ArraySetLengthIPN11AbyssEngine6AEMath6VectorEEvjR5ArrayIT_E` (template:PlayerAsteroid:1): This template instantiation (ArraySetLength<AbyssEngine::AEMath::Vector*>, i.e. element type Vector* the pointer) is currently emulated by the unresolved `extern "C" Gun_VecPtrArray_setLength(int,void*)` shim in src/game/weapons/Gun.cpp (called at Gun.cpp:322 with `this->wobbleOffsets`, which is declared `char* wobbleOffsets` in Gun.h). The correct fix is a two-file change, and BOTH files are OUTSIDE my editable component (I may only edit src/game/ship/PlayerAsteroid.h and src/game/weapons/Gun.h): (1) src/game/weapons/Gun.cpp must drop the Gun_VecPtrArray_setLength shim (and the companion Gun_VecPtrArray_ctor / Gun_ArrayReleaseClasses / Gun_ArrayDtor shims) and call the real `ArraySetLength<AbyssEngine::AEMath::Vector*>(count, *wobbleOffsets)` after retyping wobbleOffsets to a real `Array<AbyssEngine::AEMath::Vector*>*`; (2) the explicit instantiation `template void ArraySetLength<ae::AEMath::Vector*>(unsigned int, Array<ae::AEMath::Vector*>&);` must be added to the shared Array TU src/engine/core/Array.cpp (its sibling ArrayReleaseClasses<ae::AEMath::Vector*> already exists there at line 96; the bare ArraySetLength<Vector*> instantiation is absent), or alternatively owned by Gun.cpp per the ObjectGun/ExplosionGun pattern. The free-function symbol _Z14ArraySetLength... cannot be emitted from a header (an explicit instantiation in Gun.h/PlayerAsteroid.h would produce conflicting weak symbols in every including TU and is forbidden). Neither editable header has any role in this symbol; PlayerAsteroid.cpp/.h never reference wobble/Vector* arrays. Build-gate confirms Gun.cpp compiles EXIT 0 today but emits no ArraySetLength symbol (uses the shim). Requires editing Gun.cpp + Array.cpp, which are not in this component.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (absent:GameData:3): OnCreateApplication is an enormous startup routine whose faithful implementation requires coordinated cross-component work that cannot be done from GameData.cpp/.h alone, and cannot be done idiomatically without forbidden shortcuts. It (1) reads/writes ~40 unresolved module-level globals (DAT_000e07f0..DAT_000e0c68: screen width/height, device-capability/low-end flags, the global GameText* and ApplicationManager* pointers, font/language/audio state) that are owned by other components and have no authoritative names or owning headers I can include; (2) bit-packs ~20 GameData fields at offsets 0x04,0x0c,0x0e,0x10,0x14,0x3c-0x4c,0x50-0x68,0x6d-0x78,0xa0-0xc4 that are NOT named in GameData.h (only the 8 String members are) -- recovering them all correctly needs renaming/adding many fields by cross-referencing every reader across the codebase; (3) calls many engine APIs that are not declared in any owning header I can find (BuildResourceList, ApplicationManager::SetApplicationData/SetLoadingCallback/RegisterApplicationModule/CheatSetCallback/CheatAddCode/SetCurrentApplicationModule, PaintCanvas::GetWidth/GetHeight/TextureCreateGlobal, GameText::setLanguage/getLanguage, Globals::init/loadFont, Layout::initTip, Status::resetGame, AERandom::reset) -- calling them with real types needs declarations added to other components' headers; (4) the decompiled tail's PaintCanvas::TextureCreateGlobal call has register-noise / uninitialized arguments (SVar16.length=extraout_r3, in_stack_ffffffb0), so the real call signature/args are not confidently recoverable. Any honest implementation would be a wall of reinterpret_cast to raw offsets plus hand-mangled externs (forbidden), or require editing ~5+ other headers to name globals/fields and declare APIs (out of component scope).
- [ ] `_ZN9ObjectGun11isRocketGunEv` (extra:ObjectGun:7): Removing this 'extra' symbol correctly requires a coordinated cross-hierarchy vtable redesign that touches files outside this component (AbstractGun.h, RocketGun.h, MineGun.h, SentryGun.h) -- a HARD STOP per the rules. Ground truth from the original binary (program android_2.0.16_libgof2hdaa.so): _ZTV9ObjectGun @ 0x21e250 is a single-inheritance vtable (_ZTI9ObjectGun is __si_class_type_info with base 11AbstractGun, name string '9ObjectGun'/'11AbstractGun' confirmed at _ZTS 0x21373c). Its 12 slots are, in order: ~ObjectGun(D1) 0x16f904, ~ObjectGun(D0) 0x16f94c, setEnemies 0x16f95c, setEnemy 0x16f962, update 0x16f964, render 0x16fda8, translate 0x170358, replaceGun 0x170374, then slot 8 = isRocketGun 0x1668c4, slot 9 0x1668c8, slot 10, slot 11. Slot 8 (vptr+0x20, matching the virtual call '(**(code**)((int)this->field_0x0 + 0x20))(this)' in ObjectGun::render) points to AbstractGun::isRocketGun() @ 0x1668c4 (decompiles to 'return 0'), NOT a distinct ObjectGun::isRocketGun -- xref from 0x21e278 (ObjectGun vtable slot 8) lands on AbstractGun::isRocketGun. The original AbstractGun.o exports _ZN11AbstractGun11isRocketGunEv but NO _ZTV11AbstractGun, i.e. AbstractGun supplies the isRocketGun/isBombGun/isMineGun/isAutoTurret quartet that fills the LAST four vtable slots of ObjectGun while ObjectGun's own dtor/setEnemies/setEnemy/update/render/translate/replaceGun occupy the FIRST eight slots. To reproduce this exact layout (no _ZN9ObjectGun11isRocketGunEv standalone, quartet inherited at slots 8-11) ObjectGun must NOT define isRocketGun and its seven leading methods (setEnemies/setEnemy/update/render/translate/replaceGun + virtual dtor) must be modeled as the polymorphic virtuals they are in the binary, with AbstractGun declaring the four predicate virtuals placed after them, and RocketGun/MineGun/SentryGun re-aligning their 'override' slots. That requires editing AbstractGun.h and every derived gun header (RocketGun.h/MineGun.h/SentryGun.h), all outside this component; a piecemeal edit to ObjectGun.h alone (adding ': public AbstractGun' + deleting isRocketGun) would put AbstractGun's quartet at slots 0-3 instead of 8-11, mis-slot RocketGun's override, and break every derived TU. Naive removal must be done together with the base/derived virtual reordering.
- [ ] `_ZN14PlayerJumpgate11setPositionEfff` (extra:PlayerJumpgate:8): This 'absent' and the paired 'extra' _ZN14PlayerJumpgate12setPosition3Efff are the SAME function — our PlayerJumpgate::setPosition3(float,float,float) IS the original's PlayerJumpgate::setPosition(float,float,float) (verified via Ghidra @0xb51c0: stores int(x/y/z) at +0x124/+0x128/+0x12c then tail-calls geometry->setPosition; identical to our body). The body is already correct; only the NAME is wrong. The method is a vtable override of actor slot +0x48, whose base virtual is declared in our codebase as KIPlayer::setPosition3 (src/game/ship/KIPlayer.h:151). In the ORIGINAL binary that whole hierarchy slot is named 'setPosition' (no 'setPosition3' symbol exists in the .so at all; PlayerWormHole's @0xb5336 is also 'setPosition'). To make the override mangle as 11setPositionEfff, the base virtual KIPlayer::setPosition3 must be renamed to 'setPosition' and EVERY derived override updated to match — KIPlayer (base) plus PlayerEgo, PlayerFighter, PlayerFixedObject, PlayerGasCloud, PlayerWormHole and call sites in AEGeometry.cpp, CutScene.cpp, SpaceLounge.cpp, TouchButton.cpp, SentryGun.cpp, Level.cpp. That is a coordinated cross-hierarchy virtual rename across ~20 files outside this component (KIPlayer.h + all derived .h/.cpp), which the HARD STOP rule forbids attempting or emitting as caller_rewrites. Fixing it only in PlayerJumpgate.h/.cpp would require an asm symbol-override hack (also forbidden). Must be done as a single coordinated rename pass over the KIPlayer virtual hierarchy.
- [ ] `_ZN14PlayerJumpgate12setPosition3Efff` (extra:PlayerJumpgate:8): Paired 'extra' for the entry above — same function. Our PlayerJumpgate::setPosition3 override mangles as 12setPosition3Efff because the base virtual is KIPlayer::setPosition3 in our codebase; the original names this slot 'setPosition'. Cannot rename to 'setPosition' without renaming the base KIPlayer virtual and all ~10 derived overrides (cross-hierarchy change spanning files outside this component) — forbidden by the HARD STOP rule. Resolve together with _ZN14PlayerJumpgate11setPositionEfff via a single coordinated rename of the KIPlayer::setPosition3 virtual hierarchy to 'setPosition'.
- [ ] `_ZN14PlayerWormHole11setPositionEfff` (extra:PlayerWormHole:9): HARD STOP: cross-hierarchy virtual rename. The original binary has NO `setPosition3` symbol anywhere (Ghidra search confirms only `setPosition` exists across the whole class tree); the port-wide name `setPosition3(float,float,float)` is the decompiled name for the actor virtual that the original calls `setPosition(float,float,float)` (vtable slot +0x48). The base virtual is declared in src/game/ship/KIPlayer.h:151 (`virtual void setPosition3(float,float,float)`) and is `override`-ridden in PlayerStatic/PlayerStaticFar plus PlayerEgo, PlayerFighter, PlayerJumpgate, PlayerFixedObject, PlayerGasCloud (see *.h/*.cpp under src/game/ship/) with dozens of call sites in CutScene.cpp, Level.cpp, SentryGun.cpp, SpaceLounge.cpp. Renaming only PlayerWormHole::setPosition3 -> setPosition in my two component files would break the `override` (no matching base virtual) and move it off vtable slot +0x48. Correct fix = rename the base KIPlayer virtual setPosition3 -> setPosition and every derived override + every call site in one coordinated pass across files outside this component. AEGeometry already has the float overload `setPosition(float,float,float)` so the name does not collide at the call site `this->geometry->setPosition(x,y,z)`. NOTE: the function BODY was corrected this pass (was a no-op geometry getPosition/setPosition round-trip; now stores posX/Y/Z + positionX/Y/Z then `this->geometry->setPosition(x,y,z)`, matching the DeepOpen oracle PlayerWormHole.setPosition and the original which tail-calls geometry's setPosition after storing the six position fields).
- [ ] `_ZN14PlayerWormHole12setPosition3Efff` (extra:PlayerWormHole:9): Same entity as the absent _ZN14PlayerWormHole11setPositionEfff above; deferred together. This is NOT a misnamed/inlined-only standalone we can rename or delete in isolation -- it is the PlayerWormHole override of the KIPlayer actor virtual that the port has globally named `setPosition3` but the original binary names `setPosition` (slot +0x48). Fixing requires the coordinated rename of KIPlayer's base virtual + all overrides + all call sites (KIPlayer.h, PlayerEgo, PlayerFighter, PlayerJumpgate, PlayerFixedObject, PlayerGasCloud, plus CutScene/Level/SentryGun/SpaceLounge call sites) -- all outside this component. Cannot be done piecemeal without breaking every derived TU's `override`.
- [ ] `_ZN7StarMap9touch_endEii` (extra:StarMap:12): StarMap::touch_end(int,int) is flagged 'extra' (no such symbol in the original), yet it is called from another TU: SpaceLounge::OnTouchEnd at src/game/menu/SpaceLounge.cpp:369 (`this->starMap->touch_end(x,y) != 0`). A non-LTO ARM build cannot inline a cross-TU method call, so the original must have inlined the wrapper's logic directly into SpaceLounge::OnTouchEnd rather than emitting a StarMap::touch_end symbol. The body is: `void* layout=*g_StarMap_end_layout; if(((Layout*)layout)->OnTouchEnd(x,y)) return 1; OnTouchEnd(x,y); return 0;`. Inlining it into SpaceLounge would be a multi-statement, cross-component change in a file I do not own (src/game/menu/SpaceLounge.cpp) AND it references g_StarMap_end_layout, a hidden-visibility (extern "C" __attribute__((visibility("hidden")))) global file-scoped to StarMap.cpp (declared at StarMap.cpp:79) that is not reachable from SpaceLounge.cpp. This exceeds the allowed self-contained-argument caller_rewrite and would require exposing/relocating a hidden global plus rewriting SpaceLounge's map-dismiss branch. Needs coordinated handling of SpaceLounge.cpp + the StarMap.cpp hidden global, or confirmation from ground truth of the real symbol name. Leaving touch_end in place (compiles clean, mangles to _ZN7StarMap9touch_endEii) rather than applying a shortcut.
- [ ] `_ZN13PlayerFighter11setPositionEfff` (extra:PlayerFighter:18): This is the float(x,y,z) overload, which is a VIRTUAL OVERRIDE of the actor vtable slot +0x48. The base virtual is KIPlayer::setPosition3(float,float,float) (KIPlayer.h:151, root class, no base). The original binary names this slot 'setPosition'. Renaming PlayerFighter's override from setPosition3 to setPosition would require coordinated renames across the whole hierarchy that I cannot touch: base KIPlayer::setPosition3 (KIPlayer.h/.cpp) plus the sibling overrides PlayerFixedObject::setPosition3, PlayerWormHole::setPosition3, PlayerJumpgate::setPosition3, PlayerEgo::setPosition3, plus ~25 call sites that dispatch through KIPlayer*/derived base pointers (CutScene.cpp, Level.cpp x10+, SentryGun.cpp, SpaceLounge.cpp, PlayerEgo.cpp, PlayerWormHole.cpp, PlayerFixedObject.cpp). HARD STOP: changing a base-class virtual name + override signatures across files I do not own is forbidden as a piecemeal edit.
- [ ] `_ZN13PlayerFighter12setPosition3Efff` (extra:PlayerFighter:18): Same function as the deferred absent _ZN13PlayerFighter11setPositionEfff above (paired extra/absent). Cannot remove the setPosition3 name without the cross-hierarchy virtual rename described above (base KIPlayer::setPosition3 + all sibling overrides + all KIPlayer*-based call sites). Deferred together.
- [ ] `_ZN17AELowLevelPakFile11GetFileSizeEv` (extra:AEFile:35): Cross-hierarchy change outside component. These 7 symbols come from the duplicate class AELowLevelPakFile in our component's AEFile.h/cpp (used only by AEFile::findPakFile). The original binary's class is AEPakFile (mangles _ZN9AEPakFile...), and it ALREADY exists and emits all 7 correct symbols from src/engine/file/AEPakFile.cpp / AEPakFile.h, which are OUTSIDE my editable file set. The clean fix is to delete the duplicate AELowLevelPakFile and have findPakFile build the real AEPakFile. BLOCKER: findPakFile must return the object as AELowLevelFile* (it is stored in Array<AELowLevelFile*> g_AEFile_openFiles and dispatched virtually via file->Write/Read/Skip/Release/GetFileSize). The real AEPakFile (in AEPakFile.h) does NOT derive from AELowLevelFile and its vtable only marks Release+Read virtual (2 virtual slots), so it is layout-incompatible with AELowLevelFile's 6-slot vtable [Write,Read,Skip,Release,GetFileSize]. Fixing this requires editing AEPakFile.h (not in my component) to make `class AEPakFile : public AELowLevelFile` with Write/Read/Skip/Release/GetFileSize/dtor all `override` virtual, matching the AELowLevelFile base in AEFile.h. That is a coordinated base/derived hierarchy + virtual-set change spanning AEFile.h (base) and AEPakFile.h/.cpp (derived) plus the findPakFile call site -- a HARD STOP cross-component type-system edit.
- [ ] `_ZN17AELowLevelPakFile4ReadEjPv` (extra:AEFile:35): Same as _ZN17AELowLevelPakFile11GetFileSizeEv: duplicate-class symbol; correct class AEPakFile already exists in AEPakFile.cpp (outside component). Removing the duplicate requires making AEPakFile derive from AELowLevelFile (edit AEPakFile.h, outside component) so findPakFile can return it polymorphically; AEPakFile's current vtable is layout-incompatible (only Release+Read virtual).
- [ ] `_ZN17AELowLevelPakFile4SkipEj` (extra:AEFile:35): Duplicate-class symbol of the real AEPakFile::Skip (exists in AEPakFile.cpp outside component). Eliminating the duplicate needs the AEPakFile->AELowLevelFile inheritance + override virtual-set change in AEPakFile.h (outside component).
- [ ] `_ZN17AELowLevelPakFile5WriteEjPv` (extra:AEFile:35): Duplicate-class symbol of the real AEPakFile::Write (exists in AEPakFile.cpp outside component). Eliminating the duplicate needs the AEPakFile->AELowLevelFile inheritance + override virtual-set change in AEPakFile.h (outside component).
- [ ] `_ZN17AELowLevelPakFile7ReleaseEv` (extra:AEFile:35): Duplicate-class symbol of the real AEPakFile::Release (exists in AEPakFile.cpp outside component). Eliminating the duplicate needs the AEPakFile->AELowLevelFile inheritance + override virtual-set change in AEPakFile.h (outside component).
- [ ] `_ZN17AELowLevelPakFileD0Ev` (extra:AEFile:35): Duplicate-class deleting-dtor of the real AEPakFile (D0 exists in AEPakFile.cpp outside component). Eliminating the duplicate needs the AEPakFile->AELowLevelFile inheritance change in AEPakFile.h (outside component); flipping AEPakFile's virtual destructor/vtable to match the AELowLevelFile base is a cross-hierarchy edit.
- [ ] `_ZN17AELowLevelPakFileD2Ev` (extra:AEFile:35): Duplicate-class complete-dtor of the real AEPakFile (D2 exists in AEPakFile.cpp outside component). Eliminating the duplicate needs the AEPakFile->AELowLevelFile inheritance change in AEPakFile.h (outside component).
- [ ] src/externs.h: add device-class config-flag externs used by loadPortraits (anonymous char* .bss flags in the original; no relocation symbol, defined in another TU). Following the existing g_Module_nameFlag convention, add inside the `extern "C" { ... }` block: `char *g_Portraits_ipadLargeFlag;` `char *g_Portraits_ipad1440Flag;` `char *g_Portraits_ipadFlagA;` `char *g_Portraits_ipadFlagB;` `char *g_Portraits_ipadFlagC;`. These select the texture-name infix (_ipad_large / _ipad_1440 / _ipad / empty). Names are descriptive (real binary symbols are anonymous); only needed so a full link resolves — the compile-gate already passes without them.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (absent:GameData:0): OnCreateApplication is a large app-bootstrap function (~330 bytes) whose body cannot be expressed as clean idiomatic C++ from only the GameData.cpp/.h component. It depends on a wide set of declarations that live OUTSIDE this component and do not currently exist in the codebase: (1) a free function BuildResourceList(AbyssEngine::Engine*) -- undeclared anywhere in src/, needs its own owning header; (2) an unknown indirect call through a global function pointer (loaded from DAT_000e07f4, called 3x with (&String, engine)) that probes device resolution -- the target's real symbol/signature is unrecovered; (3) roughly 20 distinct module-private global variables at PC-relative addresses (DAT_000e07f0/07f8/0bd4/0bd8/0bdc.../0c68) that are written with magic constants (resolution/aspect flags, canvas width/height caches, language/font pointers, GameText* global, current-canvas pointer, 0xbf000000 float, etc.) -- none have recoverable semantic names or owning headers, so writing them would require either fabricating ~20 extern globals in headers I am not allowed to edit, or raw reinterpret_cast pointer arithmetic into the data segment (a byte-match hack, explicitly forbidden); (4) APIs on AbyssEngine classes that the existing headers do not expose in the needed shapes: AbyssEngine::Engine::GetDeviceInfo(out), PaintCanvas::GetWidth/GetHeight/TextureCreateGlobal, GameText::setLanguage/getLanguage, Globals::loadFont(int)/init, plus constructors for GameText/MGame/ModMainMenu/ModStation/MTitle and ApplicationManager::Set*/Register/Cheat* -- several signatures/global handles are not declarable without editing those owning headers. Implementing it cleanly is a cross-file coordinated change (new BuildResourceList header + a dedicated app-globals header naming the ~20 globals + resolving the indirect device-probe call) that exceeds this single-component scope. The existing OnDestroyApplication hook is already in place; this create hook needs the companion infrastructure before it can be written without shortcuts.
- [ ] `_ZN9ObjectGun11isRocketGunEv` (extra:ObjectGun:2): HARD STOP: cross-hierarchy type-system change required, out of this component's scope. Ground truth from the original binary: ObjectGun's RTTI is a __si_class_type_info whose name string is '9ObjectGun' immediately followed by base '11AbstractGun' (typeinfo at V+0x21e288 -> name V+0x21373c, base-typeinfo V+0x21e294). So in the original ObjectGun PUBLICLY INHERITS AbstractGun and does NOT define its own isRocketGun -- it inherits the AbstractGun predicate quartet (isRocketGun/isBombGun/isMineGun/isAutoTurret, all 'return 0' at V+0x1668c4/0x1668c8/0x157d18-region). ObjectGun's vtable (functions at V+0x21e258) places its OWN 8 virtuals first (dtorD1, dtorD0, setEnemies, setEnemy, update, render, translate, replaceGun) and the inherited AbstractGun quartet at slots +0x20..+0x2c, where +0x20 = AbstractGun::isRocketGun (V+0x1668c4, return 0). RocketGun's vtable (V+0x21e218) is identical except +0x20 = RocketGun::isRocketGun (V+0x157d10, return 1) -- confirming RocketGun overrides the inherited slot at the SAME offset +0x20. Our build instead declares 'virtual int isRocketGun()' directly on ObjectGun (no AbstractGun base) and defines ObjectGun::isRocketGun() in ObjectGun.cpp, which emits the extra _ZN9ObjectGun11isRocketGunEv. The correct fix is: (a) make 'class ObjectGun : public AbstractGun', (b) delete the 'virtual int isRocketGun();' decl from ObjectGun.h AND the 'int ObjectGun::isRocketGun(){return 0;}' def from ObjectGun.cpp, (c) keep RocketGun::isRocketGun() override (now overriding the inherited AbstractGun virtual). COMPLICATION blocking a naive base-class edit: standard 'class ObjectGun : public AbstractGun' single-inheritance lays the AbstractGun virtuals FIRST in the vtable, but the original places the predicate quartet LAST (at +0x20, after ObjectGun's own 8 virtuals). Reproducing the original layout requires coordinated restructuring of AbstractGun.h/ObjectGun.h plus every derived class that overrides these slots (RocketGun, MineGun, SentryGun, BombGun) and their .cpp vtables -- edits to headers outside this component (AbstractGun.h, RocketGun.h, MineGun.h, SentryGun.h) and to a base-class virtual that derived TUs 'override'. That is explicitly excluded from this component and cannot be safely expressed as self-contained caller_rewrites.
- [ ] `_ZN14PlayerJumpgate11setPositionEfff` (extra:PlayerJumpgate:4): absent + the paired extra _ZN14PlayerJumpgate12setPosition3Efff are the SAME function. The binary names this virtual 'setPosition(float,float,float)' (slot +0x48), but the project renamed the whole vtable family to 'setPosition3' to disambiguate from setPosition(Vector const&) at slot +0x44. The name is owned by the BASE virtual KIPlayer::setPosition3 (src/game/ship/KIPlayer.h:151, NOT in my component). Renaming PlayerJumpgate's override alone to 'setPosition' would (a) drop 'override' / fail to match the base signature and stop participating in vtable dispatch, breaking the polymorphic calls the codebase relies on. The correct fix is a coordinated rename setPosition3 -> setPosition across: base KIPlayer (KIPlayer.h/.cpp), every override (PlayerWormHole, PlayerFixedObject, PlayerFighter, PlayerEgo, PlayerGasCloud, PlayerJumpgate), and all call sites (CutScene.cpp ~3, Level.cpp ~10, SentryGun.cpp, SpaceLounge.cpp ~5, PlayerFighter.cpp, PlayerFixedObject.cpp, PlayerWormHole.cpp, PlayerEgo.cpp). This is a HARD-STOP cross-hierarchy virtual rename, not a self-contained call-site change.
- [ ] `_ZN14PlayerJumpgate12setPosition3Efff` (extra:PlayerJumpgate:4): extra; this IS our emission of the same virtual that the binary names 'setPosition'. Same coordinated cross-hierarchy rename as the paired absent _ZN14PlayerJumpgate11setPositionEfff (base virtual KIPlayer::setPosition3 + all overrides + all call sites). Cannot rename PlayerJumpgate's slot alone without breaking vtable dispatch / override matching. Defer until the whole setPosition3->setPosition family is renamed together.
- [ ] `_ZN14PlayerWormHole11setPositionEfff` (extra:PlayerWormHole:5): absent + the paired extra _ZN14PlayerWormHole12setPosition3Efff are the SAME vtable slot (actor +0x48). Ghidra confirms the original names this virtual setPosition(float,float,float) across the ENTIRE hierarchy (no setPosition3 symbol exists anywhere in android_2.0.16_libgof2hdaa.so). Our port renamed the base virtual to setPosition3 to disambiguate it from the original's other setPosition overloads (the port also has setPosition_vec). PlayerWormHole::setPosition3 is declared `override` of KIPlayer::setPosition3 (KIPlayer.h:151), which itself overrides an Actor-ancestor virtual. Renaming only PlayerWormHole's method breaks the override relationship. Correct fix = coordinated rename of the base virtual KIPlayer::setPosition3 -> setPosition (KIPlayer.h) PLUS every derived override (PlayerEgo, PlayerFighter, PlayerJumpgate, PlayerFixedObject, PlayerGasCloud, SentryGun, TouchButton, PlayerWormHole) and all 54 call sites across src/. This touches headers and virtual/override declarations outside this component -> HARD STOP per instructions; cannot be a self-contained caller_rewrite.
- [ ] `_ZN14PlayerWormHole12setPosition3Efff` (extra:PlayerWormHole:5): extra = the same vtable slot as the paired absent _ZN14PlayerWormHole11setPositionEfff. It is MISNAMED: original is setPosition(float,float,float). Fix is the same cross-hierarchy base-virtual rename described above (KIPlayer::setPosition3 -> setPosition and all overrides/call sites), which is a forbidden header/virtual/override change across files outside this component. Deferred.
- [ ] `_Z11keyReleasedPN11AbyssEngine6EngineEi` (absent:recovered_7a41c:6): keyReleased(Engine*,int) walks the 48-entry virtual-key table releasing the matching key, but its core path calls AbyssEngine::ApplicationManager::GetCurrentApplicationModule() (mangled _ZNK11AbyssEngine18ApplicationManager27GetCurrentApplicationModuleEv) plus ApplicationManager::OnTouchEnd and Engine::DrawQuad. GetCurrentApplicationModule is NOT declared in src/engine/core/ApplicationManager.h; implementing this cleanly requires ADDING that const method declaration to ApplicationManager.h (a cross-component header edit, forbidden by FILE DISCIPLINE / HARD STOP). The function is forward-declared in recovered_7a41c.h and already called by the two recovered helpers; it only needs the header method to land. Coordinated change needed: add `int GetCurrentApplicationModule() const;` to AbyssEngine::ApplicationManager in ApplicationManager.h, then implement keyReleased in recovered_7a41c.cpp.
- [ ] `_Z10MouseWheelff` (absent:recovered_7a41c:6): MouseWheel(float,float) gates its zoom-request writes on AbyssEngine::ApplicationManager::GetCurrentApplicationModule()==2, which is not declared in ApplicationManager.h (same cross-component header edit as keyReleased, forbidden). Also pokes several unnamed module zoom-request globals. Defer until GetCurrentApplicationModule is added to ApplicationManager.h.
- [ ] `_Z13simulateTouchPN11AbyssEngine6EngineE` (absent:recovered_7a41c:6): simulateTouch(Engine*) is ~300 lines of pointer/wheel-to-synthetic-touch logic that (a) calls AbyssEngine::ApplicationManager::GetCurrentApplicationModule() (undeclared in ApplicationManager.h -> forbidden header edit) and OnTouchBegin/Move/End, and (b) reads ~25 unnamed Engine-internal fields by raw offset (0x2d4,0x32c,0x3dc,0x434,0x48c,0x4e4,0x53c,0x594,0x5ec,0x644...) that are NOT named in engine/render/Engine.h plus a dozen module FP globals. Recovering this as clean, verifiable C++ would require naming those Engine fields (cross-component header edit) and is not confidently reconstructable. Defer until ApplicationManager::GetCurrentApplicationModule is declared and the relevant Engine input-state fields are named.
- [ ] `_Z23ActualizeMouseVisibiltyi` (absent:recovered_7a41c:6): ActualizeMouseVisibilty(int) drives pointer show/hide via AbyssEngine::ApplicationManager::GetCurrentApplicationModule() (undeclared in ApplicationManager.h -> forbidden header edit) and a free IsInGameSubMenuNotActive(int), reading many unnamed module-state globals and a sub-menu key-state struct by raw offset. Implementing cleanly needs the GetCurrentApplicationModule declaration added to ApplicationManager.h. Defer until that header method lands.
- [ ] `_ZN11AbyssEngine13SetFXMaterialENS_9BlendModeE` (extra:ConfigReader:7): absent: this is an empty render stub (body is just `bx lr`). Its parameter type AbyssEngine::BlendMode and the SetFXMaterial concept live in render headers (PaintCanvas.h / ResourceMaterial.h / ParticleSystem*.h), NOT in ConfigReader.h. Defining a render function in the config-reader header would be wrong placement; needs to be added to the appropriate render .h/.cpp which is outside this component.
- [ ] `_ZN11AbyssEngine25GenerateCompressedTextureEPNS_5ImageE` (extra:ConfigReader:7): absent: trivial render stub that returns 1. Takes AbyssEngine::Image* whose owning header (ImageFactory.h / render headers) is not ConfigReader.h. Belongs in a render TU, outside this component.
- [ ] `_ZN11AbyssEngine12_GLOBAL__N_115readScalarTrackEjPNS_9TransformEPfjjjb` (extra:ConfigReader:7): extra: defined in src/engine/render/Mesh.cpp, which is not in my editable component (only ConfigReader.h).
- [ ] `_ZN11AbyssEngine12_GLOBAL__N_115readVectorTrackEjPNS_9TransformEPfj` (extra:ConfigReader:7): extra: defined in src/engine/render/Mesh.cpp, which is not in my editable component (only ConfigReader.h).
- [ ] `_ZN11AbyssEngine12_GLOBAL__N_132config_reader_read_file_callbackEPvS1_` (extra:ConfigReader:7): extra: defined as an empty anonymous-namespace function in src/engine/file/ConfigReader.cpp (not editable in this component). Its address is taken and stored as a ConfigTokenReadFunction pointer in the ConfigReader ctor, so it cannot be inlined away while still being registered. The original has no such symbol, implying the original ctor registers callbacks differently (or not at all). Resolving this requires reworking ConfigReader.cpp's constructor, which is outside my component.
- [ ] `_ZN11AbyssEngine12_GLOBAL__N_137config_reader_register_token_callbackEPvS1_` (extra:ConfigReader:7): extra: same situation as config_reader_read_file_callback — empty callback in ConfigReader.cpp (not editable here) whose address is registered as a function pointer, so it cannot be eliminated from the header. Fix belongs in ConfigReader.cpp.
- [ ] `_ZN11AbyssEngine12_GLOBAL__N_138config_reader_keys_for_action_callbackEPvS1_` (extra:ConfigReader:7): extra: same situation as the other config_reader callbacks — empty callback in ConfigReader.cpp (not editable here) registered via function pointer. Fix belongs in ConfigReader.cpp, outside my component.
- [ ] `_ZN8KIPlayer11setPositionERKN11AbyssEngine6AEMath6VectorE / _ZN8KIPlayer15setPosition_vecERKN11AbyssEngine6AEMath6VectorE` (extra:KIPlayer:8): Our setPosition_vec(Vector const&) IS the original setPosition(Vector const&) (decompile @0xa6360 matches byte-for-byte: AEGeometry::setPosition + Matrix assign/multiply). But this is a polymorphic virtual at actor vtable slot +0x44, OVERRIDDEN by PlayerFighter::setPosition(Vector const&), PlayerFixedObject::setPosition(Vector const&), PlayerEgo::setPosition(Vector) (all confirmed in the .so dynsym). Renaming setPosition_vec -> setPosition requires a coordinated cross-hierarchy change: rename the base virtual in KIPlayer plus all derived overrides in PlayerFighter.h/.cpp, PlayerFixedObject.h/.cpp, PlayerEgo.h/.cpp (and the existing forwarders there), and rewrite ~5 call sites in CutScene.cpp/SpaceLounge.cpp/SentryGun.cpp/PlayerFighter.cpp/PlayerFixedObject.cpp. HARD STOP: base-virtual rename + override edits across files I do not own.
- [ ] `_ZN8KIPlayer11setPositionEfff / _ZN8KIPlayer12setPosition3Efff` (extra:KIPlayer:8): Our setPosition3(float,float,float) IS the original setPosition(float,float,float) (decompile @0xa6394: packs 3 floats into a stack Vector, dispatches through vtable slot +0x44 to setPosition(Vector const&)). This is a virtual at actor vtable slot +0x48, OVERRIDDEN by PlayerFighter, PlayerJumpgate, PlayerWormHole, PlayerFixedObject, PlayerEgo (all have setPosition(fff) in the .so dynsym). Renaming setPosition3 -> setPosition is the same coordinated cross-hierarchy rename as the Vector form: base virtual + 5 derived overrides + ~20 call sites in Level.cpp, CutScene.cpp, SentryGun.cpp, SpaceLounge.cpp (files I do not own). Must be done as one atomic hierarchy-wide rename, not piecemeal. HARD STOP.
- [ ] `_ZN8KIPlayer8getSpeedEv` (extra:KIPlayer:8): getSpeed() is a virtual (weak symbol @0xa5240, referenced from ~14 vtable DATA slots) returning 0, OVERRIDDEN by PlayerEgo::getSpeed() @0xa2227a (in .so dynsym). Adding it to the KIPlayer base requires inserting it at its exact actor-vtable slot and marking PlayerEgo::getSpeed as override in PlayerEgo.h/.cpp. That is a cross-hierarchy virtual-table change touching files I do not own. HARD STOP — defer for coordinated edit (base KIPlayer + derived PlayerEgo).
- [ ] `_ZN8KIPlayer12setAutoPilotEPS_ (extra)` (extra:KIPlayer:8): There is NO KIPlayer::setAutoPilot in the original binary; the real symbol is _ZN9PlayerEgo12setAutoPilotEP8KIPlayer @0x9b808 (PlayerEgo::setAutoPilot(KIPlayer*)). Our body is currently mis-homed onto KIPlayer (it addresses PlayerEgo-tail fields at +0x158/+0x15c/+0x160 by raw byte offset). Correct fix is to MOVE this method to PlayerEgo (declare in PlayerEgo.h, define in PlayerEgo.cpp) and update the ~5 callers in PlayerEgo.cpp/PlayerWormHole.cpp/MGame.cpp to call it on a PlayerEgo*. That is a cross-component move into files I do not own. HARD STOP — left as-is in KIPlayer for now (still compiles) and deferred.
- [ ] `_Z13loadingScreenPN11AbyssEngine11PaintCanvasEiPv` (shim:RecordHandler:13): Large (~150-instruction) loading-screen renderer that depends on roughly 7 unmodeled global holder objects (the GOT-relative DAT_000e06a4/a8/ac/b0/b8/bc/c0 singletons) plus an opaque float scale constant (DAT_000e06a0), and reads several +0xNN fields off those opaque globals (e.g. *piVar9+0x27c/0x280, +4) that are not described by any available header. It also drives language-specific (lang==9) DrawRegion2D scaling math and Layout::drawBG/drawHeader/drawEmptyFooter UI helpers. Recovering correct field/global semantics confidently would require modeling those global types across other components first; implementing now risks subtly-wrong rendering/state. Defer until the referenced singletons (the menu/progress-bar holders and font/text holders) are identified and typed.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_BackButtonPressed` (absent:NFC:14): Misassigned to NFC component. This is a JNI back-button native that sets a global back-button-pressed flag (the same int* the platform/Android input layer polls), not part of the NFC purchase bridge. Its owning TU is the input/menu module at ghidra 0x79e5c..0x7a3c8 whose state lives as file-private statics in src/platform/recovered_7a41c.cpp. Defining it in NFC.cpp would need the back-button-flag global, which is not owned by NFC; correct home is the platform module (out of component).
- [ ] `_Z10SetKeyCodePKcii` (absent:NFC:14): Belongs to the platform virtual-key input module, not NFC. SetKeyCode(name,slot,code) scans the SAME g_virtualKeys table (0x58 stride, 0x1080 bytes) that is an anonymous-namespace static in src/platform/recovered_7a41c.cpp (used by keyEventReleased there) and writes keyCode/altCode1..3 at +0x18/+0x1c/+0x20/+0x24. Implementing it in NFC.cpp would require either redeclaring that shared static table (creating a DISTINCT object and silently splitting input state — a forbidden state hack) or editing recovered_7a41c.cpp/.h (out of component). Needs the keymap promoted to a shared header owned by the platform module.
- [ ] `_Z10keyPressedPN11AbyssEngine6EngineEi` (absent:NFC:14): Platform input-dispatch function, not NFC. keyPressed(Engine*,int) reads/writes the shared g_virtualKeys table, the global focused-input-mode int, the action-key/pointer-capture globals, calls GetKeyState and ApplicationManager::GetCurrentApplicationModule/OnTouchBegin, and writes Engine fields (e.g. +0x360). All of that state is module-private to src/platform/recovered_7a41c.cpp (or owned by ApplicationManager/Engine). Cannot be defined correctly in NFC.cpp without faking the shared statics or editing out-of-component files; home is the platform input module.
- [ ] `_Z11UseJoysticki` (absent:NFC:14): Platform mouse/pointer config setter, not NFC. Writes a module-private 'use joystick' global that lives in the input layer (src/platform/recovered_7a41c.cpp neighbourhood). NFC owns no such global; redeclaring it in NFC.cpp would create a distinct object split from GetUseJoystick's. Home is the platform module.
- [ ] `_Z12CaptureMousei` (absent:NFC:14): Platform pointer setter, not NFC. Writes the g_pointerCaptured-style capture flag that is a file-private static of src/platform/recovered_7a41c.cpp (MouseInput uses it). Defining in NFC.cpp would duplicate/split that shared flag. Home is the platform input module (out of component).
- [ ] `_Z14GetUseJoystickv` (absent:NFC:14): Platform pointer/joystick config getter, not NFC. Reads the same module-private 'use joystick' global that UseJoystick(int) writes; both belong to the platform input module's private state, not the NFC bridge.
- [ ] `_Z15IsDialogVisiblei` (absent:NFC:14): Game UI menu-state query, not NFC. Returns whether either of two global dialog/container pointers is non-null (double indirection through module globals owned by the menu/UI subsystem). NFC has no access to those globals; implementing here would need duplicate extern decls of UI state that belongs to another component.
- [ ] `_Z15IsInPrimaryMenui` (absent:NFC:14): Game UI menu-state query, not NFC. Reads five global UI/dialog/submenu pointers and a bool flag (all module globals of the menu subsystem) to decide if the primary menu is showing. None of this state is owned by NFC; correct home is the UI/menu module.
- [ ] `_Z15IsSubMenuActivei` (absent:NFC:14): Game UI submenu query, not NFC. Indexes the same 0x58-stride virtual-key/menu table at +0x40 plus several UI-state globals and calls ApplicationManager::GetCurrentApplicationModule. Depends on the platform/UI module's private state, not the NFC bridge.
- [ ] `_Z15keyEventPressedPN11AbyssEngine6EngineEPc` (absent:NFC:14): Platform input-dispatch function, not NFC. Iterates the shared g_virtualKeys table (0x58 stride, 0x1080 bytes — the anonymous-namespace static in src/platform/recovered_7a41c.cpp, mirror of keyEventReleased) and calls keyPressed. Both the table and keyPressed live in the platform input module; defining keyEventPressed in NFC.cpp would split the shared table or require editing out-of-component files.
- [ ] `_Z18IsDialogNotVisiblei` (absent:NFC:14): Game UI menu-state query (negation of IsDialogVisible), not NFC. Reads two global dialog/container pointers owned by the menu subsystem; that state is not owned by NFC.
- [ ] `_Z19IsDialogNotVisible2i` (absent:NFC:14): Game UI menu-state query, not NFC. Reads three global dialog/container pointers owned by the menu subsystem; not part of the NFC bridge and NFC owns none of that state.
- [ ] `_Z19IsStarMapNotVisiblei` (absent:NFC:14): Game UI state query, not NFC. Returns the inverse of a global 'star map visible' bool owned by the map/UI subsystem; NFC has no access to that global.
- [ ] `_Z21IsInGameSubMenuActivei` (absent:NFC:14): Game UI submenu query, not NFC. Tests an in-game submenu-index global (== -1) owned by the UI/menu module; not NFC state.
- [ ] `_Z24IsInGameSubMenuNotActivei` (absent:NFC:14): Game UI submenu query, not NFC. Reads the in-game submenu-index global, a second UI flag, and a 'star map visible' bool — all menu-subsystem globals, none owned by NFC.
- [ ] `_Z31SwitchToOtherMouseConifgurationi` (absent:NFC:14): Platform mouse-config setter, not NFC. Writes a module-private mouse-configuration global belonging to the input layer (src/platform/recovered_7a41c.cpp neighbourhood); NFC owns no such global.
- [ ] `_Z9HideMousev` (absent:NFC:14): Platform pointer state getter, not NFC. Reads a module-private pointer/mouse global of the input layer; that state lives with the platform input module, not the NFC bridge.
- [ ] `loadAPK` (absent:FModSound:16): NDK/JNI bridge function, not an FModSound member. Decompile shows it opens the APK via zip_open and stores the handle in a global zip pointer (DAT at 0xa3d08), iterating zip_get_num_files/zip_get_name. It belongs in the NDK-bridge translation unit alongside the other ndk23_* entry points, not in the audio component (src/engine/audio/FModSound.cpp/.h are the only files I may edit). Placing it here would be wrong TU + pull in unrelated zip globals.
- [ ] `loadAPKAndZip` (absent:FModSound:16): NDK bridge function (twin of loadAPK). Opens two zip archives (APK + extra zip) via zip_open, stores both into global zip-handle pointers, returns whether both succeeded. Belongs in the NDK-bridge TU, not the audio component.
- [ ] `ndk23_Init` (absent:FModSound:16): Top-level NDK lifecycle entry point. Decompile shows it calls glViewport, stores display width/height into global pointers, calls loadAPK, then new AbyssEngine::Engine, sets app root dir / app version, Engine::Initialize/SetOnDestroyApp, PaintCanvas::SetGameOrientation. Entirely engine/JNI bootstrap; references the global Engine singleton and display globals. Does not belong in FModSound; needs the NDK-bridge file.
- [ ] `ndk23_InitWithZip` (absent:FModSound:16): Twin of ndk23_Init that also takes a second zip path and calls loadAPKAndZip instead of loadAPK. Same engine-bootstrap nature; belongs in the NDK-bridge TU, not the audio component.
- [ ] `ndk23_getCurrentFiredStatus` (absent:FModSound:16): NDK bridge accessor (returns 0 constant in shipped build). Belongs with the other ndk23_* entry points in the NDK-bridge TU, not in FModSound.
- [ ] `ndk23_getExitFlag` (absent:FModSound:16): NDK bridge accessor: returns **(global exit-flag pointer) (DAT at 0xa3cc0). References an NDK-global, belongs in the NDK-bridge TU, not the audio component.
- [ ] `ndk23_getScreenshotFlag` (absent:FModSound:16): NDK bridge accessor (returns 0 in shipped build). Belongs in the NDK-bridge TU, not in FModSound.
- [ ] `ndk23_resetScreenshotFlag` (absent:FModSound:16): NDK bridge no-op (bx lr). Belongs in the NDK-bridge TU with the other ndk23_* functions, not in the audio component.
- [ ] `ndk23_setDisplayHeightAndWidth` (absent:FModSound:16): NDK bridge no-op (bx lr) in shipped build. Belongs in the NDK-bridge TU, not in FModSound.
- [ ] `ndk23_handleAcceleration` (absent:jni_bridge:17): The Ghidra decompile (0xa4018) is materially corrupted in exactly the part that determines correctness: the floating-point argument shuffling into AbyssEngine::Engine::SetAccelValue / SetGravValue comes out as nonsensical CONCAT44(param_2,param_7)/extraout_sN tuples (register-allocation artifacts), so the precise x/y/z ordering and which of the three SetGrav components is the low-pass-filtered axis vs the negated axis cannot be recovered with confidence. The body also does a low-pass gravity filter (alpha=0.95 / 0.05, DAT_000a4108/0xa4110) keyed on a double-deref orientation-flag global and on the current module's first byte ((*(*(engine+0x30))+0x30)[0]), whose semantics I cannot name reliably. Recovering this byte-faithfully would require reverse-engineering the instruction sequence rather than expressing recovered intent, which is forbidden. The JNI wrapper ToJNI_handleAccelerometer that forwards to it IS implemented (negates x, forwards x/y/z); ndk23_handleAcceleration is declared extern "C" so the wrapper links. Needs a clean re-decompile / asm pass focused on the SetAccelValue/SetGravValue call to pin the exact axis mapping and filter wiring.
- [ ] `_ZN11AbyssEngine18IApplicationModule10OnTouchEndEiiPv` (extra:IApplicationModule:21): Requires relocating the entire polymorphic base class IApplicationModule into namespace AbyssEngine (original mangles N11AbyssEngine18IApplicationModule...; ours mangles global N18IApplicationModule...). This is a coordinated cross-hierarchy type-system change: IApplicationModule is a public base with a vtable, and derived classes that override its virtuals live OUTSIDE this component (src/game/menu/MTitle.h: 'class MTitle : public IApplicationModule' overriding OnTouchEnd/OnInitialize/OnRender2D/etc., plus the src/game/ui/* window hierarchy). Relocating the base changes its type identity and mangling, breaking every derived TU's override/vtable resolution. Cannot be done within this component or as piecemeal caller_rewrites (HARD STOP).
- [ ] `_ZN11AbyssEngine18IApplicationModule11OnTouchMoveEiiPv` (extra:IApplicationModule:21): Same blocker: needs IApplicationModule relocated into namespace AbyssEngine, a coordinated change across the derived hierarchy (MTitle + game/ui/* windows) that override this virtual. Out of component; cannot fix piecemeal.
- [ ] `_ZN11AbyssEngine18IApplicationModule12OnTouchBeginEiiPv` (extra:IApplicationModule:21): Same blocker: needs IApplicationModule moved to namespace AbyssEngine; derived overriders (MTitle, game/ui/* hierarchy) are out of component. Coordinated cross-hierarchy change required.
- [ ] `_ZN11AbyssEngine18IApplicationModule21SetApplicationManagerEPNS_18ApplicationManagerE` (extra:IApplicationModule:21): Doubly blocked: (1) IApplicationModule must move to namespace AbyssEngine (cross-hierarchy, see above), and (2) the parameter type is AbyssEngine::ApplicationManager* (PNS_18ApplicationManagerE), but ApplicationManager is still defined at global scope in src/engine/core/ApplicationManager.h (NOT this component) — that class also needs relocating into AbyssEngine. Both are out-of-component type-system changes.
- [ ] `_ZN11AbyssEngine18IApplicationModule8OnResumeEv` (extra:IApplicationModule:21): Same blocker: needs IApplicationModule relocated to namespace AbyssEngine; coordinated change across out-of-component derived classes (MTitle, game/ui/* windows). HARD STOP.
- [ ] `_ZN11AbyssEngine18IApplicationModule9OnSuspendEv` (extra:IApplicationModule:21): Same blocker: needs IApplicationModule relocated to namespace AbyssEngine; coordinated change across out-of-component derived classes. HARD STOP.
- [ ] `_ZN18IApplicationModule10OnKeyPressExx` (extra:IApplicationModule:21): Extra global-scope symbol that exists ONLY because IApplicationModule is ported at global scope instead of namespace AbyssEngine. It disappears (becoming the correct AbyssEngine:: variant) once the class is relocated — but that relocation is a coordinated cross-hierarchy change touching out-of-component derived classes (MTitle, game/ui/* that override these virtuals). Cannot delete/inline because the virtual is part of the vtable consumed by those derived classes. HARD STOP.
- [ ] `_ZN18IApplicationModule10OnRender2DEv` (extra:IApplicationModule:21): Extra global-scope artifact of the global-namespace port; resolved only by relocating IApplicationModule into AbyssEngine, a coordinated cross-hierarchy change (out-of-component overriders MTitle/game-ui). HARD STOP.
- [ ] `_ZN18IApplicationModule10OnRender3DEv` (extra:IApplicationModule:21): Extra global-scope artifact; same namespace-relocation blocker across the out-of-component derived hierarchy. HARD STOP.
- [ ] `_ZN18IApplicationModule10OnTouchEndEii` (extra:IApplicationModule:21): Extra global-scope artifact; same namespace-relocation blocker. This 2-arg virtual is part of the vtable overridden by MTitle (OnTouchEnd(int,int)), so it cannot be inlined/removed. HARD STOP.
- [ ] `_ZN18IApplicationModule10OnTouchEndEiiPv` (extra:IApplicationModule:21): Extra global-scope artifact; correct form is the AbyssEngine:: variant (paired absent _ZN11AbyssEngine18IApplicationModule10OnTouchEndEiiPv). Fixed only by relocating the class into namespace AbyssEngine — coordinated cross-hierarchy change, out of component. HARD STOP.
- [ ] `_ZN18IApplicationModule11OnTouchMoveEii` (extra:IApplicationModule:21): Extra global-scope artifact; same namespace-relocation blocker; virtual overridden by out-of-component derived classes. HARD STOP.
- [ ] `_ZN18IApplicationModule11OnTouchMoveEiiPv` (extra:IApplicationModule:21): Extra global-scope artifact; correct form is AbyssEngine:: variant (paired absent _ZN11AbyssEngine18IApplicationModule11OnTouchMoveEiiPv). Same relocation blocker. HARD STOP.
- [ ] `_ZN18IApplicationModule12OnInitializeEv` (extra:IApplicationModule:21): Extra global-scope artifact; virtual overridden by MTitle and game/ui/* (out of component). Fixed only by class relocation into AbyssEngine. HARD STOP.
- [ ] `_ZN18IApplicationModule12OnKeyReleaseExx` (extra:IApplicationModule:21): Extra global-scope artifact; same namespace-relocation blocker across the out-of-component derived hierarchy. HARD STOP.
- [ ] `_ZN18IApplicationModule12OnTouchBeginEii` (extra:IApplicationModule:21): Extra global-scope artifact; 2-arg virtual overridden by MTitle (out of component); fixed only by relocating the class into AbyssEngine. HARD STOP.
- [ ] `_ZN18IApplicationModule12OnTouchBeginEiiPv` (extra:IApplicationModule:21): Extra global-scope artifact; correct form is AbyssEngine:: variant (paired absent _ZN11AbyssEngine18IApplicationModule12OnTouchBeginEiiPv). Same relocation blocker. HARD STOP.
- [ ] `_ZN18IApplicationModule17ShowLoadingScreenEv` (extra:IApplicationModule:21): Extra global-scope artifact; virtual overridden by MTitle (ShowLoadingScreen) out of component; fixed only by relocating IApplicationModule into namespace AbyssEngine. HARD STOP.
- [ ] `_ZN3Hud10catchCargoEiibbbbbi` (extra:Hud:23): extra=our-build-only. This 8-arg catchCargo is the real implementation (the 7-arg overload forwards to it) but the original binary has no such standalone symbol -> the original inlined the cargo/docking/mission HUD-event builder at every call site. Re-inlining is not a self-contained per-symbol fix: it has FOUR out-of-component call sites (src/game/ship/KIPlayer.cpp:504,544 and src/game/ship/PlayerGasCloud.cpp:306,314) plus the in-component 7-arg forwarder, and the body is a large GameText-formatting/queueing routine. Inlining a multi-statement body into other components' .cpp files is not a simple arg-change caller_rewrite, so this needs a coordinated cross-file refactor.
- [ ] `_ZN3Hud14enterCinematicEii` (extra:Hud:23): extra=our-build-only. enterCinematic forwards through a hidden engine function-pointer slot and the original binary has no Hud::enterCinematic symbol (the dispatch was inlined). It has an out-of-component caller at src/game/menu/MGame.cpp:2264 ('((Hud*)*(void**)self->level)->enterCinematic(self->deltaTime, 1)'). Removing the standalone requires inlining the function-pointer dispatch into MGame's call site -- a multi-statement body, not a self-contained arg-change caller_rewrite -- so it needs a coordinated cross-file change.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_GetAchievementId` (absent:Mesh:25): Misassigned to the Mesh render component. This is an Android JNI native method that reads from a global achievement-id array (orig: load g_achievementIds pointer, return g_achievementIds[index]). It has no relationship to AbyssEngine::Mesh and references global state not present in this component. Belongs in a platform/JNI bridge TU (e.g. src/platform/jni_bridge.cpp) alongside the existing Java_net_fishlabs_* functions. Cannot be implemented in Mesh.cpp/Mesh.h without nonsensical, out-of-component global decls.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_GetLeaderboardScore` (absent:Mesh:25): Misassigned to Mesh component. JNI getter returning g_leaderboardScores[index] from a global array. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU with the other Java_net_fishlabs_* DLC/achievement entry points. Globals are out of this component's scope.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_GetLinkGameGP` (absent:Mesh:25): Misassigned to Mesh component. JNI getter dereferencing a global Google-Play-link struct pointer (return *g_linkGameGP). No connection to AbyssEngine::Mesh; belongs in the platform JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_GetShowAchievements` (absent:Mesh:25): Misassigned to Mesh component. JNI getter dereferencing a global show-achievements flag pointer. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_GetShowLeaderboards` (absent:Mesh:25): Misassigned to Mesh component. JNI getter dereferencing a global show-leaderboards flag pointer. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_ResetAchievements` (absent:Mesh:25): Misassigned to Mesh component. JNI method zeroing 3 words of a global achievements struct (g_ach[0]=g_ach[1]=g_ach[2]=0). Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_ResetLeaderboardScore` (absent:Mesh:25): Misassigned to Mesh component. JNI method setting g_leaderboardScores[index]=0 in a global array. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_ResetLinkGameGP` (absent:Mesh:25): Misassigned to Mesh component. JNI method setting *g_linkGameGP=0 via a global pointer. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_ResetShowAchievements` (absent:Mesh:25): Misassigned to Mesh component. JNI method setting *g_showAchievements=0 via a global pointer. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_ResetShowLeaderboards` (absent:Mesh:25): Misassigned to Mesh component. JNI method setting *g_showLeaderboards=0 via a global pointer. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_GOF2HD2012_SetGPIsLinked` (absent:Mesh:25): Misassigned to Mesh component. JNI setter storing the jint arg into *g_gpIsLinked via a global pointer. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component global state.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_correctBoughtDLC1` (absent:Mesh:25): Misassigned to Mesh component. JNI tail-call: correctBoughtDLC(0, true). Tail-jumps to a shared DLC-correction helper. Unrelated to AbyssEngine::Mesh; belongs in the platform JNI bridge TU with the helper. Out-of-component.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_correctBoughtDLC2` (absent:Mesh:25): Misassigned to Mesh component. JNI tail-call: correctBoughtDLC(1, true). Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_correctBoughtDLC3` (absent:Mesh:25): Misassigned to Mesh component. JNI tail-call: correctBoughtDLC(2, true). Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_correctBoughtDLC4` (absent:Mesh:25): Misassigned to Mesh component. JNI tail-call: correctBoughtDLC(3, true). Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_correctBoughtDLC5` (absent:Mesh:25): Misassigned to Mesh component. JNI tail-call: correctBoughtDLC(4, true). Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_getDLC1BOUGHT` (absent:Mesh:25): Misassigned to Mesh component. JNI tail-call to a shared getDLCBought-style helper for DLC slot 1. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component.
- [ ] `Java_net_fishlabs_gof2hdallandroid2012_ToJNI_getDLC2BOUGHT` (absent:Mesh:25): Misassigned to Mesh component. JNI tail-call to a shared getDLCBought-style helper for DLC slot 2. Unrelated to AbyssEngine::Mesh; belongs in the JNI bridge TU. Out-of-component.
- [ ] `_ZN11AbyssEngine6String10ctor_wcharEPKtb` (extra:AEString:26): extra (our build emits it, original has none). It is an in-place RE-init helper (clears + Set_wchar + optional Reverse on an ALREADY-constructed object), not a true constructor, so it cannot be replaced by the real String(const uint16_t*,bool) ctor at its call sites. It is called as obj.ctor_wchar(s,false) from 9+ out-of-component sites I may not edit: src/engine/core/GameText.cpp (lines 173,186,207,341,345), src/engine/file/FileInterfaceAndroid.cpp (lines 373,377,412,432), and inline in src/engine/file/AEFile.cpp (AEStr_init). Converting each to assignment `obj = String(s,false)` changes semantics/temporaries and is not a self-contained single-arg call-site rewrite. Needs a coordinated pass over those files. Left in place as a thin delegator to the real Set(unsigned short const*).
- [ ] `_ZN11AbyssEngine6String10ctor_floatEf` (extra:AEString:26): extra (our build emits it, original has none). Same situation as ctor_wchar: an in-place re-init helper (clear + Set(float)), called as obj.ctor_float(v) from out-of-component src/engine/core/AbyssEngine.cpp lines 1281 and 2761. Cannot be expressed as a trivial call-site arg change (would require obj = String(v) assignment semantics). Needs a coordinated edit to AbyssEngine.cpp. Left in place delegating to the real Set(float).
- [ ] `_ZN11AbyssEngine6String11Compare_strEPS0_` (extra:AEString:26): extra; the real method is Compare(AbyssEngine::String const&), now implemented (_ZNK..7CompareERKS0_). Compare_str(String*) is kept as a delegator because it is called from 12 out-of-component sites across 7 files (src/game/ui/ChoiceWindow.cpp 247-248, src/game/ui/Hud.cpp 566,697, src/game/core/Globals.cpp 706, src/game/world/Level.cpp 2545, src/game/menu/SpaceLounge.cpp 560/562/564/566, src/game/ship/Player.cpp 1272, src/engine/core/AbyssEngine.cpp 2686). Several call sites are byte-identical (SpaceLounge.cpp has 3 identical `missionText.Compare_str((String *)text)` lines), so serial find/replace caller_rewrites cannot uniquely target them and would be applied incorrectly. Needs a manual coordinated rewrite of those call sites (x->Compare_str(&y) -> x->Compare(y); a.Compare_str((String*)p) -> a.Compare(*(String*)p)) before the standalone can be deleted.
- [ ] `_ZN11AbyssEngine18ApplicationManager10CheckCrackEPKc` (absent:ApplicationManager:29): Needs whole-class relocation into namespace AbyssEngine (see shared reason below). CheckCrack is also currently ABSENT entirely; once relocated, add `bool CheckCrack(const char* name)` returning false (binary: `movs r0,#0; bx lr`, anti-piracy stub).
- [ ] `_ZN11AbyssEngine18ApplicationManager10OnKeyPressEi` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::OnKeyPress(int); only the namespace prefix is wrong. Blocked on the namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager10OnTouchEndEiiPv` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::OnTouchEnd(int,int,void*); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager10OnTouchEndEv` (absent:ApplicationManager:29): This nullary OnTouchEnd() is currently named OnTouchEndSimple() (verified: clears currentKey/keyState/actionMask/actionState fields, matches 0x93c90). Rename to OnTouchEnd() AND relocate into namespace AbyssEngine. Blocked on relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager10SoundPauseEi` (absent:ApplicationManager:29): Currently SoundPause() with no param; binary signature takes an unused int (mangled ...SoundPauseEi). Add the unused int param AND relocate into namespace. Blocked on relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager11CheatEnableEb` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::CheatEnable(bool); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager11CheatUpdateEt` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::CheatUpdate(unsigned short); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager11GetKeyStateEv` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::GetKeyState(); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager11OnTouchMoveEiiPv` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::OnTouchMove(int,int,void*); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager11SoundEnableEb` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::SoundEnable(bool); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager11SoundResumeEi` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::SoundResume(int); signature already correct, only namespace prefix wrong. Blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager11SoundResumeEv` (absent:ApplicationManager:29): This nullary SoundResume() is currently named SoundResumeSelf() (verified: soundResource->resume(), matches 0x922a8). Rename to SoundResume() (overload of the int variant) AND relocate into namespace. Blocked on relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager12CheatAddCodeERKNS_6StringEi` (absent:ApplicationManager:29): Currently CheatAddCode(void* code, int) doing AddCheatCode(*(String*)code, value). Retype first param to `const AbyssEngine::String&` AND relocate class into namespace. Blocked on relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager12OnKeyReleaseEi` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::OnKeyRelease(int); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager12OnTouchBeginEiiPv` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::OnTouchBegin(int,int,void*); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager12SoundReleaseEi` (absent:ApplicationManager:29): Currently SoundRelease() with no param; binary signature takes an unused int (mangled ...SoundReleaseEi). Add the unused int param AND relocate into namespace. Blocked on relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager13ResetKeyStateEv` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::ResetKeyState(); blocked on namespace relocation.
- [ ] `_ZN11AbyssEngine18ApplicationManager13SoundFxEnableEb` (absent:ApplicationManager:29): Body already implemented as global ApplicationManager::SoundFxEnable(bool); blocked on namespace relocation.
- [ ] `_Z17BuildResourceListPN11AbyssEngine6EngineE` (extra:FModSound:30): Function is too large to recover confidently: Ghidra decompile times out (>180s) on it; the body is a ~10KB-stack-frame resource registrar that issues hundreds of PaintCanvas::TextureCreateGlobal / ResourceTexture(...) calls with per-resource literal ids. Reconstructing it faithfully would require transcribing the raw disassembly call-by-call, which is exactly the error-prone byte-reconstruction the task forbids. It also has zero references anywhere in src/, so no call site forces its shape. Defer until it can be decompiled in chunks.
- [ ] `_Z18Globals_drawLines4jPvP5ArrayIPN11AbyssEngine6StringEEii` (extra:FModSound:30): drawLines4 forwards to drawLines5; the real Globals::drawLines overloads have NO exported symbol in the binary (inlined in original). Methodizing requires defining a coherent Globals::drawLines overload set AND reconciling the unrelated callers that use a DIFFERENT name 'Globals_drawLines' with mismatched signatures across Radio.cpp, StatusWindow.cpp (mine) and Layout.cpp (NOT mine, currently being refactored by a concurrent worker — its Layout.cpp declares Globals_drawLines as variadic). This is a coordinated cross-file signature unification touching a concurrently-edited file; defer to a single owner.
- [ ] `_Z18Globals_drawLines5jPvP5ArrayIPN11AbyssEngine6StringEEiii` (extra:FModSound:30): Same cluster as drawLines4: real Globals::drawLines(uint,Array<String*>*,int,int,bool) has no binary symbol; unifying the drawLines4/5/7 overload set and the divergent 'Globals_drawLines' call sites in Layout.cpp (concurrent worker), Radio.cpp and StatusWindow.cpp must be done together. Defer.
- [ ] `_Z18Globals_drawLines7jP5ArrayIPN11AbyssEngine6StringEEiiji` (extra:FModSound:30): Same cluster: real Globals::drawLines(uint,Array<String*>*,int,int,uint,bool) has no binary symbol; needs the coordinated drawLines overload unification + call-site reconciliation across Layout.cpp (concurrent), Radio.cpp, StatusWindow.cpp. Defer.
- [ ] src/engine/core/ApplicationManager.h: AbyssEngine::ApplicationManager is MISSING a public `int GetCurrentApplicationModule() const;` declaration (mangles to _ZNK11AbyssEngine18ApplicationManager27GetCurrentApplicationModuleEv). It exists in the binary and is called by keyReleased/MouseWheel/simulateTouch/ActualizeMouseVisibilty; needed before those four can be implemented in recovered_7a41c.cpp.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (absent:GameData:0): OnCreateApplication is a large application bring-up routine that cannot be recovered as clean, idiomatic C++ this pass. The decompiled body (1) allocates a 200-byte GameData and writes ~30 distinct fields NOT in GameData.h (offsets 0x4,0x8,0xc,0xe,0x10,0x14,0x3c-0x4c,0x50-0x6c,0x6d-0x77,0x78-0x7a,0xa0,0xa4,0xa5,0xb4,0xc4) whose true layout/semantics are unknown — every one would be a meaningless field_0xNN, and recovering the full GameData layout is a separate larger effort; (2) reads/writes ~30 opaque DAT_* global variables (boolean screen-size flags, gAppManager/paintCanvas mirrors, font/text globals, AERandom seed, etc.) whose real declared global identities are not known — implementing them would mean fabricating reinterpret_cast pokes into anonymous addresses, i.e. exactly the byte-match clutter the goal forbids; (3) drives many cross-module singletons (GameText ctor+setLanguage, MGame/ModMainMenu/ModStation/MTitle ctors + ApplicationManager::RegisterApplicationModule, Globals::init, Layout::initTip, Status::resetGame, AERandom::reset, PaintCanvas::TextureCreateGlobal, ApplicationManager Cheat* APIs, Engine::GetDeviceInfo resolution gating). Faithfully expressing the device-info/screen-size gating and the global-flag writes requires confidently naming those globals and GameData fields first. Recommend deferring until GameData's full field layout and the OnCreateApplication global table are recovered and named in a dedicated pass; only then can this be written as real C++ without fabricated field_0xNN/DAT_* accesses.
- [ ] `_ZN8KIPlayer11setPositionEfff` (wrong_type:KIPlayer:3): Both this entry and the paired extra _ZN8KIPlayer12setPosition3Efff are the SAME function: our codebase names the polymorphic float-form position setter 'setPosition3', but the original binary names it 'setPosition(float,float,float)' for KIPlayer AND every derived class (orig symtab has _ZN8KIPlayer11setPositionEfff @0xa6394, _ZN13PlayerFighter11setPositionEfff, _ZN9PlayerEgo11setPositionEfff, _ZN14PlayerJumpgate11setPositionEfff, _ZN14PlayerWormHole11setPositionEfff, _ZN17PlayerFixedObject11setPositionEfff; there is NO ...setPosition3... symbol anywhere). The fix is a coordinated cross-hierarchy rename of the BASE VIRTUAL slot +0x48 from setPosition3 -> setPosition(float,float,float). This is a HARD STOP: it requires editing files OUTSIDE my component (src/game/ship/KIPlayer.{h,cpp}) -- the derived 'override' declarations in PlayerFighter.h, PlayerEgo.h, PlayerJumpgate.h, PlayerWormHole.h, PlayerFixedObject.h, PlayerGasCloud.h; their definitions in PlayerFighter.cpp/PlayerEgo.cpp/PlayerJumpgate.cpp/PlayerWormHole.cpp/PlayerFixedObject.cpp; and call sites in src/game/world/Level.cpp, src/game/weapons/SentryGun.cpp, src/game/core/CutScene.cpp, src/game/menu/SpaceLounge.cpp. Renaming only the base in KIPlayer.h would break every derived TU ('override' with no matching base virtual). Per the rules, a base-class virtual rename across derived overrides must NOT be emitted as caller_rewrites and must be deferred. NOTE: the asm also shows the float-form is NON-virtual and DISPATCHES to a VIRTUAL setPosition(Vector const&) at vtable slot 0x44 (currently we have the virtualness inverted), which is an additional vtable-layout change spanning the same hierarchy. (Unrelated: TouchButton::setPosition3(int,int,int) is a different class, not part of this rename.)
- [ ] `_ZN8KIPlayer12setPosition3Efff` (wrong_type:KIPlayer:3): Paired with _ZN8KIPlayer11setPositionEfff -- same function. Our 'setPosition3' is a codebase-wide misnomer for the original 'setPosition(float,float,float)' (no setPosition3 symbol exists in the original .so). Removing this 'extra' = renaming the base virtual setPosition3 -> setPosition(float,float,float), which is a coordinated cross-hierarchy change touching derived overrides/definitions and call sites in ~13 files outside my component (PlayerFighter/PlayerEgo/PlayerJumpgate/PlayerWormHole/PlayerFixedObject/PlayerGasCloud headers+cpps, Level.cpp, SentryGun.cpp, CutScene.cpp, SpaceLounge.cpp). HARD STOP per the rules: base-virtual rename across overrides cannot be done within KIPlayer.{h,cpp} alone and must not be split into per-TU caller_rewrites.
- [ ] `_ZN14PlayerJumpgate11setPositionEfff` (extra:PlayerJumpgate:4): Same function as the 'extra' _ZN14PlayerJumpgate12setPosition3Efff: our build names this override setPosition3, but the original binary names the whole virtual family setPosition(float,float,float) (confirmed: 0xb51c0 is named setPosition in Ghidra; the original mangles _ZN14PlayerJumpgate11setPositionEfff). Renaming PlayerJumpgate's override alone to setPosition would break the 'override' against the base virtual KIPlayer::setPosition3(float,float,float). The correct fix is a HARD-STOP coordinated cross-hierarchy rename of the base virtual KIPlayer::setPosition3 -> setPosition (overloaded with the existing KIPlayer::setPosition(const Vector&)), applied to KIPlayer (base, KIPlayer.h:150 + KIPlayer.cpp:412) and every derived override that uses 'override': PlayerEgo, PlayerFighter, PlayerFixedObject, PlayerWormHole, PlayerJumpgate, plus all call sites (Level.cpp, CutScene.cpp, SentryGun.cpp, SpaceLounge.cpp, PlayerFighter.cpp, PlayerFixedObject.cpp, PlayerEgo.cpp, PlayerWormHole.cpp). All of these files are OUTSIDE this component, and base-virtual/override renames cannot be emitted as caller_rewrites. Defer to a coordinated pass that owns the KIPlayer hierarchy.
- [ ] `_ZN14PlayerJumpgate12setPosition3Efff` (extra:PlayerJumpgate:4): This 'extra' symbol IS the misnamed standalone for the absent _ZN14PlayerJumpgate11setPositionEfff (identical body: store (int)x/y/z into positionX/Y/Z and tail-call geometry->setPosition(x,y,z)). It is a called virtual (vtable slot +0x48), not inlinable. Renaming requires the same coordinated KIPlayer::setPosition3 -> setPosition base-virtual hierarchy rename described above (base + all Player* overrides + all call sites, all outside this component). Cannot rename PlayerJumpgate's override in isolation without breaking 'override' against the base. Defer with the absent entry as one coordinated cross-hierarchy fix.
- [ ] `_ZN14PlayerWormHole11setPositionEfff` (extra:PlayerWormHole:5): SAME FUNCTION as the 'extra' _ZN14PlayerWormHole12setPosition3Efff. Ghidra-verified (0xb5336): the original PlayerWormHole::setPosition(float,float,float) stores the raw floats at +0x58/+0x5c/+0x60 (posX/posY/posZ), the truncated ints at +0x124/+0x128/+0x12c (positionX/positionY/positionZ), then tail-calls geometry->setPosition via [this+8] -- byte-identical to our current PlayerWormHole::setPosition3 body. The original binary names ALL of these (KIPlayer + every ship/actor subclass: KIPlayer@72148, PlayerEgo, PlayerFighter, PlayerFixedObject, PlayerJumpgate, PlayerWormHole@b5336, etc.) 'setPosition' (mangled 11setPositionE) -- it is a legal C++ overload pair with the Vector-form setPosition(const Vector&). The port renamed the float-form virtual to 'setPosition3' purely to disambiguate. To produce the original symbol this PlayerWormHole method must be named 'setPosition', but it is an override of the actor vtable slot +0x48 virtual declared as 'virtual void setPosition3(float,float,float)' in the BASE class KIPlayer (src/game/ship/KIPlayer.h:150). Renaming only PlayerWormHole would silently break the override (no longer matches the base virtual) and the vtable. The correct fix is a coordinated cross-hierarchy rename setPosition3 -> setPosition across: base KIPlayer.h:150 (the virtual decl), every override declaration (PlayerFixedObject.h:98, PlayerFighter.h:168, PlayerJumpgate.h:30, PlayerEgo.h:344, PlayerWormHole.h:33) and their .cpp definitions, plus ~30 call sites (Level.cpp, CutScene.cpp, SentryGun.cpp, SpaceLounge.cpp, PlayerEgo.cpp, PlayerFighter.cpp, PlayerFixedObject.cpp, KIPlayer.cpp). That touches the base-class virtual and many derived TUs outside this component -> HARD STOP, cannot be done piecemeal or via caller_rewrites.
- [ ] `_ZN14PlayerWormHole12setPosition3Efff` (extra:PlayerWormHole:5): SAME FUNCTION as the 'absent' _ZN14PlayerWormHole11setPositionEfff (see that entry's reason). Our PlayerWormHole::setPosition3 IS the original PlayerWormHole::setPosition; it is 'extra' only because the port renamed the actor vtable slot +0x48 virtual from 'setPosition' to 'setPosition3' hierarchy-wide. Removing this extra == renaming it back to 'setPosition', which is the same coordinated cross-hierarchy rename of the KIPlayer base virtual and all derived overrides/callers described above. Cannot rename only PlayerWormHole without breaking the override against base KIPlayer's 'setPosition3' virtual and the vtable. Deferred as a cross-hierarchy type-system change outside this component.
- [ ] `loadAPK` (absent:FModSound:6): Misassigned component. loadAPK is an extern "C" free function in the platform/NDK layer (libzip APK loader): it calls zip_open/zip_get_num_files/zip_get_name and stores the archive handle into a shared platform global (binary .bss, the same g_apkArchive pointer read by ndk23_Init). It has nothing to do with the FMOD audio sound manager. Its correct home is src/platform/jni_bridge.cpp (or a sibling platform TU) alongside the other ndk23_*/asset-loading symbols, which is OUTSIDE my permitted files (only src/engine/audio/FModSound.{cpp,h}). Placing zip-loader code in the audio TU would be a gross miscategorization and force pulling libzip + platform-global headers into an unrelated component.
- [ ] `loadAPKAndZip` (absent:FModSound:6): Same as loadAPK: extern "C" platform asset loader. Opens two libzip archives (apk + zip) via zip_open and stores both handles into shared platform .bss globals, returning whether both succeeded. Belongs in the platform/NDK layer (src/platform/jni_bridge.cpp), not in the FMOD audio sound manager. Correct file is outside my permitted set.
- [ ] `ndk23_Init` (absent:FModSound:6): extern "C" NDK bring-up entry point (sibling of ndk23_InitWithZip already declared in src/platform/jni_bridge.cpp). It calls glViewport, loadAPK, constructs AbyssEngine::Engine (operator new + ctor), sets String app version via getAppVersion, calls AEFile::SetAppRootDir, Engine::Initialize/SetOnDestroyApp, and PaintCanvas::SetGameOrientation, publishing the Engine* into a platform global. It is the application/render bootstrap, entirely unrelated to FMOD audio. Its real home is the platform layer (jni_bridge.cpp / NDK TU), outside my permitted files; implementing it in FModSound.cpp would require pulling Engine/PaintCanvas/AEFile/String/zip headers into the audio component.
- [ ] `ndk23_getCurrentFiredStatus` (absent:FModSound:6): extern "C" NDK status hook (`return 0;`). Part of the ndk23_* host-status family that lives in the platform/NDK layer (src/platform/jni_bridge.cpp, alongside ndk23_getExitFlag/getScreenshotFlag). Not an audio function; correct home is outside my permitted FModSound files.
- [ ] `ndk23_setDisplayHeightAndWidth` (absent:FModSound:6): extern "C" NDK lifecycle hook (empty body / no-op in shipped build). Part of the ndk23_* platform host hooks (src/platform/jni_bridge.cpp). Unrelated to FMOD audio; correct home is outside my permitted FModSound files.
- [ ] `_ZN13PlayerFighter11setPositionEfff` (wrong_type:PlayerFighter:9): This is the PlayerFighter override of the actor vtable slot +0x48 virtual. In the original binary that virtual is named setPosition(float,float,float) across the WHOLE hierarchy (Ghidra confirms: zero 'setPosition3' symbols exist anywhere in android_2.0.16_libgof2hdaa.so; the PlayerFighter slot at 0xecbb8 is _ZN13PlayerFighter11setPositionEfff). Our codebase invented the name 'setPosition3' to disambiguate it from the non-virtual setPosition(const Vector&). The body in PlayerFighter.cpp:466 is already correct (caches posX/Y/Z, AEGeometry::setPosition, workingPosition Vector assign, Trail::reset, getMatrix/Matrix assign). The ONLY defect is the method name, which is dictated by the base virtual. Fixing it requires a coordinated cross-hierarchy virtual rename setPosition3 -> setPosition(float,float,float): base declaration is 'virtual void setPosition3(float,float,float)' in KIPlayer.h:150 (OUT of component), and overrides exist in PlayerEgo (.h:344/.cpp:299), PlayerFixedObject (.h:98/.cpp:767), PlayerJumpgate (.h:30/.cpp:69), PlayerWormHole (.h:33/.cpp:100), plus '->setPosition3(...)' call sites in those same .cpp files. Renaming PlayerFighter's method alone would break its 'override' against the base virtual and corrupt the vtable. This is the HARD STOP case (base-class virtual rename touching headers + every derived override); it cannot be done within this component nor expressed as caller_rewrites.
- [ ] `_ZN13PlayerFighter12setPosition3Efff` (wrong_type:PlayerFighter:9): Same function as the wrong_type entry above (our 'setPosition3' override, which mangles to _ZN13PlayerFighter12setPosition3Efff). It is 'extra' because the original has no setPosition3 symbol — the original name is setPosition(float,float,float). Resolving it requires the same coordinated base-virtual rename in KIPlayer.h plus all derived overrides (PlayerEgo/PlayerFixedObject/PlayerJumpgate/PlayerWormHole) and their call sites, all OUT of this component. Deferred for the identical cross-hierarchy reason; renaming only PlayerFighter's override would break 'override'/vtable consistency with the base.
- [ ] `_Z15IsSubMenuActivei` (absent:NFC:11): Body indexes the global on-screen menu-button table `keys` (symbol at .data 0x224010, stride 0x58) reading field +0x40 as an int and comparing against text-id range 0x578 / sub_menu_button_count, then falls through to topMenuIndex/is_menu_visible/subMenuIndex and AbyssEngine::ApplicationManager::GetCurrentApplicationModule(appManager)==5. The element type of that table (the VirtualKey struct, stride 0x58) is defined ONLY as a file-private anonymous-namespace struct inside src/platform/recovered_7a41c.cpp, where the `keys` symbol itself is modeled as the private static `g_virtualKeys`; no shared/owning header exposes either the struct type or a typed `keys` global. Implementing IsSubMenuActive cleanly therefore requires either a forbidden local stub struct mirroring VirtualKey, or promoting the menu-button table struct + `keys` global into a real shared header (a cross-file type-system change in src/platform/recovered_7a41c.{h,cpp}, outside this component's editable files). Needs that header to exist before it can be written without a stub.

## setPosition3 -> setPosition family rename (FULLY ANALYZED — ready for a dedicated coordinated pass)
Original binary names the actor float-form positioner setPosition(float,float,float) for KIPlayer AND
every derived class; there is NO setPosition3 symbol anywhere. Coordinated rename of the base virtual
+ all overrides + call sites:
- KIPlayer.h:150 `virtual void setPosition3(fff)` -> `setPosition(fff)` (+ KIPlayer.cpp def). KIPlayer
  already has setPosition(const Vector&) [154] -> the two become overloads (matches original).
- Derived overrides setPosition3(fff) override -> setPosition(fff) override: PlayerFighter.h:168,
  PlayerFixedObject.h:98, PlayerJumpgate.h:30, PlayerWormHole.h:33, PlayerEgo.h:344 (+ their .cpp defs).
- PlayerEgo DEDUP (verified via Ghidra 0xab91c): the REAL PlayerEgo::setPosition(fff) packs x/y/z into
  a Vector and calls geometry->setPosition(Vector) = our setPosition3 body [PlayerEgo.cpp:299]. The
  separate PlayerEgo::setPosition(fff) [decl 342 / def 409] that calls geometry->setPosition(x,y,z)
  directly is the SPURIOUS extra -> DELETE it; rename setPosition3 [344/299] to setPosition.
- setPosition_vec -> setPosition (the Vector overload): PlayerFixedObject.h:99/.cpp + CutScene.cpp caller.
- Call sites of the KIPlayer-hierarchy setPosition3: Level.cpp (11x), SentryGun.cpp, CutScene.cpp,
  SpaceLounge.cpp, AEGeometry.cpp — rename to setPosition, but VERIFY each receiver is KIPlayer-derived.
- EXCLUDE TouchButton: TouchButton::setPosition3(int,int,int) is a DIFFERENT class; the original is
  setPosition(int,int,unsigned char) [+ setPosition(int,int)] — a rename+retype handled separately,
  NOT part of this family. Do not blanket-sed setPosition3.
Build-gate every affected TU; watch linked-exact. A naive piecemeal base-only rename cascaded to 29
broken TUs before — do it all-at-once and verify.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (absent:GameData:0): OnCreateApplication is too large/opaque to recover confidently without shortcuts. (1) The body writes through ~25 anonymous global variables via DAT_xxx pointers (e.g. **(undefined1**)(DAT_000e0c54+...)=1, *(bool*)uVar2=bVar3, the GetDeviceInfo-derived flags stored to *puVar6/*pbVar15/*puVar12, the 0xbf000000 and DAT_000e0bd4 stores near the end) whose real global symbols/types are not identifiable, so writing them as named globals would be guesswork. (2) GameData's modeled layout only covers the eight AbyssEngine::String members, but this ctor-helper raw-writes ~15 unmodeled fields at offsets 0x4,0xc,0xe,0x10,0x14,0x3c-0x4c,0x50-0x68(two 16-byte vector copies),0x6d-0x7a,0xa0-0xc4 that would all need recovered semantic names/types first. (3) It calls BuildResourceList(Engine*), which has no symbol or declaration anywhere in the tree. (4) Two key calls are register-corrupted in the decompile: the triple (*pcVar9)(&local_34,param_1) (an Engine::GetDeviceInfo-style out-param call) and PaintCanvas::TextureCreateGlobal(this_06, SVar16{extraout_r3,...}, in_stack_ffffffb0) whose actual String/unit arguments are unrecoverable. Recovering this needs a coordinated GameData-layout + global-variable identification pass across multiple components before it can be expressed as clean C++.
- [ ] `loadAPK` (absent:FModSound:3): Platform/NDK free C function (opens the APK via zip_open/zip_get_num_files/zip_get_name and stashes the archive handle into the loader's zip-handle global). It has zero relationship to the FMOD audio layer and references nothing in FModSound. It belongs in the platform/NDK loader TU (alongside the existing ndk23_*/zip glue, e.g. src/platform/recovered_a462c.cpp or jni_bridge.cpp), not in src/engine/audio/FModSound.cpp. Implementing it here would be a semantic misplacement and would require declaring the zip_* externs and the archive-handle global in an audio TU. Needs the platform component (files I don't own) to add it as a clean extern "C" free function with a real zip_t* g_apkZip global.
- [ ] `loadAPKAndZip` (absent:FModSound:3): Same as loadAPK: platform/NDK loader free function (two zip_open calls publishing the apk + zip archive handles into loader globals, returns whether both opened). Unrelated to FModSound; belongs in the platform/NDK loader TU, not in the FMOD audio source. Defer to the platform component.
- [ ] `ndk23_Init` (absent:FModSound:3): Top-level NDK init free function. Body: glViewport, stores width/height into engine globals, zeroes a 6-pointer listener block, calls loadAPK, news + constructs AbyssEngine::Engine, assigns its name via AbyssEngine::String, calls AbyssEngine::getAppVersion, AEFile::SetAppRootDir, Engine::Initialize, Engine::SetOnDestroyApp, PaintCanvas::SetGameOrientation, and publishes the engine into the engine-root global. This pulls the entire engine/platform bring-up (Engine, PaintCanvas, String, AEFile, getAppVersion, glViewport, engine-singleton global) into what would be an FMOD audio TU. It is platform-layer code that must live in the NDK/platform component (jni_bridge.cpp already forward-declares ndk23_InitWithZip there). Cannot be placed cleanly in FModSound without heavy cross-cutting includes that violate file discipline. Defer to the platform component.
- [ ] `ndk23_getCurrentFiredStatus` (absent:FModSound:3): Trivial NDK status hook that unconditionally returns 0. It is a platform/NDK lifecycle stub (sibling of the ndk23_* hooks declared in src/platform/jni_bridge.cpp), with no audio relationship. Belongs in the platform component next to the other ndk23_* functions, not in FModSound.cpp. Defer to the platform component.
- [ ] `ndk23_setDisplayHeightAndWidth` (absent:FModSound:3): Trivial NDK no-op hook (bx lr / empty body). Platform/NDK lifecycle stub, sibling of the ndk23_* hooks in src/platform/jni_bridge.cpp, with no audio relationship. Belongs in the platform component, not in FModSound.cpp. Defer to the platform component.
- [ ] `_Z15IsSubMenuActivei` (absent:NFC:6): IsSubMenuActive(int player) reads g_virtualKeys[player] (the on-screen virtual-key table at binary .data 0x224010, stride 0x58, field +0x40 treated as an int keycode) and compares keycode-0x1401 against the active menu's TouchButton-array length (0x227cc4), then falls through to topMenuIndex/menu_touch_window_type/subMenuIndex and ApplicationManager::GetCurrentApplicationModule()==5. The blocker: g_virtualKeys and its `struct VirtualKey` are a file-local `static` inside an anonymous namespace in src/platform/recovered_7a41c.cpp (NOT in this component). To implement this function in NFC.cpp referencing the SAME real array (not a stub/forward-decl), VirtualKey must be moved to a shared header and g_virtualKeys must be de-static'd with an extern declaration — edits to recovered_7a41c.cpp and recovered_7a41c.h, both outside the allowed component files. Additionally the per-player TouchButton-count global at 0x227cc4 (written in ModStation::OnInitialize as the menu button-array length) has no shared declaration. These are cross-file definition/header changes, not self-contained call-site rewrites, so per the HARD STOP rule they cannot be emitted as caller_rewrites. Coordinated change needed: expose VirtualKey + g_virtualKeys via a shared platform header, expose the menu-button-count global, then IsSubMenuActive can be written cleanly in NFC.cpp.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (absent:GameData:0): OnCreateApplication(AbyssEngine::Engine*) is the full application-bootstrap hook (~0x460 bytes at 0xe0738). Faithful recovery is blocked by ~30 distinct named global variables it writes to (DAT_000e0bd4..0xe0c68), each reached via a PC-relative GOT slot whose underlying named symbol is unresolved in Ghidra (e.g. screen-width/height globals, language globals, several bool device/quality flags, the current-PaintCanvas global, the GameText* global, a -1-initialized int global, etc.). The DeepOpen oracle only lists the bare symbol, no Java body, so the global identities and their owning headers cannot be recovered confidently. Reconstructing it would require either (a) manually walking every GOT slot to a named symbol and locating/declaring each global in its correct owning header (cross-component header edits outside GameData.cpp/.h), or (b) fabricating global identities / planting forward-decls and local stubs (the explicitly-forbidden #1 defect). The function also constructs and registers GameText/MGame/ModMainMenu/ModStation/MTitle and calls Globals::init/loadFont, Layout::initTip, Status::resetGame, AERandom::reset, PaintCanvas::TextureCreateGlobal, ApplicationManager::Set*/Register*/Cheat* — none of whose globals/owning-symbol wiring can be expressed without those cross-file declarations. Deferring rather than writing a stubbed/forward-declared approximation. Needs a prior pass that names the ~30 globals and declares each in its real owning header (game state, screen metrics, language, device flags, current-canvas, GameText pointer), after which the body can be written cleanly in GameData.cpp.
- [ ] `_Z10keyPressedPN11AbyssEngine6EngineEi` (absent:recovered_7a41c:4): Body must be DEFINED in src/platform/recovered_7a41c.cpp (the only TU that includes the header), but the .cpp is OUTSIDE my editable component (only recovered_7a41c.h is allowed). It is an exported non-inline global free function whose definition cannot go in the header without making it `inline` (wrong linkage for the exported `T` symbol, a shortcut) or causing an ODR violation. Separately, the body is not confidently recoverable from the header alone: it reads a global 'selected key' state int (DAT_7a3c8+0x7a17c), iterates the 48-entry virtual-key table using fields NOT in the current VirtualKey model (a callback fn-ptr at +0x54, gating ints at +0x44/+0x48), calls GetKeyState, and drives ~10 engine/global float targets via a switch (incl. Engine::field_0x360) plus several file-scope statics that only exist in the .cpp. Implementing it requires adding those statics and the body to the .cpp.
- [ ] `_Z11keyReleasedPN11AbyssEngine6EngineEi` (absent:recovered_7a41c:4): Same file-discipline blocker: definition belongs in recovered_7a41c.cpp (outside my component; header-only is not viable without inline/ODR shortcut). Body also depends on .cpp-only state and unmodeled layout: AbyssEngine::Engine::DrawQuad, ApplicationManager::OnTouchEnd(both overloads), virtual-key table fields at +0x18/+0x1c/+0x20/+0x24/+0x28/+0x30..0x40/+0x4c, and an in-game module struct read at offsets 0x6c/0x14/0x11c/0xc4 driving global float writes — none present in the editable header.
- [ ] `_Z23ActualizeMouseVisibiltyi` (absent:recovered_7a41c:4): Definition belongs in recovered_7a41c.cpp (outside my component). Body needs ~10 module-pointer file-scope globals (all .cpp-private), IsInGameSubMenuNotActive(int), ApplicationManager::GetCurrentApplicationModule, ArrowKeyPressed, plus a pointer-emulation state struct read at +4 — none modeled in the header. Not implementable header-only without a shortcut.
- [ ] `_Z13simulateTouchPN11AbyssEngine6EngineE` (absent:recovered_7a41c:4): Definition belongs in recovered_7a41c.cpp (outside my component). The body is the most complex of the four: it reads the in-game module struct across many offsets (0x2d4,0x32c,0x3dc,0x434,0x48c,0x4e4,0x53c,0x594,0x5ec,0x644), uses SIMD/FP intrinsics (VectorSignedToFloat / FloatVectorMin / FloatVectorMax) with FPSCR artifacts, drives Engine::field_0x360 and ApplicationManager OnTouchBegin/Move/End, and touches ~20 file-scope statics — none of which exist in the editable header. Cannot be recovered confidently and cannot be placed in a header without inline/ODR shortcuts.
- [ ] `loadAPK` (absent:FModSound:5): Misassigned component. loadAPK is an NDK/libzip loader entry point (C-linkage, opens the APK via zip_open and walks zip_get_num_files/zip_get_name, storing the handle into a loader-owned zip global). It belongs in the platform NDK layer (src/platform/jni_bridge.cpp, alongside ndk23_*), NOT in the FMOD audio component. Implementing it in FModSound.cpp would force libzip headers and an unrelated cross-module zip-handle global into the audio TU. Also requires defining the shared zip-handle global (currently absent anywhere in src/). Needs reassignment to the NDK/jni_bridge component.
- [ ] `loadAPKAndZip` (absent:FModSound:5): Misassigned component. Same NDK/libzip loader family as loadAPK: it zip_opens both the APK and the secondary zip and stores both handles into two loader-owned zip globals, returning (apkHandle && zipHandle). Belongs in src/platform/jni_bridge.cpp / NDK layer with the zip globals, not in the FMOD audio TU. Needs reassignment.
- [ ] `ndk23_Init` (absent:FModSound:5): Misassigned component. NDK bring-up entry point: glViewport(0,0,w,h), publishes width/height globals, calls loadAPK, new AbyssEngine::Engine + ctor, stores it into the global Engine slot (g_pEngine in jni_bridge.cpp), sets Engine name via AbyssEngine::String, AbyssEngine::getAppVersion(), AEFile::SetAppRootDir, Engine::Initialize/SetOnDestroyApp with function-pointer callbacks, PaintCanvas::SetGameOrientation. This is the NDK/render loader, belongs in src/platform/jni_bridge.cpp (it pairs with the existing ndk23_InitWithZip / g_pEngine there). Implementing it in FModSound.cpp would pull Engine.h/PaintCanvas/AEFile/String/glViewport and the global Engine slot into the audio TU and duplicate/own cross-module globals it does not own. Needs reassignment to the NDK component.
- [ ] `ndk23_getCurrentFiredStatus` (absent:FModSound:5): Misassigned component. Trivial NDK status hook (returns 0) but it is part of the ndk23_* host-status family in the platform NDK layer (src/platform/jni_bridge.cpp), not an FMOD audio concern. Placing it in the FMOD sound TU would be wrong-home code. Needs reassignment to the NDK component so it sits with the other ndk23_* status hooks.
- [ ] `ndk23_setDisplayHeightAndWidth` (absent:FModSound:5): Misassigned component. Empty NDK display-config hook (bx lr, no-op in shipped build), part of the ndk23_* host lifecycle family in src/platform/jni_bridge.cpp, not FMOD audio. Belongs in the NDK component, not FModSound. Needs reassignment.
- [ ] `_Z10SetKeyCodePKcii` (absent:NFC:6): SetKeyCode(const char*,int,int) iterates the 48-entry VirtualKey table (byte-stride 0x58, base 0x21fd48) matching by name and writing keyCode/altCode1..3 at offsets +0x18/0x1c/0x20/0x24. That table is g_virtualKeys, a file-scope static in the anonymous namespace of src/platform/recovered_7a41c.cpp (its VirtualKey struct is also private there). Implementing this cleanly requires exposing g_virtualKeys (or moving the struct+table into a shared header) from recovered_7a41c.* which is OUTSIDE this component; defining a private duplicate table in NFC.cpp would be unpopulated parallel state (stub-mirror anti-pattern). Defer until the virtual-key table is exported.
- [ ] `_Z15IsSubMenuActivei` (absent:NFC:6): IsSubMenuActive(int) reads g_virtualKeys[param_1] at +0x40 (the private VirtualKey table in src/platform/recovered_7a41c.cpp) plus several other private menu-state globals via PC-relative indirection, and calls AbyssEngine::ApplicationManager::GetCurrentApplicationModule(...) (which is itself only reachable today via the shim ApplicationManager_GetCurrentApplicationModule in src/game/ui/MissionsWindow.cpp, not a real method on ApplicationManager). The symbol is only referenced from a data dispatch table (0x220700), never called directly. A clean implementation needs the virtual-key table exported and the real GetCurrentApplicationModule method — both outside this component. Defer.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (absent:GameData:0): OnCreateApplication is the full application bootstrap and is too under-modeled to recover into clean, idiomatic C++ this pass. Blockers, all in OTHER components' headers (cannot stub/fake per rules): (1) AbyssEngine::Engine::GetDeviceInfo() is declared `void GetDeviceInfo()` but the original returns a device-info struct via a hidden sret pointer; the body reads its fields at +0x00 (width, cmp 0x280/0x320/0x300), +0x04 (height, cmp 0x360/0x400), +0x18 and +0x24 (flag bytes) to derive ~half a dozen booleans. The struct type/layout is not modeled in engine/render/Engine.h. (2) AbyssEngine::PaintCanvas::GetWidth()/GetHeight() are declared returning void but the original uses their int return values; TextureCreateGlobal has the wrong signature too (engine/render/PaintCanvas.h). (3) The body stores those device-derived flags and the screen width/height into ~12 distinct global variables (the DAT_000e07f8/0bd8/0bf0/0bf4..0c68 set) and reads 3 function-pointer globals (the thrice-called Engine accessor at +0xe07c2, the loading callback at DAT_000e0c30/0c34, the cheat callback at DAT_000e0c38) — none of these globals are named or declared in any header. Implementing cleanly requires naming/adding ~15-20 externs across game/engine headers I do not own and correcting GetDeviceInfo/GetWidth/GetHeight/TextureCreateGlobal signatures in engine/render — coordinated cross-component changes outside src/engine/core/GameData.{h,cpp}. Subsystem ctors/calls used (GameData, Globals, GameText::setLanguage/getLanguage, MGame, ModMainMenu, ModStation, MTitle, BuildResourceList, ApplicationManager::Set*/Register*/Cheat*, Layout::initTip, Status::resetGame, AERandom::reset) are all available, so once the device-info struct + screen accessors + the global set are modeled, this becomes implementable.
- [ ] `_Z10keyPressedPN11AbyssEngine6EngineEi` (absent:recovered_7a41c:6): keyPressed(Engine*,int) depends on a large body of module-private state not modeled anywhere in the codebase: besides the (modeled) g_virtualKeys table it reads VirtualKey fields 0x44/0x48/0x50/0x54 via per-row module-id matching, reads a global at *(base+0x7a180), and its trailing switch(uVar14) writes to ~8 distinct module-private globals (separate float-write targets for wheel/pointer/zoom and Engine::field_0x360) whose identities cannot be established from the asm. It also calls the free function GetKeyState(char*) (NOT ApplicationManager::GetKeyState()), which does not exist in src/. Implementing it faithfully would require inventing those globals + struct fields (field_0xNN clutter) and forward-declaring GetKeyState (forbidden stub). Needs the shared input-emulation state module + GetKeyState(char*) recovered first.
- [ ] `_Z11keyReleasedPN11AbyssEngine6EngineEi` (absent:recovered_7a41c:6): keyReleased(Engine*,int) zeroes four module-private global pointers at entry (DAT_0007a5bc/c0/c4/c8 -> **ptr=0) and its trailing switch(uVar6) writes float constants to two more module-private float globals (DAT_0007a5dc/d8) and zeroes another (DAT_0007a5e0); none of these globals are modeled in the file and their semantics cannot be established confidently from the asm. The body also calls Engine::DrawQuad and OnTouchEnd which are present, but the global-state surface it mutates is not. Implementing it would require fabricating those globals (field/stub clutter, forbidden). Needs the shared software-pointer/wheel state recovered as named globals first.
- [ ] `_Z13simulateTouchPN11AbyssEngine6EngineE` (absent:recovered_7a41c:6): simulateTouch(Engine*) is an extremely large float/vector pipeline that (a) reads a module-private game-state struct through a global pointer at dozens of offsets (0x2d4,0x32c,0x3dc,0x434,0x48c,0x4e4,0x53c,0x594,0x5ec,0x644,...) that is not modeled anywhere, (b) uses several unmodeled NEON helper free functions (VectorSignedToFloat, FloatVectorMin, FloatVectorMax), and (c) mutates ~20 module-private pointer/float globals. Faithful idiomatic recovery is not feasible without inventing the entire game-state struct + helpers (forbidden field/stub clutter). Defer until the game-state struct and the Vector/Float NEON helpers are recovered as real types/functions.
- [ ] `_Z23ActualizeMouseVisibiltyi` (absent:recovered_7a41c:6): ActualizeMouseVisibilty(int) branches on ~12 module-private globals not modeled in the file (a +1 frame counter DAT_0007aac4, a hide/show flag DAT_0007aac0, and many **(int**) indirections to game-state flags at e.g. 0x7a928/0x7a92a/0x7a92c/0x7a8fc/0x7a8fe/0x7a900) plus reads through a module pointer at +4 (0x7aa6a). It calls ArrowKeyPressed/IsInGameSubMenuNotActive/GetCurrentApplicationModule (all present) but the bulk of its decision state is unmodeled. Implementing it would require fabricating those globals (field_0xNN/stub clutter, forbidden). Needs the shared input-visibility state recovered as named globals first.
- [ ] `ndk23_handleAcceleration` (absent:jni_bridge:7): Intricate accelerometer high-/low-pass filter (gravity = lowpass(gravity, input) with 0.95/0.05 coefficients; accel = input - gravity) over six .bss double scratch slots (0x227b60..0x227b88), gated on a raw-vs-filtered byte at engine->appManager->paintCanvas[0] and on the rotateAccelValues orientation flag. The decompiler output for the two engine->SetAccelValue()/SetGravValue() calls is garbled with extraout_sN registers, so the exact per-axis sign and the argument-permutation handed to each Set*Value cannot be recovered with confidence. The function's correct C-linkage symbol would compile trivially, but writing a behaviorally-wrong body is worse than deferring; needs a careful softfp-AAPCS register trace of 0xa40c4..0xa4102 to pin the SetAccelValue(x=-b, y=-z, z=-a)/SetGravValue argument order. A shared file-scope `double gAccelFilterState[6]` is already defined in jni_bridge.cpp (and zeroed by ndk23_InitWithZip) for the eventual implementation to use.
- [ ] `_Z10SetKeyCodePKcii` (absent:NFC:8): SetKeyCode(const char*, int, int) iterates the on-screen virtual-key table (48 entries, stride 0x58) reached via the global table-base pointer at binary addr 0x224010, strcmp-matching the entry name and writing param_3 into the entry's keyCode/altCode fields at +0x18/+0x1c/+0x20/+0x24 selected by param_2 (0..3). That exact table is the `g_virtualKeys`/`g_arrowKeys` array (struct VirtualKey) defined inside the ANONYMOUS NAMESPACE of src/platform/recovered_7a41c.cpp (it is the same base pointer used there by keyEventReleased/keyEventPressed/keyReleased/GetKeyState, confirmed via xrefs to GOT 0x21fd48 -> 0x224010). It is not exposed by any header. Implementing SetKeyCode correctly in NFC.cpp requires that table to have external linkage, i.e. editing src/platform/recovered_7a41c.cpp + recovered_7a41c.h (out of this component) to export the VirtualKey struct + table base. The only in-component alternatives are forbidden: a local mirror struct/array (wrong storage identity) or an extern stub of an anonymous-namespace symbol (no external linkage exists). Needs cross-component coordination: move VirtualKey + g_virtualKeys (or an accessor) into a shared header included by both recovered_7a41c.cpp and NFC.cpp.
- [ ] `_Z15IsSubMenuActivei` (absent:NFC:8): IsSubMenuActive(int) indexes the SAME private virtual-key table as SetKeyCode: it computes &table[param_1] (stride 0x58, base pointer at binary 0x224010 = GOT 0x21fd48, shared with the recovered_7a41c.cpp input helpers) and reads the int field at +0x40 (the entry's `touch`/state slot). When that value > 1400 it then consults a menu-entry-limit int global at binary 0x227cc4 (returns false if (field-1401) >= *limit), and the menu flags is_menu_visible(0x227cd0)/topMenuIndex(0x22819c)/subMenuIndex(0x2256b8) already extern'd in NFC.cpp, finally falling back to ApplicationManager::GetCurrentApplicationModule(gAppManager)==5. Two blockers prevent an in-component fix: (1) the virtual-key table is in the anonymous namespace of src/platform/recovered_7a41c.cpp (same out-of-component export problem as SetKeyCode); (2) the menu-entry-limit global at 0x227cc4 (written by OnInitialize/OnKeyPress/OnTouchEnd/draw/addButton/showDlcMenu) is not declared in NFC.cpp and its owning header/name is unidentified. Deferring until the virtual-key table is shared via a header and the 0x227cc4 menu-limit global is identified/exposed.
- [ ] `_Z19OnCreateApplicationPN11AbyssEngine6EngineE` (absent:jni_bridge:1): OnCreateApplication is the full application-bootstrap callback (~250 lines of asm). It news+inits a 200-byte GameData with ~25 raw-offset scalar/String field writes, news a Globals, calls Engine::GetDeviceInfo three times and sets ~12 device-capability boolean globals each living at an unnamed DAT_ .bss address that would need to be individually resolved and given a semantic name, assigns multiple Strings into GameData offsets, then BuildResourceList, news+RegisterApplicationModule for MGame/ModMainMenu/ModStation/MTitle, three CheatAddCode calls, Layout::initTip, Status::resetGame, AERandom::reset and PaintCanvas::TextureCreateGlobal. Recovering it cleanly (no stubs, real named globals/GameData fields) requires modeling many config-flag globals and GameData members not yet present in the headers; cannot complete confidently this pass without shortcuts. The function is still referenced by ndk23_InitWithZip via the existing forward declaration, so leaving it undefined is link-safe pending implementation.
- [ ] `_Z10keyPressedPN11AbyssEngine6EngineEi` (absent:recovered_7a41c:2): keyPressed(Engine*,int) dispatches a press through the 48-entry virtual-key table (g_virtualKeys, which IS modeled) BUT then drives ~10 distinct unmodeled module-private global pointers: a zoom/aim-state object reached via *(DAT_0007a3c8+0x7a17c) read/written with state codes 1..9, a GetKeyState(char*) GLOBAL free function (distinct from ApplicationManager::GetKeyState()) keyed by string literals at DAT_0007a3d4/d8/dc/e0/e4/e8, several float-target globals written via case 0..9 (DAT_0007a3b4..c4 = zoom min/max/center floats) and param_1->field_0x360 (Engine zoom target). These zoom/aim globals and the GetKeyState(char*) helper are not declared in any header I'm allowed to include, and inventing them would require either forward-decl/stub types (forbidden) or guessing the semantics of ~12 magic globals. Need: a header modeling the in-game zoom/aim state globals + the global GetKeyState(const char*) before this can be written cleanly.
- [ ] `_Z11keyReleasedPN11AbyssEngine6EngineEi` (absent:recovered_7a41c:2): keyReleased(Engine*,int) iterates g_virtualKeys (modeled), calls ApplicationManager::OnTouchEnd/OnTouchEndSimple and Engine::DrawQuad (available), but its trailing switch reinterprets the table base (DAT_0007a5d0 target) at offsets 0x14/0x6c/0xc4/0x11c as game-state floats and writes zoom-center floats (0.5f, DAT_0007a5b4/b8) into two separate unmodeled float globals (DAT_0007a5d8+0x7a584, DAT_0007a5dc+0x7a5b0), plus clears 4 module-private pointer globals (DAT_0007a5bc/c0/c4/c8) and one more at DAT_0007a5e0. Same blocker as keyPressed: the zoom/aim float globals are not in any includable header; clean recovery needs them modeled first (no stubbing allowed).
- [ ] `_Z13simulateTouchPN11AbyssEngine6EngineE` (absent:recovered_7a41c:2): simulateTouch(Engine*) is the most complex of the cluster: it reads the MGame flight-module struct (loaded via *(DAT_0007aebc+0x7ab86)) at ~10 fixed steering/firing offsets (0x2d4,0x32c,0x3dc,0x434,0x48c,0x4e4,0x53c,0x594,0x5ec,0x644), runs heavy FP-vector pointer simulation (FloatVectorMin/Max, VectorSignedToFloat, ease curves 1-(x-1)^2) across a dozen unmodeled float globals (DAT_0007aec0..b258), drives the software pointer/wheel and synthesizes OnTouchBegin/Move/End at (0xe8b) handle. The MGame flight struct is not cleanly includable here and the ~15 zoom/pointer float globals are unmodeled; faithful clean recovery is not achievable without stub types (forbidden) or guessing offsets/semantics. Defer until the flight-module struct + pointer/zoom float globals are modeled in shared headers.
- [ ] `_Z23ActualizeMouseVisibiltyi` (absent:recovered_7a41c:2): ActualizeMouseVisibilty(int) gates the emulated cursor on input activity + game state: it calls ArrowKeyPressed() (modeled), ApplicationManager::GetCurrentApplicationModule and IsInGameSubMenuNotActive(2) (available), but branches on ~12 distinct unmodeled module-private global pointers (DAT_0007aa84..aae8: a visibility-state object, an idle-frame counter, an activity byte flag, three steering-vector int globals checked for !=0, two char menu-flags, and a per-module hide flag at *(submenu)+4). None of these visibility/idle/menu globals are declared in an includable header; modeling them by guessing is exactly the wrong-semantics risk the guidance says to avoid. Defer until the cursor-visibility/idle state globals are modeled.
- [ ] `_Z10SetKeyCodePKcii` (absent:NFC:3): SetKeyCode(char const*,int,int) operates on the file-static virtual-key table that is defined as `static VirtualKey g_virtualKeys[48]` (0x58 stride) in src/platform/recovered_7a41c.cpp. Confirmed via Ghidra: SetKeyCode (ghidra 0x7a064) loads its array base from GOT slot 0x21fd48 -> the SAME table base used by keyEventPressed/keyEventReleased/keyReleased/keyPressed/simulateTouch in recovered_7a41c.cpp (Ghidra mislabels that slot's pointee as Globals::is_dialogue_window_visible, but keyEventPressed at 0x7a11c uses the identical slot as the 0x58-stride key table base). The body iterates all 48 entries, strcmp's name[+0x00] against param_1, and on match writes param_3 into keyCode[+0x18]/altCode1[+0x1c]/altCode2[+0x20]/altCode3[+0x24] selected by param_2 (switch 0..3) -- exactly the VirtualKey layout in recovered_7a41c.cpp. The correct, clean home for this free function is recovered_7a41c.cpp next to its keymap siblings so it can reach the internal-linkage g_virtualKeys table. Implementing it in NFC.cpp (the only file I may edit) would require either exposing the file-static table (an edit to recovered_7a41c.cpp, outside my component) or re-declaring a stub VirtualKey layout / extern alias in NFC.cpp (a forbidden forward-decl/stub of wrong identity). Move: add SetKeyCode to src/platform/recovered_7a41c.{h,cpp} and remove from NFC scope.
- [ ] `_Z15IsSubMenuActivei` (absent:NFC:3): IsSubMenuActive(int) (ghidra 0x79f7c) indexes the same file-static virtual-key table from recovered_7a41c.cpp: it computes `param_1 * 0x58 + tableBase` (tableBase from GOT slot 0x21fd48, the keymap table) and reads field +0x40 (VirtualKey::touch in recovered_7a41c.cpp), comparing it to 0x578/0x579, then mixes in Globals::subMenuIndex (0x2256b8) and a global count, and finally calls AbyssEngine::ApplicationManager::GetCurrentApplicationModule()==5 on the current app-manager instance. Its array base and field layout belong to the input/keymap TU (recovered_7a41c.cpp), not NFC. Same blocker as SetKeyCode: correct implementation requires reaching the internal-linkage g_virtualKeys table (and the input TU's other module-private globals), which I cannot do from NFC.cpp without exposing that static (edit to recovered_7a41c.cpp, outside my component) or writing a forbidden stub layout. Additionally several of the auxiliary globals it dereferences (the count at 0x227ccc and the pointers at 0x227cd0/0x227ce8) are ambiguously labeled by Ghidra and would need confident naming. Move: implement in src/platform/recovered_7a41c.{h,cpp}.
